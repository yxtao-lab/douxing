# 兜行 · 移动端公共组件

本文档说明 `packages/mobile` 中可复用的 Vue 组件：功能、封装方式、依赖与用法。新增公共组件时请同步更新此文档。

> **Web 管理端**表格、筛选栏、导出按钮等见 [Web管理端表格规范.md](./Web管理端表格规范.md)（`packages/web`），不在本文范围。  
> **PC 用户端**子页壳见 `packages/pc/src/components/SubPageShell.vue`；三端新页面 UI 与组件复用总则见 [Cursor-Agent规则.md](./Cursor-Agent规则.md) · `.cursor/rules/page-ui-standards.mdc`。

组件目录：`packages/mobile/src/components/`

---

## 组件索引

| 组件 | 路径 | 说明 |
|------|------|------|
| **VoiceTextComposer** | `voice-text-composer/VoiceTextComposer.vue` | 文字 + 按住说话双模式输入框 |
| **PlanPetFocusCard** | `plan/PlanPetFocusCard.vue` | 规划页宠物 Focus 卡片（口吻化 assistant · 记忆摘要） |
| **TravelPetFloatingLayer** | `travel-pet-floating-layer/TravelPetFloatingLayer.vue` | 全站悬浮旅行伙伴（H3-b） |
| DouxingTabBar | `douxing-tab-bar/DouxingTabBar.vue` | 底部 Tab 导航（兜行业务） |
| AiPlanBlockingOverlay | `ai-plan-blocking-overlay/AiPlanBlockingOverlay.vue` | AI 规划全屏阻塞遮罩（含 Tool 进度列表） |
| AgentToolStatusList | `agent-tool-status-list/AgentToolStatusList.vue` | Agent Tool 执行状态列表（SSE 进度） |
| **PetInTripAnalyzeSheet** | `travel-pet/PetInTripAnalyzeSheet.vue` | 行中宠物 AI 分析结果 Sheet（H3-d） |
| **PetCheckInCelebrationSheet** | `travel-pet/PetCheckInCelebrationSheet.vue` | 打卡庆祝动画与 exp 提示（H3-d） |

---

## VoiceTextComposer · 语音文字输入框

### 功能

统一的聊天式底部输入组件，支持：

- **文字模式（默认）**：多行输入、发送按钮、可切换语音
- **语音模式**：按住说话、松开发送、上滑取消
- **录音态 UI**：蓝色胶囊按钮 + 竖条声波动画；取消时变红
- **跨端语音**：H5（Web Speech API）、微信小程序 / App（录音上传 + 服务端 ASR）

业务侧只需处理 `@send` 返回的文本，无需关心录音、识别、模式切换等细节。

### 封装方式

| 层级 | 说明 |
|------|------|
| **组件** | UI、模式切换、按住/松开手势、上滑取消、波形动画 |
| **工具** | `@/utils/speech-to-text.ts` 按平台条件编译，统一 `startSpeechToText` / `stopSpeechToText` |
| **录音** | `speech-to-text.recorder-upload.ts` 封装 `RecorderManager`，松手后上传音频 |
| **接口** | `@/api/speech.ts` → 后端 `POST /api/speech/transcribe` |

设计要点：

- 使用 **`v-model`** 绑定输入内容；发送逻辑由父组件通过 **`@send`** 处理
- 录音 session、按住状态、异步启动竞态均在组件内部管理
- 通过 **`defineExpose({ clear })`** 暴露清空方法，供发送成功后重置输入框
- 样式自包含（`scoped`），不依赖页面级 CSS 变量

### 依赖（其他项目接入时须一并复制或等价实现）

```
packages/mobile/src/components/voice-text-composer/VoiceTextComposer.vue
packages/mobile/src/utils/speech-to-text.ts
packages/mobile/src/utils/speech-to-text.shared.ts
packages/mobile/src/utils/speech-to-text.h5.ts
packages/mobile/src/utils/speech-to-text.mp-weixin.ts
packages/mobile/src/utils/speech-to-text.app.ts
packages/mobile/src/utils/speech-to-text.recorder-upload.ts
packages/mobile/src/api/speech.ts
packages/server/src/routes/speech.ts          # 语音识别 API
packages/server/src/services/speech.service.ts
```

小程序还需在 `manifest.json` 中声明录音权限，并在微信公众平台配置合法域名。

### Props

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `modelValue` | `string` | `''` | 输入内容（`v-model`） |
| `placeholder` | `string` | `'请输入内容'` | 占位提示 |
| `disabled` | `boolean` | `false` | 禁用输入与录音 |
| `loading` | `boolean` | `false` | 发送按钮 loading |
| `defaultMode` | `'keyboard' \| 'voice'` | `'keyboard'` | 初始输入模式 |
| `maxlength` | `number` | `500` | 最大字数 |
| `sendLabel` | `string` | `'发送'` | 发送按钮文案 |
| `holdLabel` | `string` | `'按住 说话'` | 语音模式按钮文案 |
| `recordingHint` | `string` | `'松开发送  上滑取消'` | 录音中顶部提示 |
| `cancelHint` | `string` | `'松开手指，取消发送'` | 上滑取消后提示 |
| `slideCancelThresholdPx` | `number` | `50` | 上滑取消阈值（px） |

### 事件

| 事件 |  payload | 说明 |
|------|----------|------|
| `update:modelValue` | `string` | 输入内容变化（`v-model`） |
| `send` | `string` | 点击发送，或语音识别完成后触发，参数为待发送文本 |

### 暴露方法

| 方法 | 说明 |
|------|------|
| `clear()` | 清空内容并重置 `textarea`（发送成功后调用） |

### 基本用法

```vue
<template>
  <VoiceTextComposer
    ref="composerRef"
    v-model="inputText"
    placeholder="描述你的需求..."
    :disabled="loading"
    :loading="loading"
    @send="handleSend"
  />
</template>

<script setup lang="ts">
import { ref } from 'vue';
import VoiceTextComposer from '@/components/voice-text-composer/VoiceTextComposer.vue';

const composerRef = ref<InstanceType<typeof VoiceTextComposer> | null>(null);
const inputText = ref('');
const loading = ref(false);

async function handleSend(text: string) {
  const content = text.trim();
  if (!content) return;

  loading.value = true;
  composerRef.value?.clear();

  try {
    await submitMessage(content);
  } catch {
    inputText.value = content; // 失败时恢复输入
  } finally {
    loading.value = false;
  }
}
</script>
```

### 默认语音模式

```vue
<VoiceTextComposer
  v-model="inputText"
  default-mode="voice"
  @send="handleSend"
/>
```

### 项目内引用示例

AI 智能规划页 `packages/mobile/src/pages/plan/plan.vue` 已接入，可作为完整参考。

### 交互说明

1. **文字模式**：输入 → 点「发送」或键盘确认 → 触发 `send`
2. **语音模式**：按住 → 录音 + 波形 → 松开 → 识别 → 自动 `send`
3. **上滑取消**：录音过程中上滑超过阈值 → 按钮变红 → 松开放弃识别与发送

---

## 后续组件

新增公共组件时，请在本文件「组件索引」中登记，并补充：功能、封装方式、Props / 事件、依赖与示例代码。

**封装原则**（与 `page-ui-standards.mdc` 一致）：开发新页面前先查本索引与同端 `components/`；同一 UI 块跨页出现 ≥2 次或单块 >~80 行时须抽为组件，样式自包含（`scoped`），用户可见文案走 i18n。
