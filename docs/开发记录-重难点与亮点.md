# 兜行 · 开发记录（重难点与亮点）

> 记录每项交付任务的**思考过程**与**最终方案**。  
> 路线图 [ROADMAP.md](./ROADMAP.md) 只保留进度与验收；本文件专注「为什么这样设计、踩过什么坑」。

**文档版本**：1.22  
**更新日期**：2026-06-01  
**维护约定**：**每完成一项可验收任务必须更新本章**（勿只改代码/路线图）；每章含：思考过程、实现原理、最终方案，以及 **2～3 亮点 + 2～3 重难点**，篇幅从简。

### 按阶段速查

| 阶段 | 章节 |
|------|------|
| M0 | [M0-7](#m0-7-手机验证码登录--腾讯云短信) |
| A | [A1](#a1-用户资料与偏好) · [A2](#a2-景点内容基础库) · [A2+](#a2-ai-景点同步pending--合并) · [A2++](#a2-poi-分类过滤入库) · [A2+++ P0](#a2-景点封面-phase-0) · [A2+++ P1](#a2-景点封面-phase-1) · [A3](#a3-路线能力补全) · [A3-1](#a3-1-ai-路线重新生成prompt--景点同步) · [A3-2](#a3-2-移动端-ai-规划全局-loading-与中断) · [A3-3](#a3-3-路线详情地图预览动画) · [A3++](#a3-草稿编辑入口分离) · [A3+++](#a3-草稿编辑弹窗与发布状态) · [A4](#a4-订单状态机) |
| B | [B1](#b1-打卡字段增强) · [B2](#b2-地理围栏打卡) · [B3](#b3-打卡地图-ui) · [B4](#b4-徽章系统) · [B5](#b5-成就体系完善) · [B6](#b6-排行榜) |
| C | [C1](#c1-多轮对话规划) · [C1+](#c1-路线解锁支付开关) · [C2](#c2-意图结构化解析) · [C3](#c3-rag-景点检索) · [C4](#c4-多方案生成) · [C5](#c5-python-ai-微服务) |
| H | [H9-1](#h9-1-enricher-mvpphase-1) · [H9-1 UI](#h9-1-ui-行程流程图与展示策略) · [H9-2](#h9-2-路网-polyline-与酒店-ragphase-2) · [H9-4](#h9-4-玩法-rag-与段间交通phase-4) · [H9 方案](#h9-住宿与交通编排llm-排-poi--后端-enricher) · [H1-c](#h1-c-全站视觉统一) · [H2-a+D5-a](#h2-a--d5-a-路线手帐海报与分享链路) · [H2-a 内容增强](#h2-a-手帐内容与行程展示增强) |
| I | [I1](#i1-ml-训练数据生成与校验) |
| G | [G8](#g8-生产部署与-api-公有化) · [G9](#g9-国际化i18n) |
| 基础设施 | [高德](#高德地理编码补坐标) · [Redis](#redis-地理编码缓存) · [Drizzle](#数据库生成与使用命令drizzle) |

---

## 目录

1. [M0-7 手机验证码登录 + 腾讯云短信](#m0-7-手机验证码登录--腾讯云短信)
2. [A1 用户资料与偏好](#a1-用户资料与偏好)
3. [A2 景点/内容基础库](#a2-景点内容基础库)
4. [A2+ AI 景点同步（pending + 合并）](#a2-ai-景点同步pending--合并)
5. [A2++ POI 分类过滤入库](#a2-poi-分类过滤入库)
6. [高德地理编码补坐标](#高德地理编码补坐标)
7. [Redis 地理编码缓存](#redis-地理编码缓存)
8. [数据库生成与使用命令（Drizzle）](#数据库生成与使用命令drizzle)
9. [A3-1 AI 路线重新生成（prompt + 景点同步）](#a3-1-ai-路线重新生成prompt--景点同步)
10. [A3-2 移动端 AI 规划全局 Loading 与中断](#a3-2-移动端-ai-规划全局-loading-与中断)
11. [A3 路线能力补全](#a3-路线能力补全)
12. [A4 订单状态机](#a4-订单状态机)
13. [B1 打卡字段增强](#b1-打卡字段增强)
14. [B2 地理围栏打卡](#b2-地理围栏打卡)
15. [B3 打卡地图 UI](#b3-打卡地图-ui)
16. [B4 徽章系统](#b4-徽章系统)
17. [B5 成就体系完善](#b5-成就体系完善)
18. [B6 排行榜](#b6-排行榜)
19. [C1 多轮对话规划](#c1-多轮对话规划)
20. [C1+ 路线解锁支付开关](#c1-路线解锁支付开关)
21. [C2 意图结构化解析](#c2-意图结构化解析)
22. [C3 RAG 景点检索](#c3-rag-景点检索)
23. [C4 多方案生成](#c4-多方案生成)
24. [C5 Python AI 微服务](#c5-python-ai-微服务)
25. [A3++ 草稿编辑入口分离](#a3-草稿编辑入口分离)
26. [A3+++ 草稿编辑弹窗与发布状态](#a3-草稿编辑弹窗与发布状态)
27. [A3-3 路线详情地图预览动画](#a3-3-路线详情地图预览动画)
28. [G8 生产部署与 API 公有化](#g8-生产部署与-api-公有化)
29. [G9 国际化（i18n）](#g9-国际化i18n)
30. [H9 住宿与交通编排（LLM 排 POI + 后端 Enricher）](#h9-住宿与交通编排llm-排-poi--后端-enricher)
31. [H9-1 Enricher MVP（Phase 1）](#h9-1-enricher-mvpphase-1)
32. [H9-1 UI 行程流程图与展示策略](#h9-1-ui-行程流程图与展示策略)
33. [H9-2 路网 polyline 与酒店 RAG（Phase 2）](#h9-2-路网-polyline-与酒店-ragphase-2)
34. [H9-4 玩法 RAG 与段间交通（Phase 4）](#h9-4-玩法-rag-与段间交通phase-4)
35. [H1-c 全站视觉统一](#h1-c-全站视觉统一)
36. [H2-a + D5-a 路线手帐海报与分享链路](#h2-a--d5-a-路线手帐海报与分享链路)
37. [D5 补全 · 海报二维码与行程全量展示](#d5-补全--海报二维码与行程全量展示)
38. [A2+++ 景点封面 Phase 0](#a2-景点封面-phase-0)
39. [I1 ML 训练数据生成与校验](#i1-ml-训练数据生成与校验)

---

## M0-7 手机验证码登录 + 腾讯云短信

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | M0-7 |
| **关联代码** | `packages/server/src/services/sms.service.ts`、`config/sms.ts`、`routes/auth.ts`；Web/移动端登录页 |

### 背景与目标

MVP 需要手机号即可登录，且生产环境能发真实短信。设计文档要求验证码登录，但不宜在第一阶段接入多套云厂商。

### 思考过程

1. **登录与注册是否要两个接口？** 体验上用户只想「输手机号 → 收码 → 进 App」。若强行区分注册/登录，会增加表单与文案分支；验证码通过后查 `users.phone` 即可分流建号或发 Token。  
2. **短信 SDK 选型**：先在阿里云与腾讯云之间评估。阿里云 Node SDK 在 `NodeNext` + ESM 下出现 default export 无法 `new` 的问题，修 SDK 成本高；产品侧也更倾向腾讯云，故统一 `tencentcloud-sdk-nodejs`。  
3. **验证码存哪？** 首版无 Redis，用进程内 `Map` + TTL 最快可演示；接受多实例不一致，在路线图 G1 再迁 Redis。  
4. **开发环境没密钥怎么办？** `SMS_PROVIDER=mock` 时在响应/控制台给出 `devCode`，避免阻塞前后端联调。

### 最终方案

- API：`POST /auth/sms/send`、`POST /auth/sms/login`；校验通过后无用户则 `u_{phone}` 自动注册。  
- `config/sms.ts`：`auto | tencent | mock`，配置齐全走腾讯云，否则 mock。  
- 限流：60s 发送冷却、5 分钟 TTL、单码最多校验 5 次。  
- 模板参数用 `TemplateParamSet: [code]`（腾讯云 `{1}` 占位顺序）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **登录注册合一** | 一套接口 + 一个按钮覆盖新/老用户，前端无「注册/登录」分叉。 |
| 2 | **短信渠道可插拔** | `SMS_PROVIDER` 切换腾讯云与 mock，开发不依赖真实计费短信。 |
| 3 | **失败可观测** | mock 模式返回 `devCode`，联调时不必反复查库或日志猜验证码。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **ESM 与云 SDK 兼容** | 阿里云 v4 构造器在 `NodeNext` 下不可用；改为腾讯云 SDK，并在 `package.json` 保持 `"type": "module"` 一致。 |
| 2 | **腾讯云模板参数顺序** | 占位符是 `{1}`、`{2}` 而非命名参数；用数组传参，可选 `TENCENT_SMS_TEMPLATE_MINUTES` 作为第二参数。 |
| 3 | **验证码存储与多实例** | 首版进程内 `Map` 实现简单，但多 Pod 会不一致；文档标明生产需 G1 Redis，避免误以为当前方案可原样上线。 |

---

## A1 用户资料与偏好

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A1 |
| **关联代码** | `routes/users.ts`、`services/user.service.ts`；`packages/mobile/src/pages/profile/edit.vue`；迁移 `0003_user_interest_tags.sql` |

### 背景与目标

验证码登录后用户只有手机号，无法支撑个性化路线推荐。需要可编辑昵称、头像、兴趣标签，并在移动端「我的」入口完成闭环。

### 思考过程

1. **头像是否等 OSS？** G2 对象存储未做，若阻塞 A1 会拖慢整阶段。折中：本地上传至 `uploads/avatars`，用 `API_PUBLIC_BASE_URL` 拼公开 URL，接口形状与未来 OSS 一致。  
2. **兴趣标签自由输入还是预设？** 完全自由会导致推荐与统计口径混乱；采用 `USER_INTEREST_PRESETS` + 最多 8 个，服务端校验，前端只做选择器。  
3. **资料接口粒度**：`GET/PUT /users/me` 与 `POST /users/me/avatar` 分离，避免 multipart 与 JSON 混在一个 PUT 里增加客户端复杂度。

### 最终方案

- `users.interest_tags` JSON 列；`PUT /users/me` 更新昵称、邮箱、标签；头像单独 multipart 上传。  
- 共享常量 `USER_INTEREST_PRESETS`、`USER_INTEREST_MAX` 同步到 `@douxing/shared`。  
- 移动端 `profile/edit.vue` 拉取/提交资料，与 Tab「我的」联动。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **先本地存储、后 OSS 演进** | 头像 URL 模型不变，后续 G2 只换存储实现，不改客户端契约。 |
| 2 | **标签预设 + 服务端校验** | 保证推荐与路线 `interestTags` 口径一致，避免脏标签进入库。 |
| 3 | **类型全仓共享** | `UpdateUserProfileRequest`、`UserInfo.interestTags` 在 shared 包，Web/移动/服务端同一套类型。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **`.env` 与公开 URL** | 头像外链依赖 `API_PUBLIC_BASE_URL`；未配置时回退 `req.protocol + host`，小程序真机需显式配局域网或域名。 |
| 2 | **multipart 与鉴权顺序** | 先 `authMiddleware` 再 `multer`，文件名带 `userId-timestamp` 防覆盖；限制 2MB 与图片 MIME。 |
| 3 | **迁移与旧库** | 新列 `interest_tags` 必须 `db:migrate`；已有用户默认为 `NULL`，前端展示为空数组而非报错。 |

---

## A2 景点/内容基础库

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A2 |
| **关联代码** | `schema/attractions.ts`、`data/attraction-seeds.ts`、`services/attraction.service.ts`、`routes/attractions.ts`；迁移 `0004_attractions.sql` |

### 背景与目标

路线与打卡长期不能只靠字符串景点名。需要主数据表（城市、坐标、标签），并让 `route_detail` 能挂上 `attractionId`，为 RAG、地图打卡铺路。

### 思考过程

1. **表结构先做多少？** 首版不做全文检索、不做向量；`attractions` + 索引（city、name）即可支撑列表 API 与关联。  
2. **匹配发生在什么时候？** 放在路线写入前做一次 enrich，比打卡时再猜名字更稳；打卡直接带 ID。  
3. **种子数据从哪来？** 与 `route-templates.ts` 中景点名对齐，人工补坐标与别名，保证演示路线高匹配率。

### 最终方案

- `attractions` 表 + 28 条种子（杭沪京蓉等）。  
- `GET /api/attractions`、`/cities`、`/:id`。  
- `enrichRouteDetailWithAttractionIds`：按城市 + 名称/别名/包含匹配写回 `attractionId`。  
- `check_ins.attraction_id` 可空外键。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **路线与主数据联动** | 生成/种子路线后节点带 `attractionId`，打卡、成就可复用同一 ID。 |
| 2 | **种子与模板对齐** | 模板里出现的景点名在库里都有对应行，演示时匹配率接近 100%。 |
| 3 | **别名体系** | `aliases` JSON 解决「断桥 / 断桥残雪」等叫法差异，减少重复建点。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **名称不完全一致** | 三层匹配：精确名 → 精确别名 → 包含；包含匹配在 A2+ 中降权，避免误合并。 |
| 2 | **仅关联、不扩充** | A2 初版未写入新景点，库外景点仍无 ID；推动 A2+ 做 AI 同步。 |
| 3 | **旧路线不自动修复** | seed 对已有 `travel_routes` skip；旧数据需重新生成或 `db:reset` 才带 ID。 |

---

## A2+ AI 景点同步（pending + 合并）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A2 增强（路线图 §2.4 记为 A2+） |
| **关联代码** | `services/attraction-sync.service.ts`、`0005_attraction_sync_meta.sql`；`routes/attractions.ts` 审核接口 |

### 背景与目标

A2 只读匹配导致库增长慢、LLM 新景点无 ID。目标：路线生成后**反哺景点库**，但不用 AI 幻觉数据覆盖人工种子。

### 思考过程

1. **全量覆盖行不行？** 不行。LLM 的 `cost` 是估算，坐标常缺失，覆盖种子会破坏信任。应分 **来源（source）** 与 **状态（pending/active）**。  
2. **何时合并、何时新建？** 需要置信度：精确 1.0 / 别名 0.95 才合并；模糊 0.72 只关联风险大，改为新建 `pending`。阈值取 `0.85`。  
3. **pending 用户能用吗？** 生成路线的人需要 `attractionId` 打卡；公开列表只展示 `ACTIVE`，待审数据不污染搜索。  
4. **票价什么叫「最新」？** 仅 `llm_estimate` 且在价为空或超过 90 天可更新；`seed/manual` 永不因 AI 改价。

### 最终方案

- `syncAttractionsFromRouteDetail` 替代纯 enrich：匹配则 `merge`，否则 `INSERT pending`。  
- 字段：`source`、`price_source`、`match_confidence`、`verified_at` 等；坐标可空。  
- 管理端：`GET /attractions/admin/pending`、`POST .../approve` 转 `ACTIVE`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **生成即入库** | 每次规划自动扩充库，匹配率随使用次数上升。 |
| 2 | **分来源合并策略** | 信任数据（seed/manual）防覆盖；llm 行可渐进补全。 |
| 3 | **待审与公开隔离** | 用户侧路线/打卡可用 pending ID；C 端景点列表只见已审核。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **模糊匹配误合并** | 低于 0.85 不合并，新建 pending；精确/别名才更新已有行。 |
| 2 | **AI 无可靠坐标** | pending 允许 `latitude/longitude` 为 NULL；B2 围栏前需地图 API 或人工审核补全。 |
| 3 | **票价不可当真理** | `price_source=llm_estimate` 明示参考价；禁止覆盖 seed 票价，避免误导购票。 |

---

## A2++ POI 分类过滤入库

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A2 增强 |
| **关联代码** | `utils/poi-classifier.ts`、`llm-client.service.ts`、`attraction-sync.service.ts`；迁移 `0006_attraction_category` |

### 思考过程

- 路线节点里「午餐」「住宿」不是 POI，写入主数据会污染检索与地图。  
- 具体饭店/酒店要入库，但和景区应分 `category`，匹配时不能混并。  
- 景点若无坐标，后期围栏与地图无法落地，需在 LLM 与入库两侧约束。

### 实现原理

`poiType` 驱动是否调用 `sync`：仅 `attraction/restaurant/hotel` 且通过名称黑名单的节点写库；`meal/transport/other` 只留在 `route_detail` JSON。LLM 输出用 Zod 校验 `poiType` 与店名规范（景点坐标改为可选，由同步阶段高德补全）。同步链路：**先库内匹配合并**，未匹配再 pending 新建（无坐标也可入库待补全）。

### 最终方案

笼统用餐不入库；具体店名入库；表增加 `category` 字段。景点坐标优先 LLM/高德，匹配阈值 0.85 以上才合并已有行。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **展示与主数据分离** | 路线仍显示「午餐」，但不产生 `attractionId`。 |
| 2 | **三类 POI 分栏** | `attraction` / `restaurant` / `hotel` 分开匹配与展示。 |
| 3 | **LLM 契约前置** | 系统提示 + Zod 约束，减少脏数据进库。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **泛称识别** | 黑名单 + `poiType=meal`；饭店/酒店须具体店名。 |
| 2 | **无坐标景点** | 先尝试匹配已有库；未匹配则 pending 入库待补坐标（高德见下章），不再「无坐标直接跳过」。 |
| 3 | **模板路线无 poiType** | `inferPoiCategory` 按名称关键字回退推断。 |

---

## 高德地理编码补坐标

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A2 增强 / B2 前置 |
| **关联代码** | `config/amap.ts`、`services/amap-geocode.service.ts`；[amap-geocoding.md](./amap-geocoding.md) |

### 思考过程

- LLM 常漏坐标，不能把无坐标景点硬入库。  
- 高德有 **place/text**（按名称搜 POI）和 **geocode/geo**（按地址），景点名应优先前者。  
- Key 未配置时应静默降级，不阻塞路线生成。

### 实现原理

`enrichSpotCoordinates` 在 `sync` 前调用：无坐标则请求高德 REST；`location` 为「经度,纬度」解析为 GCJ-02 写入表。链路：`place/text` 失败 → `geocode/geo` 回退。未配置 `AMAP_WEB_KEY` 时跳过。

### 最终方案

同步 POI 时自动补坐标；环境变量控制开关；坐标系统一用高德 GCJ-02，便于国内地图与后续围栏。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **双接口回退** | 名称搜 POI 优先，地址 geocode 兜底。 |
| 2 | **可插拔** | 无 Key 时与改造前行为一致。 |
| 3 | **接入点单一** | 只挂在 `attraction-sync`，业务无感。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **Key 类型** | 必须用 Web 服务 Key，非 JS/小程序 Key。 |
| 2 | **经纬度顺序** | 高德返回 `lng,lat`，解析时勿颠倒。 |
| 3 | **配额** | 同景点重复生成会多次调用 → 下一章 Redis 缓存。 |

---

## Redis 地理编码缓存

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | G1 前置 / A2 增强 |
| **关联代码** | `config/redis.ts`、`geocode-cache.service.ts`、`redis-client.service.ts`；`docker-compose.yml` → `redis` |

### 思考过程

- 同一「城市 + 景点名」在多次路线生成中会重复打高德，浪费日配额。  
- Redis 在路线图 G1 已规划，可先只缓存 geocode，不引入全站缓存复杂度。  
- Redis 宕机不能影响主流程，应降级为直调高德。

### 实现原理

`resolveCoordinatesFromAmap` 先 `GET douxing:amap:geocode:{city}:{name}`（名称归一化），命中直接返回；未命中调高德，成功则 `SET EX`（默认 TTL 30 天）。`ioredis` 懒连接，失败置 `disabled` 标志不再重试连接。

### 最终方案

配置 `REDIS_URL` 即启用；`docker compose up -d redis`；与高德 Key 配合使用。开发环境缓存命中打日志 `[amap] 缓存命中`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **按 city+name 去重** | 键简单、命中率高、与业务语义一致。 |
| 2 | **失败自动降级** | 无 Redis 时仍可调高德。 |
| 3 | **Docker 一键起** | compose 已含 `redis` 服务与数据卷。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **ESM 引 ioredis** | 使用 `import { Redis } from 'ioredis'`，避免 default 构造报错。 |
| 2 | **只缓存成功结果** | 失败不写入，避免错误坐标长期生效。 |
| 3 | **多实例共享** | 生产多 Pod 共用同一 Redis 才能真正省配额。 |

---

## 数据库生成与使用命令（Drizzle）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | 横切基础设施（M0 起积累，A2/A2+ 迁移实践沉淀） |
| **关联代码** | `packages/server/drizzle/`、`src/db/migrate.ts`、`seed.ts`、`drizzle.config.ts`；根脚本 `db:*` |

### 背景与目标

Monorepo 多人协作需要可版本化的表结构变更；新人 clone 后一条命令能起 MySQL + 表 + 演示数据。

### 思考过程

1. **为什么不用手工改表？** 无法 Code Review、环境不一致；Drizzle schema 即文档，SQL 迁移可审计。  
2. **`generate` 和 `migrate` 为何常混淆？** kit 只 diff 产出文件；真正执行 DDL 的是 `migrate.ts` + `__drizzle_migrations`。必须在文档里画清流水线。  
3. **Docker 与脚本竞态** | `bootstrap` 起容器后宿主机端口未必立刻可用；`waitForDatabase` 轮询后再 `db:setup`。  
4. **seed 边界** | seed 只做 DML 且幂等 skip，不能代替 migrate；否则新人以为「跑 seed 就能加列」。

### 最终方案

- 三层：schema TS → `pnpm db:generate` → `pnpm db:migrate`；数据：`pnpm db:seed`。  
- 根目录转发 `db:migrate` / `db:seed` / `db:setup` / `db:reset`。  
- `bootstrap:dev` = Docker + wait + `db:setup` + `pnpm dev`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **Monorepo 根命令转发** | 根目录即可 `pnpm db:migrate`，不必记住 server 包路径。 |
| 2 | **自动建库 + 等待** | `ensureDatabase` 创建库并等待 TCP 就绪，降低 Docker 新人失败率。 |
| 3 | **单一 `DATABASE_URL`** | drizzle-kit、migrate、seed、运行时共用 `.env`，减少配错连接串。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **generate ≠ migrate** | 改 schema 后必须 generate + 人工审 SQL + migrate；漏 migrate 表现为「表不存在」。 |
| 2 | **journal 不可乱改** | 已执行 tag 记在 `__drizzle_migrations`；调整历史顺序会导致环境漂移。 |
| 3 | **seed 不改表结构** | 新列/新表只能走迁移；旧业务数据不会因 seed 自动补新字段（如 `attractionId`）。 |

---

## A3-1 AI 路线重新生成（prompt + 景点同步）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A3 路线能力补全（增强） |
| **关联代码** | `routes/routes.ts`（`POST /:id/regenerate`）、`services/route.service.ts`；`utils/route-detail.util.ts`；`pages/routes/detail.vue`；`packages/shared` 类型 `sourcePrompt` / `RegenerateRouteRequest` |

### 背景与目标

用户对 AI 首次生成不满意时，应能**改 prompt 覆盖草稿**而非只能新建路线；重新生成后新 POI 须与首次生成一样写入 `attractions`（pending/合并），并写回 `attractionId`。

### 思考过程

1. **新建还是原地更新？** 原地更新同一条草稿可保留订单/收藏关联（若有），且列表不膨胀；仅 `DRAFT` 可覆盖，已发布路线拒绝 regenerate 并提示去规划页新建。  
2. **旧 `attractionId` 要不要保留？** 行程节点已变，保留旧 ID 会指向错误 POI；重新生成时**剥离节点上的 `attractionId`**，强制走完整 `syncAttractionsFromRouteDetail`。  
3. **同步与创建是否同链路？** 抽 `syncDraftToAttractionLibrary`，创建与 regenerate 共用；避免 regenerate 漏调 sync 或传入被模板常量污染的 `routeDetail`。  
4. **无坐标就跳过是否合理？** 原逻辑在匹配前就要求坐标，导致「库里有、但补坐标失败」的景点也不关联；改为**先匹配、再决定是否新建 pending**（无坐标可 pending）。

### 实现原理

`regenerateRouteFromPrompt`：`generateRoute` → `normalizeRouteDetailDays` + 深拷贝 → `sync` → 更新 `travel_routes` 与 `route_detail`（含 `sourcePrompt`、`llmProviderChoice`）。`attraction-sync` 调整顺序：高德补坐标 → `resolveAttractionMatch` → 合并或 `insertPendingAttraction`。

### 最终方案

- API：`POST /api/routes/:id/regenerate`，body 同 `/generate`（`prompt`、可选 `provider`）。  
- 详情页草稿展示「修改需求重新生成」；`route_detail.sourcePrompt` 记录原始需求。  
- 重新生成后 `isUnlocked` 重置为 `false`（内容已变）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **草稿原地迭代** | 同一路线 ID 反复调 prompt，适合 MVP 快速试错。 |
| 2 | **同步链路复用** | 创建/regenerate 共用 `syncDraftToAttractionLibrary`，景点库行为一致。 |
| 3 | **需求可追溯** | `sourcePrompt` 写入 `route_detail`，详情页可预填上次描述。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **重新生成不入库** | 根因是同步前无坐标即 return；改为先匹配、未匹配再 pending；regenerate 剥离旧 `attractionId`。 |
| 2 | **模板对象被污染** | `ROUTE_TEMPLATES` 为模块级常量；LLM/模板返回前深拷贝 `days`，避免多次生成共享引用。 |
| 3 | **已发布不可覆盖** | `status !== DRAFT` 抛错，防止已分享路线被静默替换；需改需求则复制 prompt 到规划页新建。 |

---

## A3-2 移动端 AI 规划全局 Loading 与中断

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A3 / C 阶段 AI 体验前置 |
| **关联代码** | `utils/ai-plan-loading.ts`、`utils/request.ts`（`requestAiPlan`）；`components/ai-plan-blocking-overlay`；`DouxingTabBar.vue`；`pages/plan/plan.vue`、`pages/routes/detail.vue` |

### 背景与目标

LLM 生成路线耗时可达数十秒，期间用户若切换 Tab 或点其他按钮会离开流程、产生重复请求。需要**全屏阻塞 + 可取消**，与后端 abort 对齐。

### 思考过程

1. **用 `uni.showLoading` 够不够？** 原生 loading 无自定义「取消」、难拦截 Tab；自定义全屏遮罩可统一文案与按钮。  
2. **遮罩放哪？** 小程序 `App.vue` 模板不盖页面；挂在**各 Tab 页的 `DouxingTabBar`** + 非 Tab 的 `detail.vue`，保证 z-index 高于 TabBar（100000）。  
3. **如何真正中断？** `uni.request` 返回 `RequestTask`，`abort()` 可取消；封装 `requestAiPlan` 注册 task，取消时 reject 为 `已取消路线规划`，业务侧不 toast 失败。  
4. **如何防跳转？** `uni.addInterceptor` 拦截 `switchTab`/`navigateTo`/`navigateBack` 等；TabBar `onSwitch` 再判 `isAiPlanLoading()` 双保险。

### 实现原理

`beginAiPlanLoading` 置 `active` 并安装导航拦截；`requestAiPlan` 在 `finally` 中 `endAiPlanLoading`；用户点「取消规划」→ `cancelAiPlanLoading` → `task.abort()`。仅 `generateRoute` / `regenerateRoute` 走该封装，其它 API 不受影响。

### 最终方案

- 全屏遮罩：转圈 + 文案 +「取消规划」。  
- 生成/重新生成期间禁用 Tab、模型选择、重复提交。  
- 取消后停留当前页，不跳转详情。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **可中断的长请求** | 用户不必杀进程或关小程序，体验接近原生「取消」。 |
| 2 | **导航双保险** | 拦截器 + TabBar 判断，降低误触切页概率。 |
| 3 | **AI 请求隔离** | `requestAiPlan` 与普通 `request` 分离，避免全站请求都带遮罩。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **全局 UI 在 UniApp 的挂载点** | 不能依赖 App 根模板；随 TabBar/详情页组件挂载遮罩，覆盖当前栈顶页。 |
| 2 | **abort 错误文案不一** | 各端 `errMsg` 含 `abort`/`cancel` 不一；`isRequestAbortedError` 统一识别，映射为固定中文提示。 |
| 3 | **取消与 complete 竞态** | `abort` 后仍会走 `fail`/`complete`；`finally` 里 `endAiPlanLoading` 幂等，避免遮罩残留。 |

---

## A3 路线能力补全

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-21 |
| **路线图** | A3 |
| **关联代码** | 迁移 `0007_route_engagement.sql`；`route-interaction.service.ts`、`route.service.ts`；`routes.ts`；移动端 `routes/list.vue`、`routes/detail.vue` |

### 思考过程

1. **计数存列还是实时聚合？** 热门榜需按浏览/点赞排序，若每次 `COUNT(*)` 关联表会在列表接口放大查询成本。折中：`travel_routes` 冗余 `view_count` / `like_count` / `collect_count`，用户关系表 `route_likes`、`route_favorites` 保证唯一，切换时同步增减计数。  
2. **点赞与收藏是否合并一张表？** 语义与后续通知不同，拆表更清晰；接口分别为 `POST .../like` 与 `.../favorite`，移动端独立按钮。  
3. **草稿编辑边界？** 仅 `status=DRAFT` 可 `PUT` 更新名称与简介（MVP 先覆盖高频字段），已发布路线拒绝静默改写，与 regenerate 规则一致。  
4. **列表 scope 设计？** 用 `scope=mine|hot|favorites` 统一 `GET /routes`，另提供 `GET /routes/hot` 便于首页直达；热门仅返回已发布路线。

### 实现原理

详情 `GET` 时 `view_count + 1`；点赞/收藏 toggle 先查关系表再更新计数；列表按 scope 分支查询，对当前用户批量补齐 `isLiked` / `isFavorited`；热门排序默认 `like_count * 2 + view_count`。

### 最终方案

- 迁移新增统计列与两张关系表；`toRouteInfo` 抽到 `route-info.util.ts` 避免循环依赖。  
- API：`GET /routes`（筛选/排序）、`GET /routes/hot`、`PUT /routes/:id`、`POST .../like`、`POST .../favorite`。  
- 移动端路线 Tab：我的/热门/收藏 + 草稿状态筛选；详情页展示统计、互动；**地图路线预览动画**；草稿编辑为 hero 按钮 + 弹窗（见 A3+++）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **列表 scope 统一入口** | 一套 `GET /routes` 查询参数覆盖我的/热门/收藏，减少客户端分支接口。 |
| 2 | **关系表 + 冗余计数** | 保证用户级唯一点赞/收藏，同时支持热门排序无需重聚合。 |
| 3 | **详情自动计浏览** | 打开详情即 +1，管理端与移动端统计口径一致。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **服务层循环依赖** | `route.service` 与互动逻辑互相引用；抽出 `route-info.util.ts` 单向依赖。 |
| 2 | **Express 路由顺序** | `/hot` 必须在 `/:id` 之前注册，避免被当成 id。 |
| 3 | **旧库迁移** | 新列默认 0；已有路线需执行 `pnpm --filter @douxing/server db:migrate` 后接口才返回统计字段。 |

---

## A4 订单状态机

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-21 |
| **路线图** | A4 |
| **关联代码** | `packages/shared/src/order-state.ts`；`order-transition.service.ts`、`order.service.ts`、`jobs/order-timeout.job.ts`；`routes/orders.ts` |

### 背景与目标

MVP 仅有模拟支付，缺少明确的状态流转与待支付超时关闭，易出现重复支付、僵尸待支付单。A4 要求 `pending→paid→completed/cancelled` 与超时取消。

### 思考过程

1. **状态表放 shared 还是仅 server？** 前后端都要展示状态文案，流转规则也应单一来源；`canTransitionOrderStatus` 放 `@douxing/shared`，服务端 CAS 更新放 server。  
2. **支付后是否停在 paid？** 路线解锁属数字交付，支付成功即可完结；`shouldAutoCompleteAfterPay` 对 `route` 类型在解锁后 `paid→completed`，管理端与用户列表口径一致。  
3. **防重复支付怎么做？** 仅靠读后再写会有竞态；`UPDATE ... WHERE status = expectedFrom` 乐观锁，失败返回「状态已变更」；已 paid/completed 直接拒绝。  
4. **超时用什么基础设施？** 首版无 MQ，进程内 `setInterval` 扫描 + `.env` 可配分钟数，与短信验证码 Map 策略一致，G 阶段再迁队列。

### 实现原理

`applyOrderStatusTransition` 校验合法边后按当前 status 条件更新；`payOrder` 走 `pending→paid`，解锁路线后可选 `paid→completed`；`cancelOrder` 与定时任务均走 `pending→cancelled`；创建订单时复用同用户同路线的待支付单，避免重复下单。

### 最终方案

- 共享：`order-state.ts`（流转表、文案、是否自动完结）。  
- API：`POST /orders/:id/cancel`；支付/取消均经状态机。  
- 后台：`startOrderTimeoutJob` 随服务启动，默认 30 分钟超时、60 秒扫描。  
- 环境变量：`ORDER_PENDING_TIMEOUT_MINUTES`、`ORDER_TIMEOUT_SCAN_INTERVAL_SEC`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **流转规则单源** | shared 定义合法边，服务端与后续支付网关共用，避免魔法数字散落。 |
| 2 | **CAS 防双付** | 条件更新 + 读回校验，并发双击支付仅一笔成功。 |
| 3 | **待支付单复用** | 同路线已有 pending 则返回原单，配合超时取消减少订单膨胀。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **支付与解锁的原子性** | 先 CAS 到 paid 再解锁；解锁失败需人工/补偿（MVP 记录日志，E 阶段接真实支付回调）。 |
| 2 | **多实例定时任务** | 多 Pod 会重复扫描，靠 CAS 取消幂等；生产建议 G 阶段单消费者 或分布式锁。 |
| 3 | **已支付与已完成并存** | 数字商品支付后自动 completed；若仅展示「已支付」会误导，统一用 `getOrderStatusLabel`。 |

---

## B1 打卡字段增强

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-21 |
| **路线图** | B1 |
| **关联代码** | 迁移 `0009_checkin_fields.sql`；`schema/check-ins.ts`、`services/checkin.service.ts`；`routes/checkins.ts`（照片上传）；`@douxing/shared` 积分常量 |

### 背景与目标

MVP 打卡仅有经纬度与 remark，无法支撑游戏化（积分、城市维度、照片激励）与后续排行榜。B1 在不大改流程的前提下扩展字段与计分规则。

### 思考过程

1. **城市从哪来？** 优先 `attractionId` 反查 `city_code`；无景点时用客户端 `cityCode` 或城市名匹配 `attractions` 表；避免用户手填不规范编码。  
2. **积分规则要不要可配置？** 首版写死在 `@douxing/shared`（基础分 + 照片加成 + 首打卡加成），便于前后端一致；配置表留到 B5 成就体系再抽象。  
3. **照片存 URL 还是 BLOB？** 与头像策略一致：multipart 上传至 `uploads/checkins`，库内 JSON 数组存 URL，上限 `CHECKIN_MAX_PHOTOS`。  
4. **打卡后还要触发什么？** 插入成功后同步调用成就/徽章评估（B4/B5），在响应里返回 `newAchievements` / `newBadges` 供移动端 toast。

### 实现原理

`createCheckIn`：校验 GPS → 围栏（B2）→ 解析 `cityCode` → 算分 → 写入 `check_ins`（含 `photos`、`points_earned`、`gps_accuracy`）→ `evaluateAchievements` + `evaluateBadges`。列表接口批量 `cityCode → cityName` 映射提升展示可读性。

### 最终方案

- 新字段：`city_code`、`photos`、`points_earned`、`gps_accuracy`。  
- API：`POST /checkins` 支持 `photos` URL 数组；`POST /checkins/upload-photo` 上传单张。  
- 积分：基础 10 + 有图 +5 + 景点首打 +10（常量见 shared）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **城市解析多级回退** | 景点 ID → cityCode → 城市名查库，降低客户端负担。 |
| 2 | **一次打卡联动游戏化** | 响应携带新解锁成就/徽章，移动端可即时反馈。 |
| 3 | **积分规则 shared 单源** | 前后端与排行榜聚合共用同一套常量。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **无 attractionId 的打卡** | 必须提供 cityCode 或可解析 cityName；否则拒绝并提示，避免脏数据进排行榜。 |
| 2 | **照片 URL 与域名** | 真机访问需 `API_PUBLIC_BASE_URL`；与 A1 头像同一套配置说明。 |
| 3 | **旧打卡无新字段** | 迁移默认值 + 列表接口对 null 友好处理（photos 空数组、积分为 0）。 |

---

## B2 地理围栏打卡

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-21 |
| **路线图** | B2 |
| **关联代码** | `services/checkin-geofence.service.ts`、`config/checkin.ts`；迁移 `0010_checkin_geofence.sql`；`utils/checkin-errors.ts`；`packages/shared` 地理常量 |

### 背景与目标

设计文档 §4.3.3 要求 GPS 围栏与简单防作弊。需在「可演示」与「可调试」之间平衡：生产可严格校验，开发可一键关闭。

### 思考过程

1. **围栏半径取多少？** 500m 为 shared 常量 `CHECKIN_GEOFENCE_RADIUS_M`，兼顾 GPS 误差与防远程刷打卡。  
2. **无坐标的 pending 景点怎么办？** 围栏依赖目标点；景点无 lat/lng 时直接拒绝并提示补坐标（依赖 A2+ 高德链路）。  
3. **如何防刷？** 三层：GPS 精度上限、Haversine 距离、相邻两次打卡隐含速度上限（`CHECKIN_MAX_SPEED_KMH`）。  
4. **本地调试总失败？** `CHECKIN_GEOFENCE_ENABLED=false` 跳过全部围栏逻辑，启动日志与 `/api/health` 可观测当前模式。

### 实现原理

`validateCheckInGeofence`：开关关闭则返回 null；否则校验精度 → 解析目标点（景点或 POI 坐标）→ Haversine 距离 ≤500m → 与上次打卡比速度。`assertNotCheckedInToday` 限制同一景点每日一次。错误统一抛 `CheckinValidationError`，路由层映射为 400 + 中文文案。

### 最终方案

- 环境变量：`CHECKIN_GEOFENCE_ENABLED`（默认 true）。  
- 打卡响应可选带回 `distanceMeters` 便于 UI 展示「距景点 xx 米」。  
- 目标点：优先 `attractionId` 关联景点坐标，其次客户端传 `targetLatitude/Longitude`（路线节点 POI）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **可关闭的严格模式** | 一条 env 切换演示/生产，无需改代码分支。 |
| 2 | **多层防作弊 MVP** | 精度 + 距离 + 速度，不依赖第三方风控 SDK。 |
| 3 | **错误语义化** | 距离、精度、重复打卡等分文案，方便用户自助修正。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **坐标系** | 国内地图与高德为 GCJ-02；客户端 `uni.getLocation` 与库存坐标需同一参考系。 |
| 2 | **室内 GPS 漂移** | 精度差时拒绝并提示到开阔处；开发环境可关围栏联调业务逻辑。 |
| 3 | **速度误判** | 短间隔内位移不校验速度；间隔与阈值放 shared 便于调参。 |

---

## B3 打卡地图 UI

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-21 |
| **路线图** | B3 |
| **关联代码** | `packages/mobile/src/pages/checkins/map.vue`、`utils/checkin-map.ts`；`pages/checkins/list.vue` 入口 |

### 背景与目标

打卡列表无法直观呈现「足迹」。需在 UniApp 地图组件上展示历史打卡点、时间筛选与轨迹连线，作为阶段 B 的用户可见成果。

### 思考过程

1. **用原生 map 还是第三方 SDK？** 微信小程序/H5/App 均内置 `<map>`，MVP 不引入额外地图 SDK 降低包体与密钥管理成本。  
2. **无坐标记录怎么处理？** 早期打卡或非 attraction 节点可能缺坐标；地图层过滤并在工具栏提示「N 个无坐标未显示」。  
3. **时间筛选粒度？** 全部 / 近 7 天 / 近 30 天 / 本年，客户端 filter 后重算 markers 与 polyline。  
4. **照片怎么在地图上露出来？** 微信小程序用 `cover-view` + `callout` 展示 marker 缩略图；其它端降级为点击 marker 看详情卡片。

### 实现原理

`checkin-map.ts` 将 `CheckInInfo[]` 转为 markers、includePoints、polyline；map 中心与 scale 随筛选结果自适应。选中 marker 展示底部详情卡（城市、积分、照片预览）。

### 最终方案

- 页面：`pages/checkins/map`（从打卡列表进入）。  
- 能力：时间筛选、足迹连线、当前位置、marker 点击详情、照片 preview。  
- 无数据时空状态引导回列表打卡。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **跨端一套 Vue 页面** | H5/小程序/App 共用逻辑，仅 callout 用条件编译。 |
| 2 | **筛选与地图联动** | 改时间 chip 即重绘 markers + 折线 + 统计积分。 |
| 3 | **诚实展示缺口** | 明示隐藏的无坐标点数，推动 A2+ 补全坐标。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **marker id 唯一** | 使用 checkIn.id 作为 markerId，避免重复渲染异常。 |
| 2 | **小程序 cover-view 限制** | 照片 callout 仅 MP-WEIXIN 编译块；H5 用详情卡看图。 |
| 3 | **大量点性能** | MVP 全量渲染；点数上百时再考虑聚合或分页（文档留扩展点）。 |

---

## B4 徽章系统

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | B4 |
| **关联代码** | 迁移 `0011_badges.sql`；`data/badge-seeds.ts`、`services/badge.service.ts`、`services/checkin-stats.service.ts`；`routes/badges.ts`；`pages/badges/index.vue` |

### 背景与目标

设计文档 §4.3.4 徽章激励。需要可扩展的定义表 + 用户解锁关系，并在打卡后自动评估，移动端图鉴展示进度。

### 思考过程

1. **徽章与成就是否合并？** 徽章偏展示与收集（带 rarity/category），成就偏里程碑叙事；拆表 `badges` / `user_badges` 与 `achievement_definitions` 分开，避免一条记录承担两种 UI。  
2. **条件怎么表达？** `condition_type` + JSON `condition_value`（如 `{ cityCode, minCount }`），与成就共用模式，便于 seed 配置。  
3. **何时计算？** 打卡成功后 `gatherUserCheckinStats` 一次聚合，再遍历 seed 评估，避免每种条件单独扫表。  
4. **已解锁如何幂等？** `unlockIfMissing` 查重后再 insert，重复打卡不会 duplicate key。

### 实现原理

`evaluateBadges(userId)`：stats → 对每个 badge 算 `computeProgress` → 达标则写 `user_badges` 并返回新解锁列表。图鉴 API 合并定义 + 用户解锁态 + 进度条数据。

### 最终方案

- 11 种徽章 seed（城市、累计打卡、多城、照片、单路线等）。  
- API：`GET /badges`（图鉴）、`GET /badges/mine`（已解锁）。  
- 移动端「我的」入口进 badges 页，支持分类/解锁状态筛选。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **stats 聚合复用** | `checkin-stats.service` 供 B4/B5/B6 共用，一次查询多模块使用。 |
| 2 | **进度可展示** | 未解锁也返回 current/target，图鉴可显示「3/10」式进度。 |
| 3 | **seed 驱动扩展** | 新增徽章主要改 seed + 枚举，不必改评估主流程。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **条件类型扩展** | `BadgeConditionType` 枚举 + switch；新类型需同时改 compute 与 seed。 |
| 2 | **城市徽章与 cityCode** | 依赖 B1 正确写入 city_code；否则 CITY_CHECKIN 类永不触发。 |
| 3 | **评估性能** | 用户打卡量大时 stats 多表聚合；MVP 可接受，后续可 Redis 缓存 stats 快照。 |

---

## B5 成就体系完善

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | B5 |
| **关联代码** | 迁移 `0012_achievement_definitions.sql`；`data/achievement-seeds.ts`、`services/achievement.service.ts`；`routes/achievements.ts`；`pages/achievements/index.vue` |

### 背景与目标

MVP 仅 3 条硬编码成就。B5 引入 `achievement_definitions` 配置表、8 种条件类型，与徽章并行但侧重「里程碑」叙事与图鉴 completion 率。

### 思考过程

1. **旧 `achievements` 表怎么办？** 保留用户解锁记录表；定义迁到 `achievement_definitions`，`achievement_type` 与 `achievement_code` 对齐 seed。  
2. **比徽章多哪些条件？** 增加 `CONSECUTIVE_DAYS`（连续打卡天）、`DAILY_CHECKINS`（单日多次），stats 服务里算 streak 与 max daily。  
3. **兼容旧 API？** `GET /achievements` 仍返回已解锁列表；新增 `/catalog`、`/mine` 支撑图鉴页。  
4. **解锁描述从哪来？** 展示以 definition 为准，unlock 行只存 code 与时间，避免文案双份不一致。

### 实现原理

与 B4 对称：`gatherUserCheckinStats` → `computeAchievementProgress` → `unlockIfMissing` → 写 `achievements` 表。catalog 接口 left join 用户解锁态，输出 `unlocked` + `progress`。

### 最终方案

- 8 条 achievement seed，分类 explore/social/streak 等。  
- 打卡响应与徽章一并返回 `newAchievements`。  
- 移动端 achievements 页：总进度、筛选、进度条。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **定义与解锁分离** | 改文案/阈值只动 definitions + seed，不动用户历史。 |
| 2 | **连续天算法** | 对 DATE 集合排序算最长 streak，支持「坚持打卡」类成就。 |
| 3 | **与徽章互补** | 同一次 stats 评估双轨输出，用户一次打卡可能同时解锁徽章+成就。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **时区与 DATE()** | MySQL `DATE(checked_at)` 依赖 DB 时区；与 Docker MySQL `+08:00` 配置一致。 |
| 2 | **迁移 0012 与旧数据** | 旧解锁行保留；新 definition 通过 code 关联，需 seed 与旧 type 字符串一致。 |
| 3 | **catalog 数据量** | 成就数少可全量；上百条时需分页（当前未做，文档标注 MVP 范围）。 |

---

## B6 排行榜

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | B6 |
| **关联代码** | `services/leaderboard.service.ts`、`routes/leaderboard.ts`；`pages/leaderboard/index.vue`；`@douxing/shared` LeaderboardPeriod/Metric |

### 背景与目标

设计文档 §4.2 社交激励中的排行榜。提供周榜/月榜，按打卡次数或积分排序，并展示当前用户名次。

### 思考过程

1. **实时榜还是周期榜？** 首版做自然周/自然月聚合，SQL `GROUP BY user_id`，不做 Redis ZSET，与当前仅 MySQL 的基础设施一致。  
2. **排序指标？** `checkins` 次数与 `points` 求和二选一，接口参数 `metric`；同分再比打卡数。  
3. **是否只统计已通过打卡？** 是，过滤 `CheckInStatus.APPROVED` 与活跃用户，避免测试数据污染。  
4. **当前用户不在 Top N？** 全量聚合后在内存算 rank，返回 `myRank` / `myValue`；未上榜则 rank null、value 0。

### 实现原理

`resolvePeriodRange` 算本周一 0 点或本月 1 号；SQL aggregate → 排序 → slice limit → 补用户 nickname/avatar。周期与 metric 枚举放 shared，移动端 chip 切换触发重新请求。

### 最终方案

- API：`GET /leaderboard?period=week|month&metric=checkins|points&limit=`  
- 移动端独立排行榜页（profile 入口）。  
- 默认 limit 20，上限 shared 常量控制。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **周期边界清晰** | 周一为周起点，与中文产品习惯一致。 |
| 2 | **我的排名始终可见** | 不在 Top N 也返回 myRank，减少挫败感。 |
| 3 | **双指标一套聚合** | 同一次 SQL 结果按 metric 选排序字段，避免 duplicate 查询。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **全量聚合再 slice** | 用户量大时需 SQL LIMIT 或物化视图；MVP 用户少可接受。 |
| 2 | **服务器时区** | period 起止用 JS Date 构造，需与 MySQL 存储时区对齐。 |
| 3 | **积分与次数口径** | 积分来自 B1 计分规则变更会影响历史榜；文档说明「规则变更不追溯重算」。 |

---

## C1 多轮对话规划

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | C1 |
| **关联代码** | 迁移 `0013_plan_sessions.sql`；`schema/plan-sessions.ts`、`services/plan-session.service.ts`、`routes/plan-sessions.ts`；`llm-client.service.ts`（history）；`pages/plan/plan.vue`（对话 UI） |

### 背景与目标

设计文档 §3.3 规划多 Agent 协作；MVP 阶段先落地**可追问、可改方案**的多轮会话，不引入 LangChain 与独立 Python 服务。

### 思考过程

1. **扩展现有 `/generate` 还是新会话 API？** 单轮接口保留兼容；新增 `plan-sessions` 专门承载会话与消息，避免每次 generate 都猜是否在续聊。  
2. **history 怎么传给 LLM？** 用户消息原样保留；助手侧存 `route_snapshot` JSON，续聊时序列化为 assistant 内容，并在 system prompt 增加「结合历史输出完整 JSON」提示。  
3. **追问是新建路线还是改草稿？** 同一会话绑定一条 `route_id`，追问走 `regenerateRouteFromPrompt` + history，避免列表里一次对话产生多条草稿。  
4. **规划页形态？** 由单次 textarea 改为聊天气泡；首条创建会话，后续 `POST .../messages`；保留模型选择与 `requestAiPlan` 遮罩。

### 实现原理

`createPlanSession`：generate → 写 `travel_routes` → 插入 session + 首对 user/assistant 消息（assistant 带 snapshot）。`appendPlanSessionMessage`：加载历史 → `buildHistoryForLlm` → regenerate 同一路线 → 追加消息。LLM 侧 `chatCompletionWithProvider` 在 system 后插入 history，再发当前 user 内容。

### 最终方案

- 表：`plan_sessions`、`plan_session_messages`（`route_snapshot` JSON）。  
- API：`POST/GET /api/routes/plan-sessions`、`POST .../:id/messages`。  
- 移动端规划 Tab 对话式 UI；`POST /routes/generate` 仍可用于单轮。  
- 迁移文件：`0013_plan_sessions.sql`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **会话与路线一对一** | 追问更新同一条草稿，列表不膨胀，和「改 prompt 覆盖」心智一致。 |
| 2 | **snapshot 可续聊** | 助手消息存完整路线 JSON，LLM 能在上下文里做「第三天改西湖」类修改。 |
| 3 | **单轮/多轮并存** | 老接口与模板降级逻辑不变，逐步迁移到规划页对话流。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **迁移记录与表不一致** | `__drizzle_migrations` 有 tag 但 SQL 未执行会导致「表不存在」；删除错误 hash 后重跑 `db:migrate`，并强调 generate≠migrate。 |
| 2 | **history token 膨胀** | 每轮 assistant 带整段 JSON，轮次多时会超长；C1 先可用，后续 C2/C4 可摘要或只传 diff 字段。 |
| 3 | **模板降级与多轮** | 无 LLM 时把历史 user 句拼接进 prompt 做模板匹配，保证离线演示仍可追问（精度有限）。 |

---

## C1+ 路线解锁支付开关

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | 阶段 C 体验增强 / §5.4 前置 |
| **关联代码** | `config/route-unlock.ts`；`route.service.ts`、`route-info.util.ts`、`order.service.ts`；`routes/orders.ts`（payment-config）；`.env.example` |

### 背景与目标

开发演示希望「生成即可看完整行程」；商业化阶段再通过环境变量打开解锁付费，且须服务端强制生效，防止仅隐藏前端按钮。

### 思考过程

1. **开发期要不要每次模拟支付？** 会打断「生成 → 看行程」演示链路；产品希望默认**直接可见**完整 `route_detail`。  
2. **开关放哪？** 环境变量 `ROUTE_UNLOCK_PAYMENT_REQUIRED`，默认 `false`；上线商业化再设 `true`，无需改代码。  
3. **只改前端行不行？** 不行；须服务端 `toRouteInfo` 对创建者强制 `isUnlocked`，并拒绝创建解锁订单，防绕过。

### 实现原理

`isRouteUnlockPaymentRequired()` 为 false 时：新建路线 `isUnlocked: true`；详情接口对 owner 覆盖 unlocked；`createRouteUnlockOrder` 直接返回「无需支付」。`GET /orders/payment-config` 下发 `routeUnlockPaymentRequired` 供移动端隐藏解锁卡片。

### 最终方案

- `.env`：`ROUTE_UNLOCK_PAYMENT_REQUIRED=false`（默认）。  
- `true` 时恢复原有解锁卡片 + 订单 + 模拟/微信支付流程。  
- 服务启动日志打印 `[route-unlock]` 当前模式。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **一套代码两种商业模式** | 演示/内测免支付，正式环境一行配置打开变现。 |
| 2 | **服务端权威** | 前端隐藏按钮不够，详情 API 与订单创建双侧校验。 |
| 3 | **与 payment-config 统一** | 移动端一次拉配置即可同步解锁 UI 与支付能力。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **库内旧数据 isUnlocked=false** | 靠 `toRouteInfo` 运行时覆盖，无需批量迁移历史路线。 |
| 2 | **重新生成后解锁态** | 免支付模式下 `buildRouteDetailFromDraft` 直接 `isUnlocked: true`；付费模式下仍重置为 false。 |
| 3 | **与 E2 真支付衔接** | 开关仅控制「要不要解锁」；`PAYMENT_MODE` 仍管 mock/wechat，两维配置正交。 |

---

## C2 意图结构化解析

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | C2 |
| **关联代码** | `services/travel-intent.service.ts`；`route-generator.service.ts`、`llm-client.service.ts`；`plan-session.service.ts`；迁移 `0014_plan_session_intent.sql`；`pages/plan/plan.vue`（约束条） |

### 思考过程

1. **要不要上独立 NLP 模型？** 设计文档提 BERT/BiLSTM，但当前阶段用规则 + 关键词即可覆盖 MVP 验收（城市/天数/预算/主题），且 LLM/模板降级路径都能复用。
2. **意图存哪？** 会话级 `plan_sessions.intent_snapshot` 持久化；生成链路通过 `GenerateRouteInput.intent` 传递，避免每轮从 history 重算不一致。
3. **只解析不约束不够** | 验收要求是「生成结果符合约束」：LLM prompt 注入约束块 + `enforceRouteConstraints` 对天数/城市/预算/标签做后处理校正。
4. **追问怎么合并？** 首条走全量 `parseTravelIntent`；后续识别「改成 5 天」「预算降到 3000」等修改句，在已有 intent 上增量合并。

### 实现原理

用户消息 → `buildIntentFromHistory` 抽取/合并 → 写入 session → `generateRoute` 带 intent 调 LLM 或模板 → `formatIntentConstraintsForLlm` 写入 user 内容 → 返回 JSON → `enforceRouteConstraints` 校正 days/city/budget/tags → 助手回复附带「已理解需求：…」摘要。

### 最终方案

- 新增 `TravelIntentSnapshot` 类型与 `travel-intent.service.ts`（解析、合并、LLM 约束文案、后处理）。
- `plan_sessions.intent_snapshot` JSON 字段；API 响应与详情带 `intentSnapshot`。
- 规划页展示「已理解需求」约束条；助手消息含解析摘要。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **双路径一致** | LLM 与模板降级共用同一套 intent 解析与 enforce，离线演示也遵守天数/城市。 |
| 2 | **多轮增量合并** | 支持「改成 5 天」「换成上海」类修改句，不必每轮重复完整需求。 |
| 3 | **约束可视化** | 移动端约束条 + 助手摘要，用户可确认系统理解是否正确。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **中文天数表达多样** | 同时支持「3天」「三日游」「周末=2天」及阿拉伯数字修改句。 |
| 2 | **LLM 仍可能越界** | prompt 强约束 + 生成后截断/补全天数、校正 matchedCity 与 budgetRange。 |
| 3 | **追问与首条解析歧义** | 用修改关键词触发 patch 模式，否则按新句全量解析并覆盖同字段。 |

---

## C3 RAG 景点检索

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | C3 |
| **关联代码** | `services/attraction-rag.service.ts`；`route-generator.service.ts`、`llm-client.service.ts`；`plan.vue`（库内景点计数） |

### 思考过程

1. **要不要上向量库？** C3 验收是「景点来自内容库」；当前 `attractions` 仅数十条，MySQL 按 city + 标签/关键词打分足够，后续数据量大再换 Milvus。
2. **RAG 放哪一环？** 检索 → 注入 LLM prompt → 生成后 `applyRagToRouteDraft` 对齐 name/坐标/票价/`attractionId`；模板降级可走 `buildRouteFromRagCatalog` 纯库组装。
3. **LLM 仍乱编怎么办？** system 规则 + 候选 JSON 白名单 + 后处理模糊匹配；`syncAttractionsFromRouteDetail` 仍作兜底。

### 实现原理

`retrieveAttractionsForPlanning` 按 `cityCode` 拉库 → 主题/关键词打分排序 → `formatRagContextForLlm` 写入 user 消息 → LLM/模板生成 → `applyRagToRouteDraft` 绑定库内 POI → 写入 `routeDetail.ragMatchedCount`。

### 最终方案

- 新增 `attraction-rag.service.ts`（检索、格式化、绑定、模板 RAG 组装）。
- `GenerateRouteInput.ragCandidates` 贯穿 LLM 与模板路径。
- 规划页约束条展示「库内景点 N 处」；助手回复同步提示。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **轻量可演进** | 规则打分 + MySQL，无额外基础设施；接口可平滑换向量检索。 |
| 2 | **双路径覆盖** | LLM 注入候选；离线模板可直接从库组装整日行程。 |
| 3 | **可观测** | `ragCandidateCount` / `ragMatchedCount` 写入 routeDetail，便于验收与调试。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **库小城市少** | 仅杭/沪/京/成等有种子；无 city 时跳过 RAG，行为与 C2 一致。 |
| 2 | **LLM 不完全遵命** | 后处理按 name/alias 模糊对齐，尽量补上 `attractionId` 与坐标。 |
| 3 | **餐食节点干扰** | `applyRag` 仅处理 attraction/restaurant/hotel，meal/transport 保持原样。 |

---

## C4 多方案生成

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | C4 |
| **关联代码** | 迁移 `0015_plan_session_candidates.sql`；`config/plan-route-variants.ts`；`plan-session.service.ts`；`POST .../select-candidate`；`pages/plan/plan.vue` 候选卡片 |

### 思考过程

1. **一次 LLM 返回数组还是多次调用？** 多次调用复用现有 `createRouteFromPrompt`，每套带 `variantHint`（经典/文化/休闲等），与 C3 RAG 轮换起点组合，差异更稳定。
2. **未选中的草稿怎么办？** 保留为用户草稿路线，会话 `route_id` 指向当前选中；切换只改关联，不删其他路线。
3. **追问还要多方案吗？** 否；`appendPlanSessionMessage` 只改当前选中路线并 `clearSessionCandidates`，避免组合爆炸。

### 实现原理

首条 `createPlanSession` → 按意图生成 3 个 variant → 各调一次 `createRouteFromPrompt` → 写入 `plan_session_candidates` → 默认选中方案 A → 移动端横向卡片 → `select-candidate` 更新 `is_selected` 与 `session.route_id`。

### 最终方案

- 表 `plan_session_candidates`（sessionId、routeId、label、variantKey、isSelected）。
- API：`POST /plan-sessions/:id/select-candidate`。
- 类型 `PlanRouteCandidate`；规划页候选卡片 UI。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **主题自适应变体** | 亲子/美食/浪漫等意图会替换 B/C 方案风格提示。 |
| 2 | **与 C3 协同** | `ragVariantIndex` 轮换 RAG 候选，模板降级也能产出差异方案。 |
| 3 | **追问单线收敛** | 选定后多轮只维护一条 active 路线，逻辑清晰。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **生成耗时 ×3** | Loading 文案改为「生成多套方案」；变体串行生成，失败则跳过并保证至少 1 套。 |
| 2 | **草稿列表膨胀** | 3 条均入库为草稿；后续可加「仅保留选中」清理策略。 |
| 3 | **LLM 方案趋同** | variantHint + RAG 轮换双保险；仍趋同时可加强 prompt 或提高 temperature（未改）。 |

---

## C5 Python AI 微服务

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | C5 |
| **关联代码** | `packages/ai-service/`（FastAPI + LangChain）；`server/config/ai-service.ts`；`server/services/ai-service-client.service.ts`；`route-generator.service.ts` 三级降级 |

### 思考过程

1. **是否必须独立 Python 服务？** 设计文档 §1.4 规划 Node + Python 双栈；当前 Node 已能直连 DeepSeek/LM Studio。C5 定位为「可选增强」：对齐架构、便于后续 Agent/RAG 扩展，默认关闭不影响现有部署。
2. **Python 与 Node 谁负责 RAG/意图？** Node 仍做 MySQL 检索与意图解析（C2/C3 已稳定）；Python 只接收结构化 payload 调 LangChain，避免双份 DB 依赖。
3. **失败如何降级？** 严格三级：`AI_SERVICE` → Node `chatCompletionForRoute` → 模板/RAG 组装；任一级失败只 warn 不中断用户流程。

### 实现原理

Node `generateRoute` 在 `AI_SERVICE_ENABLED=true` 时 POST `packages/ai-service` 的 `/v1/route/generate`；Python 用 LangChain `ChatOpenAI`（OpenAI 兼容）按与 Node 相同的 system prompt 与约束块生成 JSON；Node 用现有 `llmRouteSchema` 二次校验后走 RAG 对齐与意图校正。

### 最终方案

- 新包 `packages/ai-service`：`GET /health`、`GET /v1/status`、`POST /v1/route/generate`。
- 环境变量：`AI_SERVICE_ENABLED`、`AI_SERVICE_URL`（默认 `http://127.0.0.1:8100`）。
- 启动：`pnpm dev:ai-service`；与 Node 共用根目录 `.env` 中的 DeepSeek/LM Studio 配置。
- `llmProvider=ai-service` 标记来源；`/llm-status` 增加 `aiService` 探测字段。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **可选开关** | 默认 `AI_SERVICE_ENABLED` 未开启，零侵入现有 MVP 部署。 |
| 2 | **Prompt 对齐** | Python 与 Node 共用同一 JSON 结构与约束文案，输出可互换校验。 |
| 3 | **三级降级链** | Python 宕机 / 超时 / 502 自动回退 Node LLM，再回退模板。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **双栈 env 同步** | Python 读取 monorepo 根 `.env`，与 Node 共用 `DEEPSEEK_*` / `LLM_*`，避免两套密钥。 |
| 2 | **LangChain JSON 模式** | DeepSeek 用 `response_format=json_object`，失败时去掉该参数重试（与 Node 一致）。 |
| 3 | **类型双端校验** | Python Pydantic + Node Zod 各验一遍，防止微服务返回脏数据入库。 |

---

## A3++ 草稿编辑入口分离

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22（2026-05-26 由 [A3+++](#a3-草稿编辑弹窗与发布状态) 演进替代） |
| **路线图** | A3 体验优化 |
| **关联代码** | 初版：`pages/routes/detail.vue`（`showDraftEdit`）；`pages/routes/list.vue`（`edit=1` query） |

### 背景与目标

A3 已在详情页提供草稿名称/简介编辑。C1 生成流落地后，用户首屏更需要看行程而非编辑表单；需把「改标题简介」从 AI 生成后的默认详情页中移出。

### 思考过程

1. **生成后详情页信息过载** 用户刚生成完更关心行程节点，「编辑草稿」名称/简介块抢注意力。  
2. **编辑能力要不要删？** 不删；改名称/简介仍是有效需求，应放在 intentional 入口。  
3. **如何区分场景？** 初版用列表打开草稿时带 `?edit=1`；规划页跳转不带参，详情页内嵌编辑卡片默认隐藏。

### 最终方案（初版，已被 A3+++ 替代）

- 详情页：`showDraftEdit = isDraft && isOwner && allowDraftEdit`。  
- `我的` scope 下点击草稿：`/pages/routes/detail?id=&edit=1` 才显示内嵌编辑卡片。  
- 规划页「查看路线详情」等路径不带 `edit=1`，首屏无编辑表单。

> **后续演进**：内嵌卡片改为 hero 编辑按钮 + 弹窗；移除 `edit=1` 与自动展开；见 [A3+++](#a3-草稿编辑弹窗与发布状态)。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **零后端改动** | 纯前端路由参数区分场景，风险小。 |
| 2 | **生成流更干净** | 首屏聚焦行程与发布/追问，符合 C1 对话规划主路径。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **内嵌表单仍占版面** | 带 `edit=1` 时编辑区挤在详情中部；后续改为弹窗 + 手动点击打开（A3+++）。 |

---

## A3+++ 草稿编辑弹窗与发布状态

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-26 |
| **路线图** | A3 体验优化（承接 A3++） |
| **关联代码** | `pages/routes/detail.vue`；`static/iconfont/svg/edit.svg`；`i18n/locales` 中 `routes.edit*`、`routeMap.*` |

### 背景与目标

在 A3++「生成后默认不编辑」基础上，草稿仍须支持改名称/简介；同时要与**发布状态**一致（仅草稿可改，已发布/已归档不可编辑），且查看详情时不应自动弹出编辑层。

### 思考过程

1. **内嵌卡片还是弹窗？** 弹窗不占详情页纵向空间，与地图、行程列表并列更清晰；遮罩 + 取消/保存符合移动端习惯。  
2. **编辑入口放哪？** hero 卡片右上角铅笔按钮，与标题/统计同屏，草稿所有者一眼可见。  
3. **发布状态如何表达？** hero 标题旁展示状态徽章（草稿/已发布/已归档）；编辑按钮仅 `isOwner && status=DRAFT`，与后端 `updateDraftRoute` 仅草稿可 PUT 一致。  
4. **还要不要 `edit=1` 自动打开？** 否；进入详情只浏览，**必须点击编辑按钮**才开弹窗，避免误触与信息过载。  
5. **名称/简介怎么展示全？** 微信小程序单行 `input` 易截断长标题；名称与简介均用 `textarea` + `auto-height` + 按字数估算 `minHeight`。

### 实现原理

`canEditRouteInfo = isOwner && DRAFT` 控制 hero 编辑按钮；`editModalVisible` 由 `openEditModal` / `closeEditModal` 管理。弹窗内带 label（路线名称/路线简介）、滚动区 `scroll-view`、保存走既有 `PUT /routes/:id`。保存成功关闭弹窗并刷新 hero 标题与描述。

### 最终方案

- hero：标题 + 状态徽章 + 条件显示编辑按钮（SVG 图标）。  
- 弹窗：标题、说明文案（仅草稿可改）、带 label 的表单、取消/保存草稿。  
- 移除：`edit=1` query、`autoOpenEditModal`、页面内嵌 `edit-card`。  
- 列表 `goDetail` 不再拼接 `edit=1`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **发布状态与后端一致** | 仅草稿显示编辑；已发布路线无编辑入口，避免调用 `仅草稿状态的路线可编辑` 错误。 |
| 2 | **查看与编辑分离** | 进详情默认只看内容；编辑为显式操作，符合 AI 生成后主路径。 |
| 3 | **长文案可编辑** | 名称/简介 textarea 自适应高度，弹窗 body 可滚动。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **单行 input 截断长路线名** | 改为 textarea + `auto-height` 与动态 `minHeight`。 |
| 2 | **编辑图标识别度** | CSS 伪元素铅笔在小程序上易变形；改用 `/static/iconfont/svg/edit.svg` 白色描边图标。 |
| 3 | **与 A3++ 行为迁移** | 删除 `edit=1` 后需同步更新列表跳转与文档，避免仍期望自动展开编辑。 |

---

## A3-3 路线详情地图预览动画

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-26 |
| **路线图** | A3 增强 / B3 地图能力延伸 |
| **关联代码** | `packages/shared/src/route-path.ts`；`packages/mobile/src/components/route-map/RouteMapPlayer.vue`；`utils/route-path-animation.ts`、`utils/route-map.ts`；`pages/routes/detail.vue` |

### 背景与目标

AI 生成路线后，用户需要在详情页直观看到 POI 空间关系与「走线」感；同时为后期「多条路线/打卡合并为一条足迹动画」预留统一路径模型。

### 思考过程

1. **动画能力从哪来？** uni-app `<map>` 的 `polyline` 无原生 draw 动画，需自研：逐帧增加折线点数 +  traveler marker 沿路径插值移动。  
2. **路径点从哪来？** `routeDetail.days` 中带坐标的 POI 按日程顺序连线；MVP 用直线插值 densify，不依赖新 API。  
3. **真实路网何时做？** 阶段 2 服务端接高德 direction + Redis 缓存；当前 shared 已留 `RoutePathSegment.mode` 与 `mergeRoutePaths`。  
4. **何时展示地图？** 与行程可见性一致：`isUnlocked` 且至少 2 个 POI 有坐标；无坐标站点计数提示 i18n。  
5. **跨端动画定时器？** H5 可用 `requestAnimationFrame`；**微信小程序无全局 rAF**，须降级 `setTimeout(16ms)`。

### 实现原理

```text
routeDetail → buildRoutePathFromDetail（shared）
    → pois / segments / fullPoints
    → RouteMapPlayer
        → createRoutePathAnimation（polyline slice + marker 插值）
        → buildRoutePoiMarkers / buildRoutePolyline
```

播放器支持播放/暂停/重播，默认自动播放约 8s。

### 最终方案

- **shared**：`extractRouteDetailDays`、`interpolateSegment`、`buildRoutePathFromDetail`、`mergeRoutePaths`（后期多路线合并）。  
- **mobile**：`RouteMapPlayer` 组件挂于路线详情解锁区；`buildRoutePoiMarkers` 为每个打卡点绘制 **常驻气泡**（`序号. 景点名`，`callout.display: 'ALWAYS'`），图钉序号圆标保留；文案键 `routeMap.*`。  
- **待办（A3-3+）**：足迹页 `mergeRoutePaths` 多路线合并动画（路网 polyline 已由 [H9-2](#h9-2-路网-polyline-与酒店-ragphase-2) 经 `GET /routes/:id/map-path` 提供）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **路径模型在 shared** | 详情预览与后期「人生足迹」合并共用 `RoutePath`，避免重复解析 `routeDetail`。 |
| 2 | **可复用播放器** | `RouteMapPlayer` 可接入规划候选预览、足迹页，仅换数据源。 |
| 3 | **跨端动画调度** | `scheduleFrame` 自动 rAF / setTimeout 降级，H5 与微信小程序行为一致。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **微信小程序无 `requestAnimationFrame`** | 捕获 `TypeError: requestAnimationFrame is not a function`；统一用 `scheduleFrame` + `setTimeout(16)` 降级。 |
| 2 | **部分 POI 无坐标** | RAG/高德补全前过滤无坐标节点；UI 提示 `hiddenSpots` 数量，避免用户以为路线不完整是 bug。 |
| 3 | **MVP 直线穿楼** | Phase 1 接受演示级效果；[H9-2](#h9-2-路网-polyline-与酒店-ragphase-2) 已改走服务端 direction + Redis 缓存，客户端 API 失败时仍回退本地直线。 |

---

## G8 生产部署与 API 公有化

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-23（2026-05-24 补充 MySQL/PM2 踩坑） |
| **路线图** | G8 |
| **关联代码** | `scripts/deploy-server.mjs`、`scripts/fix-mysql-password.mjs`、`scripts/setup-debian12.sh`、`scripts/install-nginx-auto.sh`、`scripts/install-nginx-certbot.sh`、`scripts/install-nginx-ssl.sh`；`deploy/docker-compose.prod.yml`、`deploy/ecosystem.config.cjs`、`deploy/nginx/*.template`；`packages/server/src/config/env.ts`、`packages/server/src/routes/health.ts`；`docs/deploy-production.md`、`docs/启动与部署流程.md`、`docs/env-environments.md`；`packages/server/src/utils/public-asset-url.util.ts` |

### 背景与目标

微信小程序上线要求 API **HTTPS + 合法域名**。本地 `127.0.0.1:3000` 无法真机 uploadFile；需将 Node API 部署到腾讯云轻量服务器（`api.yxtao.site`），并沉淀**可重复的一键发版流程**，避免每次部署手工配 Nginx/Certbot。

### 思考过程

1. **系统选 Debian 还是 OpenCloudOS？** 轻量机常见 OpenCloudOS 的 Nginx 目录、dnf、SELinux 与文档差异大；重装 **Debian 12** 后 `apt` + `sites-available` 与社区教程一致，踩坑更少。  
2. **进程怎么守护？** 开发用 `pnpm dev:server`，生产用 **PM2** + `deploy/ecosystem.config.cjs`；MySQL/Redis 用 **Docker** 且端口只绑 `127.0.0.1`，对外仅 Nginx 80/443。  
3. **2G 内存能否跑 `tsc`？** 生产 `pnpm build:server` 在轻量机上常卡在 `@douxing/server` 的 `tsc`；改为 **tsx 直跑源码**（仍 build `@douxing/shared`），PM2 通过 `SERVER_RUNTIME=tsx` 或内存 ≤4G 自动启用。  
4. **Nginx 与 deploy 要不要分两步？** 用户每次 deploy 后再跑 certbot 体验差；从 `.env` 的 `API_PUBLIC_BASE_URL` 解析域名，`deploy:server` 末尾调用 `install-nginx-auto.sh`：**HTTPS 已通则跳过**，已有 Certbot 配置则不覆盖。  
5. **静态资源 URL 怎么给小程序？** DB 存相对路径 `/uploads/...`，接口返回时按 `API_PUBLIC_BASE_URL` 动态拼 HTTPS，避免历史 localhost 写入库。  
6. **三套环境怎么区分？** 根目录 `.env.development` / `.env.staging` / `.env.production` 管前端打包；服务器 `.env` 管后端；文档 [env-environments.md](./env-environments.md) + [启动与部署流程.md](./启动与部署流程.md) 统一说明。  
7. **改 `.env` 数据库密码后 API 仍 Access denied？** 分两类：**MySQL 数据卷**里用户密码仍是旧值（`MYSQL_*` 环境变量只在容器**首次初始化**时生效）；**PM2 进程**可能缓存旧 `DATABASE_URL`，且 `dotenv` 默认不覆盖已有环境变量——部署 shell 里 `wait-only` 能连上，PM2 内查库仍失败。  
8. **`/api/health` 能通是否代表数据库正常？** 否。health 不查库，仅返回静态 JSON；后台任务（如 `[order-timeout]`）或登录接口才会暴露真实连接问题。  
9. **改密后如何可靠发版？** 提供 `pnpm fix:mysql-password`（宿主机 `DATABASE_URL` 验证 + 容器内 `ALTER USER`）；PM2 改为 **delete + start**（不用 reload）；`env.ts` 设 `dotenv override: true`；`ecosystem.config.cjs` 启动时注入完整 `.env`。

### 实现原理

`pnpm deploy:server`：校验生产 `.env` → Docker compose 起 MySQL+Redis → `pnpm install` →（低内存则 shared build + tsx）→ 宿主机 `wait-only` 验连 → `db:migrate` → **PM2 delete + start**（注入 `.env`）→ 健康检查 → **Nginx 自动配置**。Nginx 脚本检测 `curl https://域名/api/health`，失败则写反代配置；Let's Encrypt 证书存在则装 SSL 模板，否则 HTTP + 可选 `NGINX_CERTBOT_EMAIL` 自动 certbot。

**数据库密码与连接链路**：

```text
.env（MYSQL_PASSWORD + DATABASE_URL 三处须一致）
    ↓
PM2 进程环境变量（reload 可能残留旧 DATABASE_URL）
    ↓
dotenv 加载（override: true 后以 .env 为准）
    ↓
Node API → 127.0.0.1:3307 → Docker MySQL（MySQL 侧见来源 IP 172.18.0.1）
```

改 `.env` 只影响**新启动**的 Docker 卷初始化；已有 `mysql_data` 卷须在容器内 `ALTER USER`，或 `pnpm fix:mysql-password --recreate` 清卷重建。

### 最终方案

- **一键部署**：`pnpm deploy:server --skip-docker --use-tsx`（2G 机推荐）。  
- **Nginx 合并进 deploy**：`.env` 设 `NGINX_AUTO_SETUP=true`、`API_PUBLIC_BASE_URL`；跳过：`--skip-nginx`。  
- **HTTPS**：`install-nginx-certbot.sh` 或 deploy 自动 certbot；已有证书用 `install-nginx-ssl.sh`。  
- **发版**：后端 `git pull` + deploy；小程序本地 `pnpm build:mp-weixin` 上传微信；服务器重启**不必**重配 Nginx（PM2/Docker/Nginx 开机自启）。  
- **运维文档**：`deploy-production.md`（Debian 12）、`启动与部署流程.md`（含发版 checklist）。  
- **改数据库密码**：`.env` 中 `MYSQL_ROOT_PASSWORD`、`MYSQL_PASSWORD`、`DATABASE_URL` 三处一致 → `pnpm fix:mysql-password` → `pnpm exec pm2 delete douxing-api` → `pnpm deploy:server --skip-docker --skip-build`；可接受丢库时用 `pnpm fix:mysql-password --recreate`。  
- **发版后验证**：除 `curl .../api/health` 外，应用登录等查库接口或查看 `pm2 logs` 无 `Access denied`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **deploy + Nginx 一步到位** | `API_PUBLIC_BASE_URL` 驱动域名；HTTPS 已通则自动跳过，避免重复配置。 |
| 2 | **tsx 低内存模式** | 内存 ≤4G 自动启用，跳过 server `tsc`，2G 轻量机可稳定发版。 |
| 3 | **文档化发版流程** | 开发/测试/生产启动命令、发版类型决策、重启 vs 发版区分，新人可按文档操作。 |
| 4 | **MySQL 改密工具链** | `fix:mysql-password` 以宿主机 `DATABASE_URL` 验连；`ecosystem` + `dotenv override` 保证 PM2 与 `.env` 一致。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **Nginx 首次 SSL 与 certbot 死锁** | 模板不能预写 `listen 443 ssl` 且无证书；首次仅 HTTP:80，certbot 成功后再追加 HTTPS。Debian 12 用 `listen 443 ssl http2`，勿用 `http2 on;`。 |
| 2 | **根域证书不能给 api 子域用** | `yxtao.site` 证书 SAN 不含 `api.yxtao.site`；须单独申请子域证书或 `*.yxtao.site` 通配符，或 Certbot 签 `api.yxtao.site`。 |
| 3 | **`@douxing/shared` 未 build 导致 PM2 报错** | `package.json` exports 指向 `dist/`；`deploy:server` 必须先 build shared；tsx 模式同样依赖 shared dist。 |
| 4 | **MySQL 改 `.env` 不生效** | `MYSQL_*` 仅容器**首次**建卷时写入；已有 `mysql_data` 须 `ALTER USER` 或 `fix:mysql-password --recreate`。`DATABASE_URL` 密码须与 `MYSQL_PASSWORD` 一致。 |
| 5 | **PM2 reload 残留旧 `DATABASE_URL`** | `dotenv` 默认不覆盖进程已有变量；deploy shell 的 `wait-only` 通过但 PM2 内 `[order-timeout]` 仍 Access denied。**解决**：`pm2 delete` + 重新 start；`env.ts` 加 `override: true`；`ecosystem.config.cjs` 注入 `.env`。 |
| 6 | **health 正常但业务接口失败** | `/api/health` 不查库，不能作为数据库验收依据；应测登录等写库接口或看 `pm2 logs`。 |
| 7 | **小程序真机仍连 127.0.0.1** | 须打生产包 `pnpm build:mp-weixin`（读 `.env.production`），并在微信后台配置 uploadFile/request 合法域名；与 MySQL Access denied **无关**。 |

---

## G9 国际化（i18n）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-27（框架 05-26 + 扫尾 + en-US 主流程手测验收） |
| **路线图** | G9 |
| **关联代码** | `packages/shared/src/i18n/`；`packages/mobile/src/i18n/`（含 `mobileT.ts`）、`packages/web/src/i18n/`；`packages/server/src/middleware/locale.ts`、`utils/response.ts`、`i18n/api-*`；`docs/国际化.md` |

### 背景与目标

小程序提审与潜在出海用户需要 **界面中英文切换**；API 错误与成功提示也应随语言变化，而不是硬编码中文。目标是在不破坏现有接口的前提下，建立 **shared 单源文案 + 各端 vue-i18n + 服务端 `messageKey`** 三层体系，并保留旧中文 message 的兼容映射。

### 思考过程

1. **文案放哪一层？** Tab、导航、会员等级等跨 Web/移动复用，放 `@douxing/shared`；各端页面专属文案放 `mobile` / `web` 的 `locales/*.ts`，在 `i18n/index.ts` 浅合并。  
2. **API 怎么改不炸客户端？** 响应体增加可选 `messageKey`，`message` 仍按 `Accept-Language` 翻译后返回；历史 `fail(res, '未登录')` 经 `API_LEGACY_MESSAGE_MAP` 自动映射到 key，业务可渐进迁移。  
3. **UniApp 与 vue-i18n 插值** | 部分端上模板 `t('key', { name })` 不替换占位符；移动端增加 `useTf()` + shared 的 `formatMessage` 做运行时 `{key}` 替换。  
4. **LLM 与 DB 仍是中文怎么办？** 用户兴趣标签、意图主题在库内仍存中文预设值；展示层用 `formatInterestTagLabel` / slug 映射，服务端助手回复用 `plan-messages.ts` 按 locale 生成，不把翻译塞进 prompt。  
5. **语言如何与 API 对齐？** 移动端 `request.ts` 附带 `Accept-Language`（读 `douxing_locale` 存储）；服务端 `localeMiddleware` 写入 `res.locals.locale`，`success` / `fail` 统一走 `resolveApiMessageInput`。  
6. **非 Vue 模块怎么翻译？** 定位、语音、上传 API 等纯 TS 无法 `useI18n()`；增加 `mobileT(key, params)`，内部读 `i18n.global` + `formatMessage`，与 `useTf` 插值规则一致。  
7. **状态标签怎么跨端？** 订单/路线状态不再硬编码中文 label；shared 新增 `getOrderStatusI18nKey()`，各端 `t(key)` 展示。

### 实现原理

```text
用户切换语言 → localStorage / uni.storage（LOCALE_STORAGE_KEY）
    ↓
vue-i18n locale + 各端 messages（shared ∪ mobile/web）
    ↓
HTTP 请求头 Accept-Language
    ↓
localeMiddleware → res.locals.locale
    ↓
success/fail → resolveApiMessage → { message, messageKey }
```

服务端动态文案（规划助手回复、变体标签、模板路线名）集中在 shared 的 `plan-messages.ts`、`plan-variants.ts`、`plan-route-names.ts`，由 `plan-session.service` 等传入 `locale` 参数生成。

非 Vue 模块（`location.ts`、`speech-to-text.*`、`api/speech.ts` 等）走 `mobileT()`，与界面语言同源。

### 最终方案

分 **两阶段** 交付：

| 阶段 | 日期 | 范围 |
|------|------|------|
| **框架** | 2026-05-26 | shared i18n 包；Web/移动 `createI18n`；`localeMiddleware`；规划页/Tab/登录主体；`plan-messages` 动态文案；`API_LEGACY_MESSAGE_MAP` |
| **扫尾** | 2026-05-27 | 移动子页（打卡/成就/徽章/排行榜/订单）；`plan` + `VoiceTextComposer` + `login`；`location.ts` / `checkin-map.ts`；语音全平台 + 上传 API 错误；Web 管理端各业务 View；后端高频 `throw new Error` → `ApiError`（30+ key） |

**前端**

- Web / 移动 `createI18n`（`legacy: false`）；移动 `useLocale`、`usePageTitle`、`useInterestTagLabel`、`useTf`。  
- **`mobileT`**：非 Vue 工具链，供定位权限弹窗、ASR 错误、uploadFile 失败等使用。  
- Shared 扩展：`common.*`、`orderStatus.*`、`routeStatus.*`、`getOrderStatusI18nKey()`、`CHECKIN_TIME_RANGE_OPTIONS` 改 `{ labelKey }`。

**后端**

- 全局 `localeMiddleware`；`ApiError` + `ApiMessageKey`；`plan-session` / `route` / `user` / `speech` 等 service 高频错误已接入。  
- `api-legacy-map.ts`：含逗号的中文 legacy key 须加引号，避免 TS1127。

**文档**

- [国际化.md](./国际化.md)：用法、迁移清单、en-US 验收记录（2026-05-27 通过）、扩展新语言步骤。

**已知例外（开发者向，暂不 i18n）**

- `assertRemoteApiBase` 本地 API 提示、`console.*` 日志。  
- 配置类/第三方 SDK 内部英文错误、LLM 原始输出语言未强制跟随 locale（G9+）。

**验收**

- en-US 主流程手测已于 **2026-05-27** 通过（登录 → 规划含语音 → 路线详情/地图 → 打卡 → 订单/成就），见 [国际化.md § en-US 验收](./国际化.md#en-us-验收清单)。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **shared 单源 + 浅合并** | Tab/导航/API 文案一处维护，Web 与移动自动同步。 |
| 2 | **旧中文 message 零破坏迁移** | `API_LEGACY_MESSAGE_MAP` 让存量 `fail(res, '中文')` 也能返回 `messageKey` 与翻译。 |
| 3 | **服务端动态文案 i18n** | 规划助手、多方案标签、RAG 路线名按 locale 生成，不依赖 LLM 自行翻译。 |
| 4 | **Accept-Language 与 UI 绑定** | 界面语言即 API 语言，Toast 可直接展示 `message` 字段。 |
| 5 | **`mobileT` 统一非 Vue 文案** | 定位/语音/上传与页面 `t()` 同源，H5 识别语言随 locale（`zh-CN` / `en-US`）。 |
| 6 | **状态键跨端复用** | `getOrderStatusI18nKey` + shared `orderStatus.*`，移动/Web/API 展示一致。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **UniApp 插值不生效** | 模板带参改用 `useTf().tf()`；`formatMessage` 统一 `{key}` 替换，与 vue-i18n 解耦。 |
| 2 | **`@` 与 vue-i18n 链接语法冲突** | 文案中勿写 `@{var}`；用户名等用模板 `@{{ text }}` + 普通 `{username}` 占位符。 |
| 3 | **DB 存中文、界面要英文** | 兴趣标签/主题用 slug 映射表；库内仍写中文保证 C2/C4 规则匹配，展示层翻译。 |
| 4 | **pages.json 标题静态** | 构建占位 + 页面内 `usePageTitle` + `watch(locale)` 运行时改标题。 |
| 5 | **迁移范围大、触点分散** | 分框架/扫尾两阶段；清单按「移动子页 → plan/语音 → Web → 后端 ApiError」推进；2026-05-27 en-US 主流程手测通过后 G9 标 `[x]`。 |
| 6 | **legacy map 特殊字符** | 中文错误文案含逗号时，`api-legacy-map.ts` 键名必须引号包裹，否则编译报错。 |
| 7 | **条件编译与 i18n** | `speech-to-text.ts` 保留 `#ifdef H5/MP-WEIXIN/APP-PLUS` 导出；仅默认平台分支用 `mobileT`，避免破坏多端构建。 |

---

## H9 住宿与交通编排（LLM 排 POI + 后端 Enricher）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-29（**H9-1～H9-4 已全部落地**） |
| **路线图** | H9 |
| **关联代码（规划）** | `server/services/route-enricher.service.ts`；`server/services/amap-direction.service.ts`；扩展 `travel-intent.service.ts`（C2）；`shared/types.ts`；`route-generator.service.ts`；`llm-client.service.ts` / `ai-service`；`mobile/components/route-day-flow/`、`pages/routes/detail.vue` |

### 背景与目标

H9 要求生成路线含**住与行**、天数切换可见**交通耗时**、意图能识别「住哪」「怎么去」。当前 C 阶段是**单次 LLM 输出完整 JSON**（含 `poiType=hotel/transport`），交通时刻与酒店店名常由模型编造，与 A2 内容库、高德坐标链路不一致，详情页也未分区展示。

设计文档 §3.3 描述多 Agent 协作；MVP 已落地的是 **C1 多轮 + C2 意图 + C3 RAG + 管道式 generateRoute**，并非独立 Agent 协调器。H9 需在不大改架构的前提下提升「可执行感」。

### 思考过程

1. **是否新建「路径规划 Agent」？** 否。无 12306/航班/订房 API 时，独立 Agent 仍是 LLM 编造；还会增加延迟、多步 JSON 合并与调试成本。与 C5 结论一致：**编排式管道**优于过早引入 LangChain 多 Agent。
2. **LLM 一次写全 vs 分工？** 选定 **LLM 只排 POI（玩什么）**，**后端 Enricher 补交通与住宿（怎么动、住哪）**。市内耗时、顺序、时刻表用路网数据 + 确定性算法；酒店用 A2 RAG/高德 POI + 选址规则；大交通 Phase 1 用城市对模板，Phase 3 再接班次 API。
3. **和设计文档 §3.7 遗传算法的关系？** 文档 GA（种群 200）面向「候选景点很多」的组合爆炸；H9 每日 2～6 POI，采用 **Nearest Neighbor + 2-opt + 前向时间调度** 即可，可解释、易单测；GA 留作远期「从 RAG 20+ 候选里选进一天」的增强项。
4. **Enricher 挂在哪？** 放在 `generateRoute` 的 LLM/模板产出之后、`enforceRouteConstraints` 与 `applyRagToRouteDraft` 之前（或紧接其后），使 C1 追问、C4 多方案、Python/Node 降级链**共用同一补全逻辑**；用户改 POI 后交通自动重算，避免整段 JSON 漂移。
5. **与现有地图动画的关系？** `buildRoutePathFromDetail` 当前为 POI 间直线插值；Enricher 产出 `transit[]` 后，地图可只画 `attraction`+有坐标 `hotel`，路段 polyline 来自高德 direction（Phase 2），与 A3-3 动画链路衔接。

### 实现原理

```text
用户 prompt / 多轮 history
  → C2 意图（扩展 transportPreference、lodgingArea/lodgingTier）
  → C3 RAG 景点候选
  → LLM：仅输出 days[].attractions[]（name、poiType、description；time 可空）
  → route-enricher.service（确定性 + 高德 API）
       1. geocode 补坐标（复用 amap-geocode）
       2. assignLodgingPerDay：hotel RAG/POI + 设施选址（最小化到当日 POI 路网耗时和）
       3. buildDailySchedule：Distance Matrix → 最近邻排序 → 2-opt → 选 walk/transit/drive → 前向排时刻
       4. injectIntercitySegments：跨城日 intercity 段（Phase 1 模板；Phase 3 班次 API）
       5. validateTimeWindows + budgetRollup
  → enforceRouteConstraints + applyRagToRouteDraft（现有）
  → 写入 travel_routes / plan_session snapshot
```

**算法与数据分工（摘要）**

| 子问题 | 方法 | 数据依赖 |
|--------|------|----------|
| 市内 POI 顺序 | 最近邻 + 2-opt | 高德 Distance Matrix / Direction |
| 路段耗时与方式 | 规则阈值（如 &lt;800m 步行）+ API 分钟数 | 高德 |
| **玩法动线与段间交通** | **Phase 4 玩法 RAG** + 多候选比选 | **`route_playbooks` / 模板路线 / 攻略摘要** |
| 每日时刻表 | 前向调度 + 开放时长窗检查 | POI 默认游玩时长；attractions `openHours` |
| 酒店 | RAG/DB 候选 + 加权选址 | A2 `category=hotel`；高德 POI 兜底 |
| 大交通 | 约束匹配 + 耗时估算 | Phase 1：`city-pair` 模板；Phase 3：12306/航班 |
| 综合评分 | 轻量加权（体验/预算/时间/多样性） | 可选用于 C4 多方案选优 |

**Schema 扩展（`@douxing/shared`）**

- `RouteTransitSegment`：`kind`（intercity/local）、`mode`（train/flight/subway/taxi/…）、`from/to`、`time`、`durationMinutes`、`cost`、`bookingUrl?`（mock）。
- `RouteDayPlan`：增加 `lodging?`、`transit?[]`；`attractions` 保持兼容，专指游玩 POI。
- `TravelIntentSnapshot`：增加 `transportPreference`、`lodgingArea`、`lodgingTier` 等。

**LLM Prompt 变更**：system prompt 要求**不要**编造交通段与酒店时刻；笼统用餐仍 `poiType=meal`（A2++ 规则不变）。Node 与 Python `ai-service` prompt/schema **同步收窄**。

### 最终方案（分 Phase）

| Phase | 交付 | 验收 |
|-------|------|------|
| **1 — MVP** | shared 类型 + C2 交通/住宿意图 + Enricher 骨架 + 市内 matrix 排程 + 跨城 mock 模板 + **详情页行程流程图** + i18n | 生成路线含结构化 transit/lodging；详情解锁后可见完整路径；意图识别「高铁/住西湖边」 |
| **2** | 高德 direction polyline；酒店 RAG 加强；`buildRoutePathFromDetail` 接真实路段 | 地图与耗时一致；酒店来自内容库 |
| **3** | 12306/航班 API；开放时间表；Enricher 闭馆 `warnings` | 大交通可订票向；与设计文档 §3.7 对齐 |
| **4 — 玩法 RAG** | 玩法知识库 + Playbook RAG；段间 mode 比选；LLM 动线骨架；规则止血（景区不猜地铁） | 西湖等景区推荐步行/观光车；段间方式与攻略一致；流程图可选展示推荐理由 |

**明确不做（H9 范围内）**：独立 LangChain 路径规划 Agent；LLM 单独负责市内真实耗时。

### 亮点（方案层）

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **职责分离** | LLM 擅长「玩什么」，Enricher 擅长「怎么串、要多久」；减少交通/酒店幻觉。 |
| 2 | **复用 C 管道** | 不新建 Agent；C1 追问、C4 多方案、Node/Python 降级共用 Enricher，改 POI 即重算交通。 |
| 3 | **渐进式数据** | Phase 1 无班次 API 也可交付结构化住/行；高德 matrix 与 A2 酒店逐步替 mock。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **高德配额与延迟** | 按日 POI 数 N≤8 调 Matrix；结果 Redis 缓存 `(city, poiSetHash)`；失败降级 Haversine + 固定系数估时并标注 `estimated: true`。 |
| 2 | **时间窗与开放时长** | MVP 用默认游玩 90min + 简单闭馆假设；冲突时 Enricher 写 `warnings[]` 或自动 2-opt 换序；远期 attractions 存 openHours。 |
| 3 | **LLM 仍写交通怎么办** | Prompt 禁止 + Zod 忽略 LLM 内 transit 字段；Enricher 输出覆盖为准。多轮 history 中 assistant snapshot 含完整 enrich 结果，追问改 POI 后整段重算。 |
| 4 | **大交通无 API** | Phase 1 必须 i18n 标明「参考耗时/演示链接」；`bookingUrl` 仅 mock；避免用户误当真实班次。 |
| 5 | **i18n 与分区 UI** | 住/行/玩区块、交通方式标签、估算提示走 shared + mobile locale；API 动态字段（站名）随 `Accept-Language` 与 UI 一致。 |
| 6 | **段间方式规则误判（已修复 H9-4a）** | 原 `pickModeFromDistance` 在无地铁 API 时猜 `subway`；**H9-4a** 改为 `pickLocalTransitMode`，景区簇 + playbook 边优先 walk/观光车。 |

### 与后续 H 阶段关系

- **H9-4 玩法 RAG**：在 C3 景点 RAG 之上补「怎么玩、段间怎么走」；见 [§ H9-4](#h9-4-玩法-rag-与段间交通phase-4)。
- **H8 实时重规划**：复用 Enricher 的 `buildDailySchedule`，输入改为「当前 GPS + 剩余 POI」。
- **H7 错过景点**：对比计划 POI 与打卡轨迹，不改动 Enricher 主链。
- **C4 多方案**：可对 Enricher 前后草案算加权分，替代纯 LLM 主观差异。

---

## H9-1 Enricher MVP（Phase 1）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-27 |
| **路线图** | H9-1（a～g） |
| **关联代码** | `packages/shared/src/types.ts`；`server/services/route-enricher.service.ts`；`amap-direction.service.ts`；`matrix-cache.service.ts`；`data/city-pair-transit.ts`；`travel-intent.service.ts`；`route-generator.service.ts`；`route.service.ts`（`mergeEnrichedDaysWithSyncedAttractions`）；`utils/route-detail.util.ts`（`cloneRouteDaysForSync`）；`llm-client.service.ts`；`ai-service/app/prompts.py`；`mobile/components/route-day-flow/`；`mobile/pages/routes/detail.vue` |

### 思考过程

1. **挂接点**：Enricher 放在 `applyRagToRouteDraft` 之后，确保 POI 坐标与 `attractionId` 已对齐，再排程与补住宿。
2. **LLM 收窄**：Prompt 禁止 hotel/transport；Zod 解析后过滤；Enricher 输出为权威 `lodging`/`transit`。
3. **配额**：市内 N≤8 点 pairwise 距离 + Redis 缓存；失败 Haversine 并标 `estimated`。
4. **入库链路**：景点 sync 时 `cloneRouteDaysForSync` 须保留 `lodging`/`transit`/`warnings`，并与 enrich 结果 merge，否则 DB 里 enrich 字段被丢弃。
5. **路径优化**：酒店作 depot；市内段含酒店↔POI；多起点 2-opt + 前向时刻排程。

### 实现原理

`generateRoute` → LLM/模板 → `enforceRouteConstraints` → RAG 对齐 → **`enrichRouteDraft`**（geocode → 2-opt 排序 → 前向时刻 → 酒店选址 → 跨城 mock）→ 写入 `routeDetail`。

### 最终方案

- shared 扩展 `RouteTransitSegment`、`RouteDayLodging`、`TravelIntentSnapshot` 交通/住宿字段。
- C2 规则解析高铁/住西湖边/经济型酒店等。
- 详情页用 `RouteDayFlowChart` 按时刻展示交通→游玩→住宿（见 [H9-1 UI](#h9-1-ui-行程流程图与展示策略)）。
- 本地演示脚本：`pnpm --filter @douxing/server enricher:demo`（`--offline` 跳过高德）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | 单管道共用 | 单轮 generate、plan-session 追问、模板降级均走同一 Enricher |
| 2 | 渐进降级 | 高德 distance → Redis 缓存 → Haversine 估算，仍可交付结构化 transit |
| 3 | i18n 流程图 UI | 交通方式、估算提示、跨城标签、空状态 zh-CN/en-US 齐全 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | Drizzle decimal 坐标 | `toNumber()` 转换后再算酒店选址距离 |
| 2 | 旧路线兼容 | `lodging`/`transit` 可选；无 Enricher 字段的旧 detail 仍可展示；**须重新生成**才有 enrich 数据 |
| 3 | enrich 入库丢失 | `cloneRouteDaysForSync` 曾只拷贝 `attractions`；修复后 merge enrich 与 sync 结果 |
| 4 | 跨城无 API | `city-pair` 模板 + `estimated` + i18n「演示链接」避免误导 |

---

## H9-1 UI 行程流程图与展示策略

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-27 |
| **路线图** | H9-1g |
| **关联代码** | `mobile/src/utils/route-day-flow.ts`；`mobile/src/components/route-day-flow/RouteDayFlowChart.vue`；`mobile/pages/routes/detail.vue`；`mobile/pages/plan/plan.vue`（已移除对话区预览） |

### 背景与目标

H9-1 后端已产出 `transit[]` 与 `lodging`，详情页需让用户一眼看懂**整日动线**（先怎么去、再玩什么、晚上住哪），而不是三个独立列表。规划页 C1 对话区信息密度已高，不宜再塞完整行程图。

### 思考过程

1. **三分区列表还是一条时间轴？** 交通/游玩/住宿按时刻交错，分区列表会打断阅读顺序；竖向流程图 + 颜色区分类型（蓝/绿/紫）更贴近「路径」心智。
2. **规划页要不要预览？** 首版在对话末尾加过预览，产品反馈对话区应聚焦消息与候选切换；**完整路径只在路线详情页展示**，规划页通过「查看详情」跳转。
3. **节点怎么排序？** `buildRouteDayFlow` 解析 `time` 字段为分钟序；无时刻时回退为 transit → play → lodging 默认顺序。
4. **多日怎么切？** 顶部横向 Tab；单日隐藏 Tab 只显示 day 标题。

### 实现原理

```text
RouteDayPlan（transit + attractions + lodging）
    → buildRouteDayFlow() → RouteFlowNode[]
    → RouteDayFlowChart（竖向 rail + 卡片）
    → detail.vue（解锁后 + 打卡 emit）
```

交通节点 meta 展示「方式 · 时长」；跨城段与 `estimated` 段有 badge；warnings 仍在流程图下方按日展示。

### 最终方案

- 组件 `RouteDayFlowChart`：可复用 props（`showTitle`、`showCheckIn`、`initialDayIndex`）。
- **详情页**：解锁后展示流程图 + 打卡按钮。
- **规划页**：不展示流程图；候选卡片 + 进详情查看完整路径。
- i18n：`routes.flowChartTitle`、`flowChartEmpty`、`flowDayTab`、`flowIntercity` 等。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **一条路径读懂整日** | 交通→游玩→住宿按时间序合并，比三分区更符合出行叙事 |
| 2 | **对话与详情职责分离** | 规划页专注对话与选方案；详情页专注可执行行程 |
| 3 | **组件可复用** | 同一 chart 可用于后续 Web 管理端预览或候选方案对比（未做） |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **无 enrich 的旧路线** | 流程图为空时展示 `flowChartEmpty`；文档与验收说明须「重新生成」 |
| 2 | **时刻解析多样** | 支持 `09:00` 与 `09:00-10:30`；解析失败用 `MAX_SAFE_INTEGER` 排到末尾 |
| 3 | **规划页曾重复拉 session** | 预览移除后，切换候选只需更新 `routeId`，不必为 UI 额外 sync days |

### 下一步（承接 H8）

1. **H8 行中重规划**：用户偏离计划时，Enricher 重算剩余节点，详情页流程图 diff 展示。
2. **地图增强（可选）**：`RouteMapPlayer` 与流程图 transit 段联动高亮。

---

## H9-2 路网 polyline 与酒店 RAG（Phase 2）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-28 |
| **路线图** | H9-2 |
| **关联代码** | `amap-direction.service.ts`（`getDirectionPolyline`）；`polyline-cache.service.ts`；`route-map-path.service.ts`；`routes.ts`（`GET /:id/map-path`）；`attraction-rag.service.ts`（`retrieveHotelsForLodging`）；`route-enricher.service.ts`；`mobile/api/routes.ts`（`fetchRouteMapPath`）；`detail.vue`；`RouteMapPlayer.vue`；`shared/route-path.ts`（`estimated` 字段） |

### 背景与目标

H9-1 已产出结构化 `transit[]` 与 `lodging`，但 [A3-3](#a3-3-路线详情地图预览动画) 地图预览仍用 POI 间直线，与 Enricher 交通方式/耗时不一致；酒店选址也偏 geocode 兜底。Phase 2 要让**地图路径、流程图、Enricher 排程**三者同源，并让住宿优先命中 A2 内容库。

### 思考过程

1. **polyline 算在客户端还是服务端？** 高德 Key 不宜下发小程序；direction 配额需 Redis 缓存；故新增 `GET /routes/:id/map-path`，在服务端基于已有 `routeDetail` 补 polyline，不重复存库。
2. **路段 mode 从哪来？** 不另猜交通方式：市内段匹配 Enricher `transit[]` 的 `from/to`；跨城段取当日 `kind=intercity` 的 mode，与 [H9-1 UI](#h9-1-ui-行程流程图与展示策略) 流程图一致。
3. **酒店 RAG 与景点 RAG 是否共用？** 独立 `retrieveHotelsForLodging`：`category=hotel` 过滤 + 区域/档次/主题加权，Enricher 再按到 POI 距离选址，写入 `attractionId` 与 `source`。
4. **客户端失败怎么办？** `detail.vue` 请求 map-path 失败时回退 `buildRoutePathFromDetail` 本地直线，保证 A3-3 播放器始终可演示。

### 实现原理

```text
routeDetail（含 enrich transit/lodging）
  → buildRoutePathFromDetail（shared，POI 序列 + 直线段骨架）
  → buildRouteMapPathFromDetail
       → 每段 findTransitModeForSegment → getDirectionPolyline（walk/drive）
       → polyline-cache（Redis 7d，坐标对排序 key）
       → 跨城 / API 失败 → 直线 + estimated: true
  → GET /routes/:id/map-path
  → detail.vue → RouteMapPlayer

Enricher 酒店：retrieveHotelsForLodging → 距离加权 → lodging（内容库优先）
```

### 最终方案

- 服务端：`route-map-path.service.ts` + `polyline-cache.service.ts`；`amap-direction.service.ts` 扩展 walking/driving direction 解析 steps polyline。
- API：`GET /api/routes/:id/map-path`（需登录 + 路线解锁权限，与详情一致）。
- 移动端：解锁后 `fetchRouteMapPath`，失败降级本地路径；`RoutePathSegment.estimated` 可在 UI 标注估算路段（与 i18n 流程图 badge 呼应）。
- Enricher：住宿候选改走 `retrieveHotelsForLodging`，无内容库命中时仍 geocode 兜底。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **地图与流程图一致** | 路段 mode 来自 Enricher `transit`，polyline 来自高德 direction，替代 POI 间直线 |
| 2 | **渐进降级** | 无 Key / API 失败 → 直线插值 + `estimated`；客户端 API 失败回退本地 `buildRoutePathFromDetail` |
| 3 | **酒店内容库优先** | RAG 打分（区域/档次/主题）+ 到 POI 距离加权；写入 `attractionId` 与 `source` |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **direction 配额与延迟** | 按路线段并行请求 + Redis 缓存（坐标对排序 key，TTL 7 天）；与 H9-1 matrix 缓存分离前缀，避免混键 |
| 2 | **transit 与 POI 名对齐** | `findTransitModeForSegment` 用 Enricher 的 `from/to` 字符串匹配；对不上则按距离阈值默认 walk/drive |
| 3 | **内容库酒店稀疏** | `cityCode` 与 `city` 双路查询；无 hotel 行时仍 geocode，但 `source` 区分 content vs amap |
| 4 | **旧路线无 enrich** | 无 `transit[]` 时 direction 仍可用默认 mode；完整体验仍须重新生成路线 |

### 下一步（承接 H8 / A3-3+）

1. **H8 行中重规划**：复用 Enricher + map-path，对剩余 POI 重算并 diff 展示。
2. **A3-3+**：打卡足迹页 `mergeRoutePaths` 合并多路线 polyline 动画。
3. **可选**：`RouteMapPlayer` 播放时高亮当前 `transit` 段，与 `RouteDayFlowChart` 联动。
4. **playbook 库扩充**：更多城市 seed；Web 管理端 CRUD；Enricher POI 顺序向 playbook soft 对齐。

---

## H9-4 玩法 RAG 与段间交通（Phase 4）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-29 |
| **路线图** | H9-4（a～d） |
| **关联代码** | `server/data/route-playbooks.ts`；`server/services/playbook-rag.service.ts`；`server/services/route-enricher.service.ts`（`buildTransitLeg`）；`server/services/amap-direction.service.ts`（`pickLocalTransitMode`）；`server/services/route-generator.service.ts`；`server/services/llm-client.service.ts`；`server/services/ai-service-client.service.ts`；`ai-service/app/route_generator.py`；`shared/src/i18n/enricher-messages.ts`；`server/scripts/enricher-demo.ts`（`--playbook`） |

### 背景与问题（用户反馈）

用户在 **西湖** 等景区游玩时，详情流程图出现 **「坐地铁」** 等不合理段间交通。根因并非 LLM「搜了全网」，而是：

1. **C3 景点 RAG** 只解决「玩什么 POI」，不包含「两点之间怎么走」。
2. **Enricher** 用 `getTravelDuration` + 旧版距离规则：距离 &gt;800m 且驾车耗时 ≤25 分钟 → 标记为 `subway`，**未调用任何地铁线路 API**。
3. 缺少 **景区语义**（环湖步行、观光车、游船）与 **攻略级热门动线**。

**玩法 RAG** 目标：检索「经典怎么玩 + 段间交通共识」，再交给 LLM 排 POI、Enricher 排时刻与 mode，使推荐可解释、可执行。

### 思考过程

1. **景点 RAG vs 玩法 RAG 是否合并？** 不合并表结构职责：景点库管 POI 实体；玩法库管 **动线 + 段间边**（from/to/mode/hint）。检索时可先 playbook 定骨架，再 C3 填 POI 细节。
2. **是否实时「搜全网」？** Phase 4 先做 **自建 playbook 库**（官方导览、现有 `route-templates`、人工攻略摘要），质量可控；远期再接网页检索更新 playbook，而非让 LLM 每次裸搜。
3. **Enricher 还要不要高德？** 要。Playbook 定 **mode 优先级与理由**；高德 Matrix/Direction 做 **耗时校验与 `estimated` 标注**；二者冲突时 playbook 优先、高德兜底。
4. **能否先小步交付？** **H9-4a** 独立止血（去除虚假 `subway`）；**H9-4b～d** 同日完成完整玩法 RAG 链路。

### 实现原理（已落地链路）

```text
用户 prompt + C2 意图
  → Playbook RAG（retrievePlaybooksForPlanning：城市/主题/关键词 → 经典动线 + 段间边）
  → C3 景点 RAG（POI 候选，name 对齐库）
  → LLM：formatPlaybookContextForLlm 注入动线摘要 → days[].attractions[]（仍禁止编造 transit/lodging）
  → Enricher：
       1. 段间 mode：findPlaybookSegmentHint → pickLocalTransitMode（playbook 优先）
       2. formatPlaybookTransitDescription → transit.description（i18n 推荐理由）
       3. 高德 Matrix 验时 + estimated 标注
  → 详情 RouteDayFlowChart 展示 mode + description
```

**玩法库 seed（3 条）**

| id | scope | 经典动线要点 |
|----|-------|-------------|
| `hangzhou-west-lake` | 西湖景区 | 断桥 → 白堤 → 孤山 → 苏堤 → 花港 → 雷峰塔；湖内步行为主，花港→雷峰塔观光车 |
| `beijing-forbidden-city` | 故宫博物院 | 午门 → 太和殿 → 乾清宫 → 御花园 → 神武门；全程步行 |
| `shanghai-bund` | 外滩商圈 | 外滩 → 南京路 → 豫园；短距步行或打车 |

### 最终方案（分步 · 已验收）

| 步 | 交付 | 验收 |
|----|------|------|
| **H9-4a** | `pickLocalTransitMode`：无地铁 API 不输出 `subway`；距离分层 walk/bus/taxi/drive；`isDayInScenicCluster` 景区簇优先 walk | `enricher:demo --playbook --offline` 西湖线无 subway |
| **H9-4b** | `ROUTE_PLAYBOOKS` 3 条；`retrievePlaybooksForPlanning` / `findPlaybookSegmentHint` | 杭州+西湖命中 `hangzhou-west-lake` |
| **H9-4c** | Node `llm-client` + Python `route_generator` 注入 playbook 摘要；`generateRoute` / plan-session 追问均经同一管道 | LLM 收到 classicOrder + 段间 hint |
| **H9-4d** | `buildTransitLeg` 读 playbook 边；`enricher.transit.*` i18n（classicWalk/scenicWalk/sightseeingBus/ferry/taxiShort） | 流程图见「景区内建议步行（西湖景区）」「景区建议观光车（西湖景区）」 |

**验收命令**

```bash
pnpm --filter @douxing/server enricher:demo -- --playbook --offline
```

典型输出（西湖 demo）：

```text
花港观鱼 → 雷峰塔 | bus | 景区建议观光车（西湖景区）
苏堤 → 花港观鱼 | walk | 景区内建议步行（西湖景区）
```

**明确不做（H9-4 范围内）**：每次规划实时爬取马蜂窝/小红书全文；独立 LangChain「攻略 Agent」长链推理（仍走 RAG + Enricher 确定性链）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **补 C3 盲区** | 景点 RAG 管实体，玩法 RAG 管动线与段间，分工清晰 |
| 2 | **可渐进** | 4a 止血已发；playbook 可城市级增量录入 |
| 3 | **可解释** | `transit.description` 承载「多数攻略建议苏堤步行」类理由，提升信任 |
| 4 | **全链路挂接** | `generateRoute` → LLM/ai-service → Enricher 共用 `playbookMatches`，plan-session 追问自动继承 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **Playbook 数据从哪来** | Phase 1 用 `route-templates` + 人工整理 TOP 3 城市；字段标准化；Web 管理端后续可 CRUD |
| 2 | **POI 名与 playbook 边对齐** | 边支持 `fromAliases`/`toAliases` + 模糊 `namesMatch`；对不上则降级 `pickLocalTransitMode` 距离规则 |
| 3 | **LLM 仍乱序 POI** | Playbook 作 soft 约束注入 prompt；Enricher 侧暂未做编辑距离重排（远期增强） |
| 4 | **观光车/游船 schema** | 短期用 `bus` + i18n 文案「景区观光车」；远期扩展 `RouteTransitMode` 或 `transit.vehicleType` |
| 5 | **与 H9-3 开放时长联动** | Playbook 动线 + `openHours` + 星期几 → 排程 warnings；闭馆日自动换序或提示（部分已有） |

### 与 C3 / H9-1 关系

| 能力 | C3 景点 RAG | H9-1 Enricher | H9-4 玩法 RAG |
|------|-------------|---------------|---------------|
| 检索对象 | attractions 行 | — | route_playbooks 动线 |
| 输出 | POI 候选注入 LLM | transit/lodging/时刻 | 动线骨架 + 段间 mode 建议 |
| 时机 | LLM 前 | LLM 后 | LLM 前（骨架）+ Enricher（段间） |

### 下一步（远期增强）

> 路线图排期见 [ROADMAP § H9+](./ROADMAP.md#h9-远期增强phase-4-之后)（H9+-1～H9+-8）。

1. **playbook 库扩充**（H9+-1）：更多城市/景区；从 `route-templates` 批量迁移。
2. **Web 管理端 CRUD**（H9+-2）：运营可维护动线与段间边。
3. **Enricher POI 顺序 soft 对齐**（H9+-3）：对 playbook `classicOrder` 算编辑距离，必要时重排。
4. **C2 解析 `startDate`**（H9+-4）：与 H9-3 Catalog/Juhe 日期查询更准确联动。
5. **开放时长数据补全**（H9+-5）、**景区交通 schema**（H9+-6）、**Juhe 可选**（H9+-7）、**Playbook 外部检索**（H9+-8）：见 ROADMAP H9+ 表。

---

## H1-c 全站视觉统一

| 项 | 说明 |
|----|------|
| **完成日** | 2026-06-01（H1-a/b 部分交付 2026-05-29；token 扫尾 2026-06-01） |
| **路线图** | H1-c |
| **关联代码** | `styles/theme.css`（含 `--dx-primary-border/ring`）；`utils/theme-colors.ts`；`i18n/useTheme.ts`；`RouteMapPlayer/ByDay`、`RouteDayFlowChart`、`RouteDayTabs`、`VoiceTextComposer`、`AiPlanBlockingOverlay`；`utils/route-map.ts`、`checkin-map.ts`；成就/徽章/排行榜解锁态 |

### 背景与目标

H1-a/b 已交付品牌 Token 与空状态；H1-c 要求**主流程页面视觉一致**，并支持**探索青/主色蓝**主题切换。产品侧还要求 Tab 页采用「Header 固定 + 中间内容区 + TabBar 固定」布局：**内容未溢出时不滚动**，避免整页 `scroll-view` 与轮播/地图手势冲突。

### 思考过程

1. **Token 放哪？** 延续 H1-a，集中在 `theme.css`；新增 `.theme-teal` / `page.theme-teal` **主辅色对调**（蓝主青辅 ↔ 青主蓝辅），避免各页散落 hex。  
2. **主题怎么存？** `useTheme()` + `uni.setStorageSync('douxing_app_theme')`；**`App.vue` `onLaunch` 调 `initAppTheme()`**，避免冷启动闪默认色；各页根节点 `:class="themeClass"`。  
3. **Tab 页布局怎么拆？** 参考路线 Tab：`height: 100vh` + `flex` 列；**Hero 单独 `page-header`**；快捷入口/列表进 **`page-content` scroll-view**；`DouxingTabBar` 与 scroll 同级。首页、我的页均按此改造。  
4. **何时允许滚动？** `syncPageLayout()` 用 `createSelectorQuery` 比较 `.page-body` 与 `.page-content` 高度，动态 `:scroll-y="contentNeedsScroll"`，内容够放则禁用纵向滚动。  
5. **子页（非 Tab）怎么统一？** 打卡/成就/徽章/排行榜/订单等采用 **page-hero（渐变 + emoji + 标题/副文案）+ filter-panel + page-body 卡片**，与 [品牌视觉规范 §8](./品牌视觉规范.md) 对齐。  
6. **首页热门路线怎么更「可传播」？** 在保持卡片字段不变前提下，横向 `scroll-view` 改为 **`swiper` Cover Flow**：居中放大、两侧缩小+模糊+3D 旋转；测量卡片高度设定 swiper 高度，避免固定 `min-height` 撑高页面。

### 实现原理

```text
theme.css（--dx-* + .theme-teal 覆盖）
    ↓
useTheme → themeClass 挂到 page 根
    ↓
Tab 页：page-header（Hero）| page-content（scroll-y 条件开启）| DouxingTabBar
    ↓
子页：page-hero | filter-panel（tabs/chips）| page-body（卡片列表）
    ↓
首页热门：swiper + cardCarouselClass + hotSwiperStyle（测量高度）
```

**已包装页面（2026-05-29）**

| 类型 | 页面 |
|------|------|
| Tab + Hero | 首页、规划、路线、我的 |
| 子页 Hero | 编辑资料、会员、打卡列表/地图、成就、徽章、排行榜、我的订单 |
| 登录 | 自定义 Hero + 登录/注册双色渐变 |
| 交互增强 | 首页热门路线 Cover Flow；我的页未登录 Hero 横向 CTA |

**2026-06-01 token 扫尾**

- 新增 `theme-colors.ts`：`getDxPrimaryColor()` / `getDxPrimaryColorAlpha()`，供 **微信 map polyline**、marker label、原生 **switch** 等无法写 CSS 变量的场景。  
- `RouteMapPlayer` 监听 `themeId`，主题切换后 polyline/marker 重算。  
- `AiPlanBlockingOverlay` 根节点挂 `themeClass`，遮罩 spinner/按钮走 `--dx-*`。  
- CSS 组件：`VoiceTextComposer`、`RouteDayFlowChart`、`RouteDayTabs`、`RouteMapByDay` 等 `#1677ff` → `var(--dx-primary)`。  
- 成就/徽章/排行榜 **解锁态边框** → `--dx-primary-border`（青主题自动换色）。

### 最终方案

- **主题**：`profile` 宫格增加「主题 🎨」入口；`DouxingTabBar` 选中色改为 `var(--dx-primary)`。  
- **布局**：首页/我的采用 Header + 条件滚动内容区；路线 Tab 维持 header + 列表 scroll 模式。  
- **订单页**：Hero + 胶囊 Tab 筛选面板 + token 化卡片；新增 `orders.pageDesc` i18n。  
- **运行时色**：`theme-colors.ts` 供 map polyline、switch 等；`RouteMapPlayer` 监听 `themeId` 重绘。  
- **验收**：zh-CN/en-US 切换主流程不破版；蓝/青主题下地图、语音、遮罩、解锁态同步变色。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **Token + 主题一套代码** | `theme.css` 对调主辅色即可切换探索青/经典蓝，TabBar 与 Hero 同步 |
| 2 | **条件滚动** | 测量内容高度再开 `scroll-y`，Tab 页既不整页乱滚，溢出时仍可滑到底 |
| 3 | **子页模板可复制** | Hero + filter-panel + 卡片列表，订单/打卡/成就等风格一致、接入快 |
| 4 | **Cover Flow 不改数据** | 热门路线仍用 `fetchPlazaRoutes`，仅交互层 swiper 化，便于后续接 H2 海报 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **swiper 必须设高度** | 微信小程序 `swiper` 不能纯 content 撑开；用 `boundingClientRect` 测 `.hot-card` 高度赋给 `hotSwiperStyle` |
| 2 | **scroll-view 与 swiper 嵌套** | 整页 scroll 时手势冲突；改为中间区域 scroll，Hero/TabBar 固定 |
| 3 | **误删 import 导致运行时错误** | 我的页加 `useTheme` 时曾漏 `useInterestTagLabel`，微信 devtools 报 `ReferenceError`；合并功能时须核对 script import |
| 4 | **主题未全覆盖（已解决）** | 地图/switch 须运行时 hex → `theme-colors.ts`；CSS 走 `var(--dx-primary)`；overlay 自带 `themeClass`。 |
| 5 | **未登录 Hero 布局** | 全宽登录按钮留白过多；改为横向 brand + 紧凑 CTA，与首页 guest-bar 心智一致 |

### 下一步（承接 H2-a+）

> H2-a + D5-a 已于 2026-06-01 验收，详见 [§ H2-a+D5-a](#h2-a--d5-a-路线手帐海报与分享链路)。

1. **H2-a+**：副标题、配色预设、每天 POI 条数、角标贴纸。  
2. Web 管理端视觉统一（可选，未纳入 H1 范围）。

---

## H2-a + D5-a 路线手帐海报与分享链路

| 项 | 说明 |
|----|------|
| **完成日** | 2026-06-01 |
| **路线图** | H2-a、D5-a |
| **关联代码** | `packages/mobile/src/poster/`（`build-poster-payload.ts`、`poster-layout.ts`、`draw-kind-badge.ts`、`draw-poster-images.ts`、`registry.ts`、`templates/*`、`canvas-export.ts`、`draw-qr-code.ts`）；`components/route-poster/RoutePosterSheet.vue`；`utils/route-map.ts`；`pages/routes/detail.vue`；`pages/share/route.vue`；`packages/server/src/routes/share.ts`、`services/share.service.ts` |

### 背景与目标

宣传 Sprint Phase 3 要求：用户在路线详情一键生成**手帐风分享图**，保存相册或转发小程序卡片；海报二维码可打开 **H5 只读页**，让未安装 App 的好友浏览已公开路线摘要。App 内仍走探索青/蓝 token，分享图另用手帐纸感视觉，与 H1-c 主流程区分。

### 思考过程

1. **Canvas 放哪一层？** 不引入服务端渲染；统一 `packages/mobile/src/poster/` 管线（payload → 模板 → Canvas → PNG），H2-b 打卡海报可复用。  
2. **模板怎么扩展？** `registry` 注册表 + 模板 ID；MVP 交付 `timeline-columns`（分列行程）与 `diary-page`（日记页拍立得）；`guide-map` / `path-scrapbook` 后置。  
3. **二维码链什么？** v0.8.7 起 **三层兜底**：H5 只读 URL → 服务端小程序码 PNG → 微信 URL Link + 客户端标准 QR；见 [§ D5 补全](#d5-补全--海报二维码与行程全量展示)。  
4. **分享 API 要不要登录？** `GET /api/share/routes/:id` 仅返回**已发布**路线摘要，无 JWT；与广场公开心智一致。  
5. **i18n 怎么不破版？** 海报文案走 `routes.poster.*`；英文路线名/POI 较长时模板内 `wrapText` 换行（v0.8.6 起描述与副标题不再 80 字截断）。

### 实现原理

```text
路线详情 detail.vue
  → buildPosterPayload(route)（含 coverImageUrl，见 A2+++）
  → RoutePosterSheet 选模板 → registry[templateId].draw(ctx, payload)
  → canvas-export → 保存相册 / onShareAppMessage

buildRouteShareUrl(routeId) → H5 URL（可选）
  → draw-qr-code（qrcode-generator）或 drawQrImage（小程序码 PNG）

无 H5 时：
  → GET /share/routes/:id/wxacode → load-poster-image（小程序写临时文件）
  → 失败则 GET /share/routes/:id/link → URL Link → 客户端 drawQrCode

扫码 / 打开 H5 或小程序
  → pages/share/route.vue（支持 query.id 与 scene id=）→ GET /api/share/routes/:id
```

### 最终方案

- **移动端**：路线详情「生成分享图」弹层；双模板预览；保存相册 + 微信转发；`enableShareAppMessage` 带路线标题。  
- **D5-a**：`share/route.vue` 只读页展示路线名、天数、POI 列表（含封面缩略图）；海报 QR 指向该页。  
- **D5 补全（v0.8.7）**：`GET /share/routes/:id/wxacode`、`/link`；`wechat-mini.service` 调微信 `getwxacodeunlimit` / `generate_urllink`；已发布路线即可生成码（生成海报不要求广场公开）。  
- **后端**：`share.service` 只读 API 仍校验 **已发布 + 广场公开**；错误走 `ApiMessageKey`（`SHARE_ROUTE_*`、`SHARE_WXACODE_*`）。  
- **环境**：见 [env-environments.md §8](./env-environments.md#8-海报二维码配置) — 前端 `VITE_H5_BASE_URL`；后端 `MP_WEIXIN_APPID` + `WECHAT_MINI_APP_SECRET`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **统一 poster 管线** | payload / registry / template 分层，H2-b 打卡海报可复用同一导出链路 |
| 2 | **App 与分享图视觉分离** | 手帐纸感 + 米黄主卡专用于传播，不污染 `--dx-*` 主流程 |
| 3 | **公开分享零登录** | 独立 share API + H5 页，拉新路径清晰；与详情页权限解耦 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **小程序 Canvas 异步绘图** | 网络图须 `load-poster-image` 预加载后再 draw；日记页拍立得区依赖 A2+++ 封面 |
| 2 | **QR 与 H5 基址配置** | 开发/生产 `VITE_H5_BASE_URL` 分环境；无 H5 时走服务端小程序码与 URL Link（v0.8.7） |
| 3 | **en-US 长文案破版** | 模板内 `wrapText` 换行 + 动态 `measurePosterHeight`；**H2-a+** 再提供用户可选 POI 条数 |

### 下一步（承接 H2-a+）

1. 副标题、2～4 套配色预设、**用户可选**每天 POI 数（2/3/4）、角标贴纸 PNG。  
2. **H10-c**：带视频 POI 在海报上预留播放/扫码入口。

---

## D5 补全 · 海报二维码与行程全量展示

| 项 | 说明 |
|----|------|
| **完成日** | 2026-06-01（v0.8.7） |
| **路线图** | D5 补全（承接 D5-a） |
| **关联代码** | `packages/mobile/src/poster/draw-qr-code.ts`、`load-poster-image.ts`、`build-poster-payload.ts`、`poster-layout.ts`、`draw-kind-badge.ts`、`templates/diary-page.ts`；`RoutePosterSheet.vue`；`pages/share/route.vue`；`packages/server/src/routes/share.ts`、`services/wechat-mini.service.ts`、`services/share.service.ts` |

### 背景与目标

用户反馈：海报底栏 **二维码空白**（仅半透明占位）；日记卡片 **内容被裁切** 且出现「还有 N 项」却不展开。根因分别为：小程序包未配 H5、Canvas 无法直接加载 base64 小程序码、payload **人为截断**（每天 8 节点 / 5 天），以及标题行 **测高与绘制不一致**。

### 最终方案

**海报 QR（三层兜底，优先级从高到低）**

1. **H5 QR**：`VITE_H5_BASE_URL` + `qrcode-generator` → `{base}/#/pages/share/route?id=`  
2. **小程序码 PNG**：`GET /api/share/routes/:id/wxacode` → 微信 `getwxacodeunlimit`（`scene=id={routeId}`，`page=pages/share/route`）→ 小程序端 **写 USER_DATA_PATH 临时文件** 再 `canvas.createImage`  
3. **URL Link QR**：上述失败时 `GET /api/share/routes/:id/link` → `generate_urllink` → 客户端 `drawQrCode`

**行程全量展示**

- 取消 `MAX_ITEMS_PER_DAY=8`、`MAX_DISPLAY_DAYS=5`；payload 输出 **全部天数、全部 flow 节点**（交通/游玩/住宿）。  
- 不再生成「还有 N 项 / 还有 N 天」；画布高度由 `measurePosterHeight` 随内容增高（上限 **4096px**，微信小程序 Canvas 硬限制）。

**版式修复**

- `measureItemTitleRowHeight` 与 `drawItemTitleRow` 对齐，避免日记卡片白底矮于内容。  
- 生成分享码：已 **发布** 即可（`assertPublishedRouteForPoster`）；只读 H5 API 仍要求 **广场公开**。

### 配置速查

| 变量 | 文件 | 作用 |
|------|------|------|
| `VITE_H5_BASE_URL` | `.env.development` / `.env.staging` / `.env.production` | 打包时写入小程序/H5；H5 二维码 |
| `MP_WEIXIN_APPID` | 根目录 `.env`（服务端） | 小程序码 / URL Link |
| `WECHAT_MINI_APP_SECRET` | 根目录 `.env`（服务端） | 同上 |
| `WECHAT_MINI_ENV_VERSION` | 可选，`develop` / `trial` / `release` | 体验版/正式版小程序码 |

改 `VITE_*` 后须 **重新编译** 小程序；改服务端密钥后须 **重启 API**。

### 重难点

| # | 重难点 | 说明 |
|---|--------|------|
| 1 | **小程序 Canvas 不支持 data URL 图** | 小程序码须 `writeFile` 到本地路径再绘制 |
| 2 | **生产包无 VITE_H5** | 依赖服务端微信配置；`api.yxtao.site` 的 `.env` 须同步 AppSecret |
| 3 | **超长路线 >4096px** | 平台裁切；极端行程需 H2-a+ 用户自选条数或分页海报（远期） |

---

## A2+++ 景点封面 Phase 0

| 项 | 说明 |
|----|------|
| **完成日** | 2026-06-01 |
| **路线图** | A2+++ Phase 0 |
| **关联代码** | 迁移 `0018_attraction_cover_image.sql`；`packages/server/src/db/schema/attractions.ts`；`services/attraction.service.ts`、`route-attraction-media.service.ts`；`routes/attractions.ts`；`services/attraction-image-enricher.service.ts`（骨架）；`packages/web/src/views/AttractionsManageView.vue`；`RouteDayFlowChart.vue`、`build-poster-payload.ts`、`pages/share/route.vue`；`@douxing/shared` `AttractionImageSource` |

### 背景与目标

路线详情、手帐海报、H5 分享页的 POI 块需要**可读缩略图**，提升传播质感。产品决策：**不接入爬虫**作主路径；Phase 0 先做 schema + 运营上传 + 消费端展示，Phase 1 再补高德 POI 图 / OSS / UGC 聚合。

### 思考过程

1. **封面存哪？** 扩展 `attractions` 表：`cover_image_url`、`image_source`、`image_license`、`image_attribution`、`image_fetched_at`，便于 Phase 1 带来源合规字段。  
2. **路线 JSON 要不要改？** 不改持久化结构；`enrichRouteDetailWithAttractionCovers` 在 `getRouteById` / `getPublicSharedRoute` 响应时按 `attractionId` 注入 `coverImageUrl`。  
3. **上传放哪？** Web 管理端 `/attractions/manage` 目录页；静态文件 `/uploads/attractions`，与现有 public asset 工具对齐。  
4. **自动补图怎么做？** Phase 0 仅骨架 `attraction-image-enricher.service.ts` + CLI 占位；避免半成品爬虫上线。  
5. **为何拒绝爬虫主路径？** 版权/稳定性/维护成本高；高德 POI + 运营 seed + UGC 更可控。

### 实现原理

```text
Web AttractionsManageView
  → POST /api/attractions/admin/:id/cover（multipart）
  → 写 uploads + updateAttractionCoverImage

getRouteById / getPublicSharedRoute
  → enrichRouteDetailWithAttractionCovers
  → RouteDayAttraction.coverImageUrl

消费端：RouteDayFlowChart · diary-page 模板 · share/route.vue · buildPosterPayload
```

### 最终方案

- **数据层**：Drizzle 迁移 + shared 类型 `AttractionImageSource`（`manual` / `amap` / `wikimedia` / `ugc` 等枚举预留）。  
- **服务端**：管理端 catalog + 封面上传；`ATTRACTION_COVER_*` ApiMessageKey。  
- **Web**：景点目录表格 + 封面上传；i18n `web.attractionsManage.*`。  
- **移动端**：流程图节点缩略图；手帐「日记页」有封面时绘真图；分享页 POI 列表带图。  
- **Phase 1 预留**：`scripts/enrich-attraction-images.ts` 调用 enricher 骨架。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **Enrich 注入不改路线 JSON** | 封面随 attractions 表更新，历史路线自动受益 |
| 2 | **合规字段前置** | source/license/attribution 为 Phase 1 自动拉图铺路 |
| 3 | **一条链路多端消费** | 同一 `coverImageUrl` 覆盖详情、海报、分享页 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **小程序远程图 Canvas 绘制** | `load-poster-image` 跨域/缓存；失败回退占位框 |
| 2 | **管理端与静态路径** | `public-asset-url.util` 扩展 attractions 目录；生产 Nginx 需放行 `/uploads` |
| 3 | **Phase 0 边界** | 明确不做爬虫；enricher 仅骨架，避免误用半成品脚本 |

### 下一步（Phase 1）

1. ~~高德 POI 图拉取 + `image_source=amap`~~ → 见 [A2+++ Phase 1](#a2-景点封面-phase-1)。  
2. Wikimedia fallback、UGC 聚合策略。  
3. OSS 持久化（替代单机 `uploads`）；批量 CLI 定时任务（可选）。

---

## A2+++ 景点封面 Phase 1

| 项 | 说明 |
|----|------|
| **完成日** | 2026-06-01 |
| **路线图** | A2+++ Phase 1（局部：本地盘 + 高德拉图；OSS / Wikimedia 待续） |
| **关联代码** | `amap-poi-image.service.ts`、`attraction-image-enricher.service.ts`、`utils/download-asset.util.ts`、`utils/local-upload.util.ts`；`attraction-sync.service.ts`（同步 await 拉图）；`route-attraction-media.service.ts`（`getAttractionsByIdsForRouteMedia`）；`scripts/enrich-attraction-images.ts`；`drizzle/0018` + `meta/_journal.json`；详见 [代码解析与审核.md](./代码解析与审核.md) §2 |

### 背景与目标

Phase 0 仅有运营上传；新生成路线 POI 无封面，手帐/行程路径观感差。Phase 1 目标：**高德 POI 图自动拉取 → 本地持久化 → 注入路线展示**，且不无限占用磁盘。

### 思考过程

1. **何时拉图？** 路线同步入库后立刻需要封面；纯 fire-and-forget 导致首屏无图。→ **sync 结束 await 批量拉图**，读详情时再兜底补拉。  
2. **pending 景点读不到？** 注入封面原用 `getAttractionsByIds`（仅 active），AI 新建多为 pending。→ **`getAttractionsByIdsForRouteMedia`（active + pending）**。  
3. **磁盘会爆吗？** 图片在**磁盘**非内存；曾用 `{id}-amap-{timestamp}` 会堆文件。→ **每景点固定 `{id}-amap.*` 覆盖** + 更新封面时删旧文件。  
4. **合规？** 不爬虫；`image_source=amap`、`image_attribution` 写高德 POI 来源；不覆盖 `manual` 上传。

### 实现原理

```text
syncAttractionsFromRouteDetail
  → 新建/合并且无封面 → 收集 ID
  → await enrichAttractionCoversForIds（高德 place/text → detail → download → DB）
  → schedule 队列仅作兜底

getRouteById / share
  → enrichRouteDetailWithAttractionCovers
  → getAttractionsByIdsForRouteMedia
  → 仍无封面则再 await 补拉（最多 16 条）
  → 注入 routeDetail.days[].attractions[].coverImageUrl
```

### 最终方案

- **高德**：`place/text extensions=all` 取 photos，无图则 `place/detail` 回退。  
- **存储**：`packages/server/uploads/attractions/{id}-amap.jpg`（单景点最多 1 张 amap 图）。  
- **CLI**：`pnpm enrich:attraction-images`（支持 `--dry-run`、`--limit=N`）。  
- **环境**：`AMAP_WEB_KEY`、`AMAP_IMAGE_ENRICH_ENABLED`、`AMAP_IMAGE_FETCH_DELAY_MS`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **同步 await + 读时兜底** | 生成路线返回前尽量已有封面；打开旧路线也会补拉 |
| 2 | **pending 可展示** | 与 AI 同步 pending 入库策略一致 |
| 3 | **磁盘线性可控** | 按景点数增长，重复拉图覆盖不堆文件 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **0018 迁移未执行** | SQL 须登记 `drizzle/meta/_journal.json`，否则 migrate 静默跳过 |
| 2 | **首屏无图** | active-only 查询 + 异步拉图；改为 pending 查询 + sync await |
| 3 | **生产磁盘** | 监控 `uploads/attractions`；量大时迁 OSS（Roadmap 待办） |

### 下一步

1. OSS/COS 存储与 CDN 公网 URL。  
2. Wikimedia fallback、孤儿文件清理脚本。  
3. 管理端「重新拉取高德封面」按钮（`refreshAttractionCoverFromAmap` 已预留）。

---

## H2-a 手帐内容与行程展示增强

| 项 | 说明 |
|----|------|
| **完成日** | 2026-06-01（v0.8.5 首版 · v0.8.6 体验扫尾 · **v0.8.7 全量节点**） |
| **路线图** | H2-a 迭代（内容完整性，非 H2-a+ 自定义选项） |
| **关联代码** | `poster/build-poster-payload.ts`、`poster/poster-layout.ts`（`measurePosterHeight`）、`poster/draw-kind-badge.ts`、`poster/draw-poster-images.ts`、`templates/diary-page.ts`、`templates/timeline-columns.ts`；`RoutePosterSheet.vue`（两阶段测高）；`RouteDayFlowChart.vue`（缩略图预览）；`utils/route-map.ts`（打卡点名称气泡） |

### 背景与目标

手帐海报与「行程路径」展示路线时，用户反馈**内容细节不全**：仅部分 POI、无交通/住宿、日记模板每天只显示 1 条；后续又反馈**描述被省略**、**日记只出 1 天**、**图片仅每天 1 张**、**分列标题被裁切**、**类型标签应在标题前**；地图预览仅序号、无法对应景点。

### 最终方案（v0.8.5 + v0.8.6）

**数据与 Payload**

- 复用 `buildRouteDayFlow`，与详情页行程路径一致（交通 / 游玩 / 住宿）。  
- v0.8.6：**不再**对路线简介、节点 `description` 做 80 字截断；完整换行绘制（`WRAP_LINES_UNLIMITED`）。  
- v0.8.7：**取消**每天 8 节点、5 天上限与「还有 N 项/天」提示；**全部节点、全部天数**进入 payload，画布随 `measurePosterHeight` 增高（上限 4096px）。  
- 每日 `highlightImages`：从当天游玩节点收集最多 **3 张**去重封面（`draw-poster-images.ts`，仅影响照片条，不截断行程列表）。

**模板与版式**

- **`diary-page`**：顶栏与每日卡片高度随标题/简介/描述撑开；每日 **照片条**（1 张通栏或 3 张并排，白框阴影）；行程项 **标签 + 标题同一行**（`drawItemTitleRow`）；绘制**全部天数**（去掉空间不足时的 `break`）。  
- **`timeline-columns`**：列标题绿色条 **随文案增高**（不再固定 52px）；副标题与描述完整换行；列顶缩略图条。  
- **画布高度**：`RoutePosterSheet` 两阶段 `measurePosterHeight`；上限 **4096px**（微信 Canvas 硬限制）。  
- v0.8.7：`measureItemTitleRowHeight` 与 `drawItemTitleRow` 对齐，日记卡片高度随内容撑开。

**路线地图预览**

- `buildRoutePoiMarkers`：`callout.display: 'ALWAYS'`，文案 `序号. 景点名`（过长截断）；图钉 `label` 仍显示序号。

**交互**

- 行程路径缩略图 `uni.previewImage` 大图预览（`routes.coverImagePreview`）。

### 重难点

| # | 重难点 | 说明 |
|---|--------|------|
| 1 | **固定 1334px / 估算偏低裁切** | 两阶段 `measurePosterHeight` + 移除日记循环 `break` |
| 2 | **MVP 截断过狠** | 原 MAX_POI=3、diary 只画 items[0]；已与 Enricher 动线对齐 |
| 3 | **描述与顶栏省略** | `wrapText` 不限行 + 动态 header 高度 |
| 4 | **分列标题裁切** | `measureTimelineDayLabelBarHeight` 按换行撑开绿条 |
| 5 | **标签在标题上** | 共用 `draw-kind-badge`，标题从标签右侧起排 |
| 6 | **地图仅序号** | 常驻 callout 标景点名 |
| 7 | **「还有 N 项」误导** | v0.8.7 取消 payload 截断，改为全量绘制 + 动态增高（见 [§ D5 补全](#d5-补全--海报二维码与行程全量展示)） |

---

## I1 ML 训练数据生成与校验

| 项 | 说明 |
|----|------|
| **完成日** | 2026-06-01 |
| **路线图** | I1 / C6 |
| **关联代码** | `packages/server/src/scripts/generate-training-dataset.ts`、`validate-training-dataset.ts`；`services/training-data.service.ts`；`services/llm-route-generator.service.ts`；`packages/ml-training/`（`prompts-seed.jsonl`、`configs/distill-qwen-7b-lora.yaml`）；根脚本 `pnpm ml:generate-dataset`、`pnpm ml:validate-dataset` |

### 背景与目标

[阿里云专属模型操作指南](./阿里云-兜行专属模型训练与部署.md) 阶段一要求：在 **不改变 RAG + Enricher 架构** 的前提下，用 DeepSeek 批量产出 SFT 训练 JSONL，且 assistant 输出须与线上 **同一套 Zod schema** 对齐（只含 POI，不含 transit/lodging）。目标是为 PAI LoRA 微调（I2）提供可校验、可划分的数据集。

### 思考过程

1. **训练样本从哪来？** 不能手抄 JSON；应复用线上 `C2 意图 → C3 RAG → H9-4 playbook → DeepSeek generateRouteFromLlm` 全链路，保证 prompt 与生产一致。  
2. **assistant 里要不要 enrich 字段？** 不要。`draftToLlmTrainingPayload` 过滤 `hotel/transport`，与 LLM prompt 收窄（H9-1f）一致；交通/住宿仍由 Enricher 在线补全，模型只学「玩什么」。  
3. **如何保证数据质量？** 生成时用 `llmRouteSchema.safeParse` + **POI 命中率**（相对 RAG 候选白名单 ≥90%）；不合格行跳过，避免脏数据进 train。  
4. **脚本放哪？** 逻辑在 `packages/server`（可访问 DB RAG 与 LLM）；产物落在 `packages/ml-training/datasets/`，与 LoRA 配置同目录，便于上传 OSS。

### 实现原理

```text
prompts-seed.jsonl
  → generate-training-dataset.ts（逐条）
       C2 buildIntentFromHistory
       → C3 retrieveAttractionsForPlanning
       → H9-4 retrievePlaybooksForPlanning
       → generateRouteFromLlm（DeepSeek）
       → buildTrainingSample（system+user+assistant JSON）
       → validateTrainingSample（Zod + POI hit rate）
  → generated/raw.jsonl
  → validate-training-dataset.ts --split
  → train.jsonl / val.jsonl（默认 9:1）
```

`buildTrainingSample` 调用 `buildRoutePlannerMessages`，assistant 内容为 `JSON.stringify(draftToLlmTrainingPayload(draft))`，与 Node LLM 训练目标一致。

### 最终方案

- 根命令：`pnpm ml:generate-dataset`（可选 `--limit`、`--delay-ms`、`--seed`）；`pnpm ml:validate-dataset -- --split`。  
- 前置：`DEEPSEEK_API_KEY` + MySQL（RAG 检索）；种子见 `packages/ml-training/datasets/prompts-seed.jsonl`（可扩充城市/主题）。  
- 校验：`validateTrainingSample` 检查 JSON 合法、days 与 routeDetail.days 一致、POI 命中率阈值。  
- 下一步（I2）：上传 train/val 至阿里云 OSS → PAI Model Gallery LoRA，见 [阿里云 doc §3～§5](./阿里云-兜行专属模型训练与部署.md)。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **复用生产 prompt 链** | 意图/RAG/playbook/LLM 与线上一致，微调后行为漂移风险低 |
| 2 | **Schema 单源校验** | `llmRouteSchema` 同时用于推理与造数，JSON 合法率可量化 |
| 3 | **POI 命中率门禁** | 默认 ≥90% 相对 RAG 白名单，减少模型学「库外幻觉 POI」 |
| 4 | **与 Enricher 解耦** | 训练目标不含 transit/lodging，职责边界与 H9 架构一致 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **DeepSeek 配额与耗时** | 种子逐条生成 + `--delay-ms` 限流；失败/校验不通过计入 skip，可重跑补量 |
| 2 | **种子覆盖不足** | 初版 `prompts-seed.jsonl` 条数有限；I2 前须按 TOP 城市/主题扩充 seed |
| 3 | **RAG 库小城市 miss** | 无 city 或库内无 POI 时命中率低会被 skip；与 C3 内容库扩充联动 |
| 4 | **train/val 划分简单** | 当前为顺序 slice 9:1；数据量大后应按 city/主题分层抽样 |

### 下一步（承接 I2）

1. 扩充 `prompts-seed.jsonl`（目标 ≥200～500 条有效样本）。  
2. 执行 `pnpm ml:validate-dataset -- --split`，上传 OSS。  
3. 按 `distill-qwen-7b-lora.yaml` 在 PAI 发起 LoRA；完成后 I3 百炼 Endpoint 接入 `llmProvider`。

---

## 附录：撰写模板（新任务复制使用）

```markdown
## {任务名称}

| 项 | 说明 |
|----|------|
| **完成日** | YYYY-MM-DD |
| **路线图** | {步骤编号} |
| **关联代码** | {路径} |

### 思考过程
（为何这样做、曾考虑又否定的做法，2～4 条即可）

### 实现原理
（关键链路 / 数据流，一句话到一小段）

### 最终方案
（落地结论，不展开代码细节）

### 亮点（2～3 个）
| # | 亮点 | 说明 |

### 重难点（2～3 个）
| # | 重难点 | 思考与解决 |
```

---

## 维护说明

1. 完成可验收任务后，按模板追加一章，**每章 2～3 亮点 + 2～3 重难点**，不写流水账。  
2. [ROADMAP.md](./ROADMAP.md) §2.4 只保留索引表，指向本章对应锚点。  
3. 若思考过程涉及未采纳方案，简要写明「为何不选」，便于后人避免重复踩坑。

---

*文档版本 1.21 · 最后更新：2026-06-01*
