# 兜行 · 开发记录（重难点与亮点）

> 记录每项交付任务的**思考过程**与**最终方案**。  
> 路线图 [ROADMAP.md](./ROADMAP.md) 只保留进度与验收；本文件专注「为什么这样设计、踩过什么坑」。

**文档版本**：1.10  
**更新日期**：2026-05-24  
**维护约定**：**每完成一项可验收任务必须更新本章**（勿只改代码/路线图）；每章含：思考过程、实现原理、最终方案，以及 **2～3 亮点 + 2～3 重难点**，篇幅从简。

### 按阶段速查

| 阶段 | 章节 |
|------|------|
| M0 | [M0-7](#m0-7-手机验证码登录--腾讯云短信) |
| A | [A1](#a1-用户资料与偏好) · [A2](#a2-景点内容基础库) · [A2+](#a2-ai-景点同步pending--合并) · [A2++](#a2-poi-分类过滤入库) · [A3](#a3-路线能力补全) · [A3-1](#a3-1-ai-路线重新生成prompt--景点同步) · [A3-2](#a3-2-移动端-ai-规划全局-loading-与中断) · [A3++](#a3-草稿编辑入口分离) · [A4](#a4-订单状态机) |
| B | [B1](#b1-打卡字段增强) · [B2](#b2-地理围栏打卡) · [B3](#b3-打卡地图-ui) · [B4](#b4-徽章系统) · [B5](#b5-成就体系完善) · [B6](#b6-排行榜) |
| C | [C1](#c1-多轮对话规划) · [C1+](#c1-路线解锁支付开关) · [C2](#c2-意图结构化解析) · [C3](#c3-rag-景点检索) · [C4](#c4-多方案生成) · [C5](#c5-python-ai-微服务) |
| G | [G8](#g8-生产部署与-api-公有化) |
| 基础设施 | [高德](#高德地理编码补坐标) · [Redis](#redis-地理编码缓存) · [Drizzle](#数据库生成与使用命令drizzle) |

---

## 目录

1. [M0-7 手机验证码登录 + 腾讯云短信](#m0-7-手机验证码登录--腾讯云短信)
2. [A1 用户资料与偏好](#a1-用户资料与偏好)
3. [A2 景点/内容基础库](#a2-景点内容基础库)
4. [A2+ AI 景点同步（pending + 合并）](#a2-ai-景点同步pending--合并)
5. [A2++ POI 分类过滤入库](#a2-poi-分类过滤入库)
6. [高德地理编码补坐标](#高德地理编码补坐标)
7. [Redis 地理编码缓存](#redis-地理编码缓存)
8. [数据库生成与使用命令（Drizzle）](#数据库生成与使用命令drizzle)
9. [A3-1 AI 路线重新生成（prompt + 景点同步）](#a3-1-ai-路线重新生成prompt--景点同步)
10. [A3-2 移动端 AI 规划全局 Loading 与中断](#a3-2-移动端-ai-规划全局-loading-与中断)
11. [A3 路线能力补全](#a3-路线能力补全)
12. [A4 订单状态机](#a4-订单状态机)
13. [B1 打卡字段增强](#b1-打卡字段增强)
14. [B2 地理围栏打卡](#b2-地理围栏打卡)
15. [B3 打卡地图 UI](#b3-打卡地图-ui)
16. [B4 徽章系统](#b4-徽章系统)
17. [B5 成就体系完善](#b5-成就体系完善)
18. [B6 排行榜](#b6-排行榜)
19. [C1 多轮对话规划](#c1-多轮对话规划)
20. [C1+ 路线解锁支付开关](#c1-路线解锁支付开关)
21. [C2 意图结构化解析](#c2-意图结构化解析)
22. [C3 RAG 景点检索](#c3-rag-景点检索)
23. [C4 多方案生成](#c4-多方案生成)
24. [C5 Python AI 微服务](#c5-python-ai-微服务)
25. [A3++ 草稿编辑入口分离](#a3-草稿编辑入口分离)
26. [G8 生产部署与 API 公有化](#g8-生产部署与-api-公有化)

---

## M0-7 手机验证码登录 + 腾讯云短信

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | M0-7 |
| **关联代码** | `packages/server/src/services/sms.service.ts`、`config/sms.ts`、`routes/auth.ts`；Web/移动端登录页 |

### 背景与目标

MVP 需要手机号即可登录，且生产环境能发真实短信。设计文档要求验证码登录，但不宜在第一阶段接入多套云厂商。

### 思考过程

1. **登录与注册是否要两个接口？** 体验上用户只想「输手机号 → 收码 → 进 App」。若强行区分注册/登录，会增加表单与文案分支；验证码通过后查 `users.phone` 即可分流建号或发 Token。  
2. **短信 SDK 选型**：先在阿里云与腾讯云之间评估。阿里云 Node SDK 在 `NodeNext` + ESM 下出现 default export 无法 `new` 的问题，修 SDK 成本高；产品侧也更倾向腾讯云，故统一 `tencentcloud-sdk-nodejs`。  
3. **验证码存哪？** 首版无 Redis，用进程内 `Map` + TTL 最快可演示；接受多实例不一致，在路线图 G1 再迁 Redis。  
4. **开发环境没密钥怎么办？** `SMS_PROVIDER=mock` 时在响应/控制台给出 `devCode`，避免阻塞前后端联调。

### 最终方案

- API：`POST /auth/sms/send`、`POST /auth/sms/login`；校验通过后无用户则 `u_{phone}` 自动注册。  
- `config/sms.ts`：`auto | tencent | mock`，配置齐全走腾讯云，否则 mock。  
- 限流：60s 发送冷却、5 分钟 TTL、单码最多校验 5 次。  
- 模板参数用 `TemplateParamSet: [code]`（腾讯云 `{1}` 占位顺序）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **登录注册合一** | 一套接口 + 一个按钮覆盖新/老用户，前端无「注册/登录」分叉。 |
| 2 | **短信渠道可插拔** | `SMS_PROVIDER` 切换腾讯云与 mock，开发不依赖真实计费短信。 |
| 3 | **失败可观测** | mock 模式返回 `devCode`，联调时不必反复查库或日志猜验证码。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **ESM 与云 SDK 兼容** | 阿里云 v4 构造器在 `NodeNext` 下不可用；改为腾讯云 SDK，并在 `package.json` 保持 `"type": "module"` 一致。 |
| 2 | **腾讯云模板参数顺序** | 占位符是 `{1}`、`{2}` 而非命名参数；用数组传参，可选 `TENCENT_SMS_TEMPLATE_MINUTES` 作为第二参数。 |
| 3 | **验证码存储与多实例** | 首版进程内 `Map` 实现简单，但多 Pod 会不一致；文档标明生产需 G1 Redis，避免误以为当前方案可原样上线。 |

---

## A1 用户资料与偏好

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A1 |
| **关联代码** | `routes/users.ts`、`services/user.service.ts`；`packages/mobile/src/pages/profile/edit.vue`；迁移 `0003_user_interest_tags.sql` |

### 背景与目标

验证码登录后用户只有手机号，无法支撑个性化路线推荐。需要可编辑昵称、头像、兴趣标签，并在移动端「我的」入口完成闭环。

### 思考过程

1. **头像是否等 OSS？** G2 对象存储未做，若阻塞 A1 会拖慢整阶段。折中：本地上传至 `uploads/avatars`，用 `API_PUBLIC_BASE_URL` 拼公开 URL，接口形状与未来 OSS 一致。  
2. **兴趣标签自由输入还是预设？** 完全自由会导致推荐与统计口径混乱；采用 `USER_INTEREST_PRESETS` + 最多 8 个，服务端校验，前端只做选择器。  
3. **资料接口粒度**：`GET/PUT /users/me` 与 `POST /users/me/avatar` 分离，避免 multipart 与 JSON 混在一个 PUT 里增加客户端复杂度。

### 最终方案

- `users.interest_tags` JSON 列；`PUT /users/me` 更新昵称、邮箱、标签；头像单独 multipart 上传。  
- 共享常量 `USER_INTEREST_PRESETS`、`USER_INTEREST_MAX` 同步到 `@douxing/shared`。  
- 移动端 `profile/edit.vue` 拉取/提交资料，与 Tab「我的」联动。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **先本地存储、后 OSS 演进** | 头像 URL 模型不变，后续 G2 只换存储实现，不改客户端契约。 |
| 2 | **标签预设 + 服务端校验** | 保证推荐与路线 `interestTags` 口径一致，避免脏标签进入库。 |
| 3 | **类型全仓共享** | `UpdateUserProfileRequest`、`UserInfo.interestTags` 在 shared 包，Web/移动/服务端同一套类型。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **`.env` 与公开 URL** | 头像外链依赖 `API_PUBLIC_BASE_URL`；未配置时回退 `req.protocol + host`，小程序真机需显式配局域网或域名。 |
| 2 | **multipart 与鉴权顺序** | 先 `authMiddleware` 再 `multer`，文件名带 `userId-timestamp` 防覆盖；限制 2MB 与图片 MIME。 |
| 3 | **迁移与旧库** | 新列 `interest_tags` 必须 `db:migrate`；已有用户默认为 `NULL`，前端展示为空数组而非报错。 |

---

## A2 景点/内容基础库

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A2 |
| **关联代码** | `schema/attractions.ts`、`data/attraction-seeds.ts`、`services/attraction.service.ts`、`routes/attractions.ts`；迁移 `0004_attractions.sql` |

### 背景与目标

路线与打卡长期不能只靠字符串景点名。需要主数据表（城市、坐标、标签），并让 `route_detail` 能挂上 `attractionId`，为 RAG、地图打卡铺路。

### 思考过程

1. **表结构先做多少？** 首版不做全文检索、不做向量；`attractions` + 索引（city、name）即可支撑列表 API 与关联。  
2. **匹配发生在什么时候？** 放在路线写入前做一次 enrich，比打卡时再猜名字更稳；打卡直接带 ID。  
3. **种子数据从哪来？** 与 `route-templates.ts` 中景点名对齐，人工补坐标与别名，保证演示路线高匹配率。

### 最终方案

- `attractions` 表 + 28 条种子（杭沪京蓉等）。  
- `GET /api/attractions`、`/cities`、`/:id`。  
- `enrichRouteDetailWithAttractionIds`：按城市 + 名称/别名/包含匹配写回 `attractionId`。  
- `check_ins.attraction_id` 可空外键。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **路线与主数据联动** | 生成/种子路线后节点带 `attractionId`，打卡、成就可复用同一 ID。 |
| 2 | **种子与模板对齐** | 模板里出现的景点名在库里都有对应行，演示时匹配率接近 100%。 |
| 3 | **别名体系** | `aliases` JSON 解决「断桥 / 断桥残雪」等叫法差异，减少重复建点。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **名称不完全一致** | 三层匹配：精确名 → 精确别名 → 包含；包含匹配在 A2+ 中降权，避免误合并。 |
| 2 | **仅关联、不扩充** | A2 初版未写入新景点，库外景点仍无 ID；推动 A2+ 做 AI 同步。 |
| 3 | **旧路线不自动修复** | seed 对已有 `travel_routes` skip；旧数据需重新生成或 `db:reset` 才带 ID。 |

---

## A2+ AI 景点同步（pending + 合并）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A2 增强（路线图 §2.4 记为 A2+） |
| **关联代码** | `services/attraction-sync.service.ts`、`0005_attraction_sync_meta.sql`；`routes/attractions.ts` 审核接口 |

### 背景与目标

A2 只读匹配导致库增长慢、LLM 新景点无 ID。目标：路线生成后**反哺景点库**，但不用 AI 幻觉数据覆盖人工种子。

### 思考过程

1. **全量覆盖行不行？** 不行。LLM 的 `cost` 是估算，坐标常缺失，覆盖种子会破坏信任。应分 **来源（source）** 与 **状态（pending/active）**。  
2. **何时合并、何时新建？** 需要置信度：精确 1.0 / 别名 0.95 才合并；模糊 0.72 只关联风险大，改为新建 `pending`。阈值取 `0.85`。  
3. **pending 用户能用吗？** 生成路线的人需要 `attractionId` 打卡；公开列表只展示 `ACTIVE`，待审数据不污染搜索。  
4. **票价什么叫「最新」？** 仅 `llm_estimate` 且在价为空或超过 90 天可更新；`seed/manual` 永不因 AI 改价。

### 最终方案

- `syncAttractionsFromRouteDetail` 替代纯 enrich：匹配则 `merge`，否则 `INSERT pending`。  
- 字段：`source`、`price_source`、`match_confidence`、`verified_at` 等；坐标可空。  
- 管理端：`GET /attractions/admin/pending`、`POST .../approve` 转 `ACTIVE`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **生成即入库** | 每次规划自动扩充库，匹配率随使用次数上升。 |
| 2 | **分来源合并策略** | 信任数据（seed/manual）防覆盖；llm 行可渐进补全。 |
| 3 | **待审与公开隔离** | 用户侧路线/打卡可用 pending ID；C 端景点列表只见已审核。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **模糊匹配误合并** | 低于 0.85 不合并，新建 pending；精确/别名才更新已有行。 |
| 2 | **AI 无可靠坐标** | pending 允许 `latitude/longitude` 为 NULL；B2 围栏前需地图 API 或人工审核补全。 |
| 3 | **票价不可当真理** | `price_source=llm_estimate` 明示参考价；禁止覆盖 seed 票价，避免误导购票。 |

---

## A2++ POI 分类过滤入库

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A2 增强 |
| **关联代码** | `utils/poi-classifier.ts`、`llm-client.service.ts`、`attraction-sync.service.ts`；迁移 `0006_attraction_category` |

### 思考过程

- 路线节点里「午餐」「住宿」不是 POI，写入主数据会污染检索与地图。  
- 具体饭店/酒店要入库，但和景区应分 `category`，匹配时不能混并。  
- 景点若无坐标，后期围栏与地图无法落地，需在 LLM 与入库两侧约束。

### 实现原理

`poiType` 驱动是否调用 `sync`：仅 `attraction/restaurant/hotel` 且通过名称黑名单的节点写库；`meal/transport/other` 只留在 `route_detail` JSON。LLM 输出用 Zod 校验 `poiType` 与店名规范（景点坐标改为可选，由同步阶段高德补全）。同步链路：**先库内匹配合并**，未匹配再 pending 新建（无坐标也可入库待补全）。

### 最终方案

笼统用餐不入库；具体店名入库；表增加 `category` 字段。景点坐标优先 LLM/高德，匹配阈值 0.85 以上才合并已有行。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **展示与主数据分离** | 路线仍显示「午餐」，但不产生 `attractionId`。 |
| 2 | **三类 POI 分栏** | `attraction` / `restaurant` / `hotel` 分开匹配与展示。 |
| 3 | **LLM 契约前置** | 系统提示 + Zod 约束，减少脏数据进库。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **泛称识别** | 黑名单 + `poiType=meal`；饭店/酒店须具体店名。 |
| 2 | **无坐标景点** | 先尝试匹配已有库；未匹配则 pending 入库待补坐标（高德见下章），不再「无坐标直接跳过」。 |
| 3 | **模板路线无 poiType** | `inferPoiCategory` 按名称关键字回退推断。 |

---

## 高德地理编码补坐标

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A2 增强 / B2 前置 |
| **关联代码** | `config/amap.ts`、`services/amap-geocode.service.ts`；[amap-geocoding.md](./amap-geocoding.md) |

### 思考过程

- LLM 常漏坐标，不能把无坐标景点硬入库。  
- 高德有 **place/text**（按名称搜 POI）和 **geocode/geo**（按地址），景点名应优先前者。  
- Key 未配置时应静默降级，不阻塞路线生成。

### 实现原理

`enrichSpotCoordinates` 在 `sync` 前调用：无坐标则请求高德 REST；`location` 为「经度,纬度」解析为 GCJ-02 写入表。链路：`place/text` 失败 → `geocode/geo` 回退。未配置 `AMAP_WEB_KEY` 时跳过。

### 最终方案

同步 POI 时自动补坐标；环境变量控制开关；坐标系统一用高德 GCJ-02，便于国内地图与后续围栏。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **双接口回退** | 名称搜 POI 优先，地址 geocode 兜底。 |
| 2 | **可插拔** | 无 Key 时与改造前行为一致。 |
| 3 | **接入点单一** | 只挂在 `attraction-sync`，业务无感。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **Key 类型** | 必须用 Web 服务 Key，非 JS/小程序 Key。 |
| 2 | **经纬度顺序** | 高德返回 `lng,lat`，解析时勿颠倒。 |
| 3 | **配额** | 同景点重复生成会多次调用 → 下一章 Redis 缓存。 |

---

## Redis 地理编码缓存

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | G1 前置 / A2 增强 |
| **关联代码** | `config/redis.ts`、`geocode-cache.service.ts`、`redis-client.service.ts`；`docker-compose.yml` → `redis` |

### 思考过程

- 同一「城市 + 景点名」在多次路线生成中会重复打高德，浪费日配额。  
- Redis 在路线图 G1 已规划，可先只缓存 geocode，不引入全站缓存复杂度。  
- Redis 宕机不能影响主流程，应降级为直调高德。

### 实现原理

`resolveCoordinatesFromAmap` 先 `GET douxing:amap:geocode:{city}:{name}`（名称归一化），命中直接返回；未命中调高德，成功则 `SET EX`（默认 TTL 30 天）。`ioredis` 懒连接，失败置 `disabled` 标志不再重试连接。

### 最终方案

配置 `REDIS_URL` 即启用；`docker compose up -d redis`；与高德 Key 配合使用。开发环境缓存命中打日志 `[amap] 缓存命中`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **按 city+name 去重** | 键简单、命中率高、与业务语义一致。 |
| 2 | **失败自动降级** | 无 Redis 时仍可调高德。 |
| 3 | **Docker 一键起** | compose 已含 `redis` 服务与数据卷。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **ESM 引 ioredis** | 使用 `import { Redis } from 'ioredis'`，避免 default 构造报错。 |
| 2 | **只缓存成功结果** | 失败不写入，避免错误坐标长期生效。 |
| 3 | **多实例共享** | 生产多 Pod 共用同一 Redis 才能真正省配额。 |

---

## 数据库生成与使用命令（Drizzle）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | 横切基础设施（M0 起积累，A2/A2+ 迁移实践沉淀） |
| **关联代码** | `packages/server/drizzle/`、`src/db/migrate.ts`、`seed.ts`、`drizzle.config.ts`；根脚本 `db:*` |

### 背景与目标

Monorepo 多人协作需要可版本化的表结构变更；新人 clone 后一条命令能起 MySQL + 表 + 演示数据。

### 思考过程

1. **为什么不用手工改表？** 无法 Code Review、环境不一致；Drizzle schema 即文档，SQL 迁移可审计。  
2. **`generate` 和 `migrate` 为何常混淆？** kit 只 diff 产出文件；真正执行 DDL 的是 `migrate.ts` + `__drizzle_migrations`。必须在文档里画清流水线。  
3. **Docker 与脚本竞态** | `bootstrap` 起容器后宿主机端口未必立刻可用；`waitForDatabase` 轮询后再 `db:setup`。  
4. **seed 边界** | seed 只做 DML 且幂等 skip，不能代替 migrate；否则新人以为「跑 seed 就能加列」。

### 最终方案

- 三层：schema TS → `pnpm db:generate` → `pnpm db:migrate`；数据：`pnpm db:seed`。  
- 根目录转发 `db:migrate` / `db:seed` / `db:setup` / `db:reset`。  
- `bootstrap:dev` = Docker + wait + `db:setup` + `pnpm dev`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **Monorepo 根命令转发** | 根目录即可 `pnpm db:migrate`，不必记住 server 包路径。 |
| 2 | **自动建库 + 等待** | `ensureDatabase` 创建库并等待 TCP 就绪，降低 Docker 新人失败率。 |
| 3 | **单一 `DATABASE_URL`** | drizzle-kit、migrate、seed、运行时共用 `.env`，减少配错连接串。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **generate ≠ migrate** | 改 schema 后必须 generate + 人工审 SQL + migrate；漏 migrate 表现为「表不存在」。 |
| 2 | **journal 不可乱改** | 已执行 tag 记在 `__drizzle_migrations`；调整历史顺序会导致环境漂移。 |
| 3 | **seed 不改表结构** | 新列/新表只能走迁移；旧业务数据不会因 seed 自动补新字段（如 `attractionId`）。 |

---

## A3-1 AI 路线重新生成（prompt + 景点同步）

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A3 路线能力补全（增强） |
| **关联代码** | `routes/routes.ts`（`POST /:id/regenerate`）、`services/route.service.ts`；`utils/route-detail.util.ts`；`pages/routes/detail.vue`；`packages/shared` 类型 `sourcePrompt` / `RegenerateRouteRequest` |

### 背景与目标

用户对 AI 首次生成不满意时，应能**改 prompt 覆盖草稿**而非只能新建路线；重新生成后新 POI 须与首次生成一样写入 `attractions`（pending/合并），并写回 `attractionId`。

### 思考过程

1. **新建还是原地更新？** 原地更新同一条草稿可保留订单/收藏关联（若有），且列表不膨胀；仅 `DRAFT` 可覆盖，已发布路线拒绝 regenerate 并提示去规划页新建。  
2. **旧 `attractionId` 要不要保留？** 行程节点已变，保留旧 ID 会指向错误 POI；重新生成时**剥离节点上的 `attractionId`**，强制走完整 `syncAttractionsFromRouteDetail`。  
3. **同步与创建是否同链路？** 抽 `syncDraftToAttractionLibrary`，创建与 regenerate 共用；避免 regenerate 漏调 sync 或传入被模板常量污染的 `routeDetail`。  
4. **无坐标就跳过是否合理？** 原逻辑在匹配前就要求坐标，导致「库里有、但补坐标失败」的景点也不关联；改为**先匹配、再决定是否新建 pending**（无坐标可 pending）。

### 实现原理

`regenerateRouteFromPrompt`：`generateRoute` → `normalizeRouteDetailDays` + 深拷贝 → `sync` → 更新 `travel_routes` 与 `route_detail`（含 `sourcePrompt`、`llmProviderChoice`）。`attraction-sync` 调整顺序：高德补坐标 → `resolveAttractionMatch` → 合并或 `insertPendingAttraction`。

### 最终方案

- API：`POST /api/routes/:id/regenerate`，body 同 `/generate`（`prompt`、可选 `provider`）。  
- 详情页草稿展示「修改需求重新生成」；`route_detail.sourcePrompt` 记录原始需求。  
- 重新生成后 `isUnlocked` 重置为 `false`（内容已变）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **草稿原地迭代** | 同一路线 ID 反复调 prompt，适合 MVP 快速试错。 |
| 2 | **同步链路复用** | 创建/regenerate 共用 `syncDraftToAttractionLibrary`，景点库行为一致。 |
| 3 | **需求可追溯** | `sourcePrompt` 写入 `route_detail`，详情页可预填上次描述。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **重新生成不入库** | 根因是同步前无坐标即 return；改为先匹配、未匹配再 pending；regenerate 剥离旧 `attractionId`。 |
| 2 | **模板对象被污染** | `ROUTE_TEMPLATES` 为模块级常量；LLM/模板返回前深拷贝 `days`，避免多次生成共享引用。 |
| 3 | **已发布不可覆盖** | `status !== DRAFT` 抛错，防止已分享路线被静默替换；需改需求则复制 prompt 到规划页新建。 |

---

## A3-2 移动端 AI 规划全局 Loading 与中断

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-20 |
| **路线图** | A3 / C 阶段 AI 体验前置 |
| **关联代码** | `utils/ai-plan-loading.ts`、`utils/request.ts`（`requestAiPlan`）；`components/ai-plan-blocking-overlay`；`DouxingTabBar.vue`；`pages/plan/plan.vue`、`pages/routes/detail.vue` |

### 背景与目标

LLM 生成路线耗时可达数十秒，期间用户若切换 Tab 或点其他按钮会离开流程、产生重复请求。需要**全屏阻塞 + 可取消**，与后端 abort 对齐。

### 思考过程

1. **用 `uni.showLoading` 够不够？** 原生 loading 无自定义「取消」、难拦截 Tab；自定义全屏遮罩可统一文案与按钮。  
2. **遮罩放哪？** 小程序 `App.vue` 模板不盖页面；挂在**各 Tab 页的 `DouxingTabBar`** + 非 Tab 的 `detail.vue`，保证 z-index 高于 TabBar（100000）。  
3. **如何真正中断？** `uni.request` 返回 `RequestTask`，`abort()` 可取消；封装 `requestAiPlan` 注册 task，取消时 reject 为 `已取消路线规划`，业务侧不 toast 失败。  
4. **如何防跳转？** `uni.addInterceptor` 拦截 `switchTab`/`navigateTo`/`navigateBack` 等；TabBar `onSwitch` 再判 `isAiPlanLoading()` 双保险。

### 实现原理

`beginAiPlanLoading` 置 `active` 并安装导航拦截；`requestAiPlan` 在 `finally` 中 `endAiPlanLoading`；用户点「取消规划」→ `cancelAiPlanLoading` → `task.abort()`。仅 `generateRoute` / `regenerateRoute` 走该封装，其它 API 不受影响。

### 最终方案

- 全屏遮罩：转圈 + 文案 +「取消规划」。  
- 生成/重新生成期间禁用 Tab、模型选择、重复提交。  
- 取消后停留当前页，不跳转详情。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **可中断的长请求** | 用户不必杀进程或关小程序，体验接近原生「取消」。 |
| 2 | **导航双保险** | 拦截器 + TabBar 判断，降低误触切页概率。 |
| 3 | **AI 请求隔离** | `requestAiPlan` 与普通 `request` 分离，避免全站请求都带遮罩。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **全局 UI 在 UniApp 的挂载点** | 不能依赖 App 根模板；随 TabBar/详情页组件挂载遮罩，覆盖当前栈顶页。 |
| 2 | **abort 错误文案不一** | 各端 `errMsg` 含 `abort`/`cancel` 不一；`isRequestAbortedError` 统一识别，映射为固定中文提示。 |
| 3 | **取消与 complete 竞态** | `abort` 后仍会走 `fail`/`complete`；`finally` 里 `endAiPlanLoading` 幂等，避免遮罩残留。 |

---

## A3 路线能力补全

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-21 |
| **路线图** | A3 |
| **关联代码** | 迁移 `0007_route_engagement.sql`；`route-interaction.service.ts`、`route.service.ts`；`routes.ts`；移动端 `routes/list.vue`、`routes/detail.vue` |

### 思考过程

1. **计数存列还是实时聚合？** 热门榜需按浏览/点赞排序，若每次 `COUNT(*)` 关联表会在列表接口放大查询成本。折中：`travel_routes` 冗余 `view_count` / `like_count` / `collect_count`，用户关系表 `route_likes`、`route_favorites` 保证唯一，切换时同步增减计数。  
2. **点赞与收藏是否合并一张表？** 语义与后续通知不同，拆表更清晰；接口分别为 `POST .../like` 与 `.../favorite`，移动端独立按钮。  
3. **草稿编辑边界？** 仅 `status=DRAFT` 可 `PUT` 更新名称与简介（MVP 先覆盖高频字段），已发布路线拒绝静默改写，与 regenerate 规则一致。  
4. **列表 scope 设计？** 用 `scope=mine|hot|favorites` 统一 `GET /routes`，另提供 `GET /routes/hot` 便于首页直达；热门仅返回已发布路线。

### 实现原理

详情 `GET` 时 `view_count + 1`；点赞/收藏 toggle 先查关系表再更新计数；列表按 scope 分支查询，对当前用户批量补齐 `isLiked` / `isFavorited`；热门排序默认 `like_count * 2 + view_count`。

### 最终方案

- 迁移新增统计列与两张关系表；`toRouteInfo` 抽到 `route-info.util.ts` 避免循环依赖。  
- API：`GET /routes`（筛选/排序）、`GET /routes/hot`、`PUT /routes/:id`、`POST .../like`、`POST .../favorite`。  
- 移动端路线 Tab：我的/热门/收藏 + 草稿状态筛选；详情页展示统计、互动与草稿编辑。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **列表 scope 统一入口** | 一套 `GET /routes` 查询参数覆盖我的/热门/收藏，减少客户端分支接口。 |
| 2 | **关系表 + 冗余计数** | 保证用户级唯一点赞/收藏，同时支持热门排序无需重聚合。 |
| 3 | **详情自动计浏览** | 打开详情即 +1，管理端与移动端统计口径一致。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **服务层循环依赖** | `route.service` 与互动逻辑互相引用；抽出 `route-info.util.ts` 单向依赖。 |
| 2 | **Express 路由顺序** | `/hot` 必须在 `/:id` 之前注册，避免被当成 id。 |
| 3 | **旧库迁移** | 新列默认 0；已有路线需执行 `pnpm --filter @douxing/server db:migrate` 后接口才返回统计字段。 |

---

## A4 订单状态机

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-21 |
| **路线图** | A4 |
| **关联代码** | `packages/shared/src/order-state.ts`；`order-transition.service.ts`、`order.service.ts`、`jobs/order-timeout.job.ts`；`routes/orders.ts` |

### 背景与目标

MVP 仅有模拟支付，缺少明确的状态流转与待支付超时关闭，易出现重复支付、僵尸待支付单。A4 要求 `pending→paid→completed/cancelled` 与超时取消。

### 思考过程

1. **状态表放 shared 还是仅 server？** 前后端都要展示状态文案，流转规则也应单一来源；`canTransitionOrderStatus` 放 `@douxing/shared`，服务端 CAS 更新放 server。  
2. **支付后是否停在 paid？** 路线解锁属数字交付，支付成功即可完结；`shouldAutoCompleteAfterPay` 对 `route` 类型在解锁后 `paid→completed`，管理端与用户列表口径一致。  
3. **防重复支付怎么做？** 仅靠读后再写会有竞态；`UPDATE ... WHERE status = expectedFrom` 乐观锁，失败返回「状态已变更」；已 paid/completed 直接拒绝。  
4. **超时用什么基础设施？** 首版无 MQ，进程内 `setInterval` 扫描 + `.env` 可配分钟数，与短信验证码 Map 策略一致，G 阶段再迁队列。

### 实现原理

`applyOrderStatusTransition` 校验合法边后按当前 status 条件更新；`payOrder` 走 `pending→paid`，解锁路线后可选 `paid→completed`；`cancelOrder` 与定时任务均走 `pending→cancelled`；创建订单时复用同用户同路线的待支付单，避免重复下单。

### 最终方案

- 共享：`order-state.ts`（流转表、文案、是否自动完结）。  
- API：`POST /orders/:id/cancel`；支付/取消均经状态机。  
- 后台：`startOrderTimeoutJob` 随服务启动，默认 30 分钟超时、60 秒扫描。  
- 环境变量：`ORDER_PENDING_TIMEOUT_MINUTES`、`ORDER_TIMEOUT_SCAN_INTERVAL_SEC`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **流转规则单源** | shared 定义合法边，服务端与后续支付网关共用，避免魔法数字散落。 |
| 2 | **CAS 防双付** | 条件更新 + 读回校验，并发双击支付仅一笔成功。 |
| 3 | **待支付单复用** | 同路线已有 pending 则返回原单，配合超时取消减少订单膨胀。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **支付与解锁的原子性** | 先 CAS 到 paid 再解锁；解锁失败需人工/补偿（MVP 记录日志，E 阶段接真实支付回调）。 |
| 2 | **多实例定时任务** | 多 Pod 会重复扫描，靠 CAS 取消幂等；生产建议 G 阶段单消费者 或分布式锁。 |
| 3 | **已支付与已完成并存** | 数字商品支付后自动 completed；若仅展示「已支付」会误导，统一用 `getOrderStatusLabel`。 |

---

## B1 打卡字段增强

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-21 |
| **路线图** | B1 |
| **关联代码** | 迁移 `0009_checkin_fields.sql`；`schema/check-ins.ts`、`services/checkin.service.ts`；`routes/checkins.ts`（照片上传）；`@douxing/shared` 积分常量 |

### 背景与目标

MVP 打卡仅有经纬度与 remark，无法支撑游戏化（积分、城市维度、照片激励）与后续排行榜。B1 在不大改流程的前提下扩展字段与计分规则。

### 思考过程

1. **城市从哪来？** 优先 `attractionId` 反查 `city_code`；无景点时用客户端 `cityCode` 或城市名匹配 `attractions` 表；避免用户手填不规范编码。  
2. **积分规则要不要可配置？** 首版写死在 `@douxing/shared`（基础分 + 照片加成 + 首打卡加成），便于前后端一致；配置表留到 B5 成就体系再抽象。  
3. **照片存 URL 还是 BLOB？** 与头像策略一致：multipart 上传至 `uploads/checkins`，库内 JSON 数组存 URL，上限 `CHECKIN_MAX_PHOTOS`。  
4. **打卡后还要触发什么？** 插入成功后同步调用成就/徽章评估（B4/B5），在响应里返回 `newAchievements` / `newBadges` 供移动端 toast。

### 实现原理

`createCheckIn`：校验 GPS → 围栏（B2）→ 解析 `cityCode` → 算分 → 写入 `check_ins`（含 `photos`、`points_earned`、`gps_accuracy`）→ `evaluateAchievements` + `evaluateBadges`。列表接口批量 `cityCode → cityName` 映射提升展示可读性。

### 最终方案

- 新字段：`city_code`、`photos`、`points_earned`、`gps_accuracy`。  
- API：`POST /checkins` 支持 `photos` URL 数组；`POST /checkins/upload-photo` 上传单张。  
- 积分：基础 10 + 有图 +5 + 景点首打 +10（常量见 shared）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **城市解析多级回退** | 景点 ID → cityCode → 城市名查库，降低客户端负担。 |
| 2 | **一次打卡联动游戏化** | 响应携带新解锁成就/徽章，移动端可即时反馈。 |
| 3 | **积分规则 shared 单源** | 前后端与排行榜聚合共用同一套常量。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **无 attractionId 的打卡** | 必须提供 cityCode 或可解析 cityName；否则拒绝并提示，避免脏数据进排行榜。 |
| 2 | **照片 URL 与域名** | 真机访问需 `API_PUBLIC_BASE_URL`；与 A1 头像同一套配置说明。 |
| 3 | **旧打卡无新字段** | 迁移默认值 + 列表接口对 null 友好处理（photos 空数组、积分为 0）。 |

---

## B2 地理围栏打卡

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-21 |
| **路线图** | B2 |
| **关联代码** | `services/checkin-geofence.service.ts`、`config/checkin.ts`；迁移 `0010_checkin_geofence.sql`；`utils/checkin-errors.ts`；`packages/shared` 地理常量 |

### 背景与目标

设计文档 §4.3.3 要求 GPS 围栏与简单防作弊。需在「可演示」与「可调试」之间平衡：生产可严格校验，开发可一键关闭。

### 思考过程

1. **围栏半径取多少？** 500m 为 shared 常量 `CHECKIN_GEOFENCE_RADIUS_M`，兼顾 GPS 误差与防远程刷打卡。  
2. **无坐标的 pending 景点怎么办？** 围栏依赖目标点；景点无 lat/lng 时直接拒绝并提示补坐标（依赖 A2+ 高德链路）。  
3. **如何防刷？** 三层：GPS 精度上限、Haversine 距离、相邻两次打卡隐含速度上限（`CHECKIN_MAX_SPEED_KMH`）。  
4. **本地调试总失败？** `CHECKIN_GEOFENCE_ENABLED=false` 跳过全部围栏逻辑，启动日志与 `/api/health` 可观测当前模式。

### 实现原理

`validateCheckInGeofence`：开关关闭则返回 null；否则校验精度 → 解析目标点（景点或 POI 坐标）→ Haversine 距离 ≤500m → 与上次打卡比速度。`assertNotCheckedInToday` 限制同一景点每日一次。错误统一抛 `CheckinValidationError`，路由层映射为 400 + 中文文案。

### 最终方案

- 环境变量：`CHECKIN_GEOFENCE_ENABLED`（默认 true）。  
- 打卡响应可选带回 `distanceMeters` 便于 UI 展示「距景点 xx 米」。  
- 目标点：优先 `attractionId` 关联景点坐标，其次客户端传 `targetLatitude/Longitude`（路线节点 POI）。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **可关闭的严格模式** | 一条 env 切换演示/生产，无需改代码分支。 |
| 2 | **多层防作弊 MVP** | 精度 + 距离 + 速度，不依赖第三方风控 SDK。 |
| 3 | **错误语义化** | 距离、精度、重复打卡等分文案，方便用户自助修正。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **坐标系** | 国内地图与高德为 GCJ-02；客户端 `uni.getLocation` 与库存坐标需同一参考系。 |
| 2 | **室内 GPS 漂移** | 精度差时拒绝并提示到开阔处；开发环境可关围栏联调业务逻辑。 |
| 3 | **速度误判** | 短间隔内位移不校验速度；间隔与阈值放 shared 便于调参。 |

---

## B3 打卡地图 UI

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-21 |
| **路线图** | B3 |
| **关联代码** | `packages/mobile/src/pages/checkins/map.vue`、`utils/checkin-map.ts`；`pages/checkins/list.vue` 入口 |

### 背景与目标

打卡列表无法直观呈现「足迹」。需在 UniApp 地图组件上展示历史打卡点、时间筛选与轨迹连线，作为阶段 B 的用户可见成果。

### 思考过程

1. **用原生 map 还是第三方 SDK？** 微信小程序/H5/App 均内置 `<map>`，MVP 不引入额外地图 SDK 降低包体与密钥管理成本。  
2. **无坐标记录怎么处理？** 早期打卡或非 attraction 节点可能缺坐标；地图层过滤并在工具栏提示「N 个无坐标未显示」。  
3. **时间筛选粒度？** 全部 / 近 7 天 / 近 30 天 / 本年，客户端 filter 后重算 markers 与 polyline。  
4. **照片怎么在地图上露出来？** 微信小程序用 `cover-view` + `callout` 展示 marker 缩略图；其它端降级为点击 marker 看详情卡片。

### 实现原理

`checkin-map.ts` 将 `CheckInInfo[]` 转为 markers、includePoints、polyline；map 中心与 scale 随筛选结果自适应。选中 marker 展示底部详情卡（城市、积分、照片预览）。

### 最终方案

- 页面：`pages/checkins/map`（从打卡列表进入）。  
- 能力：时间筛选、足迹连线、当前位置、marker 点击详情、照片 preview。  
- 无数据时空状态引导回列表打卡。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **跨端一套 Vue 页面** | H5/小程序/App 共用逻辑，仅 callout 用条件编译。 |
| 2 | **筛选与地图联动** | 改时间 chip 即重绘 markers + 折线 + 统计积分。 |
| 3 | **诚实展示缺口** | 明示隐藏的无坐标点数，推动 A2+ 补全坐标。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **marker id 唯一** | 使用 checkIn.id 作为 markerId，避免重复渲染异常。 |
| 2 | **小程序 cover-view 限制** | 照片 callout 仅 MP-WEIXIN 编译块；H5 用详情卡看图。 |
| 3 | **大量点性能** | MVP 全量渲染；点数上百时再考虑聚合或分页（文档留扩展点）。 |

---

## B4 徽章系统

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | B4 |
| **关联代码** | 迁移 `0011_badges.sql`；`data/badge-seeds.ts`、`services/badge.service.ts`、`services/checkin-stats.service.ts`；`routes/badges.ts`；`pages/badges/index.vue` |

### 背景与目标

设计文档 §4.3.4 徽章激励。需要可扩展的定义表 + 用户解锁关系，并在打卡后自动评估，移动端图鉴展示进度。

### 思考过程

1. **徽章与成就是否合并？** 徽章偏展示与收集（带 rarity/category），成就偏里程碑叙事；拆表 `badges` / `user_badges` 与 `achievement_definitions` 分开，避免一条记录承担两种 UI。  
2. **条件怎么表达？** `condition_type` + JSON `condition_value`（如 `{ cityCode, minCount }`），与成就共用模式，便于 seed 配置。  
3. **何时计算？** 打卡成功后 `gatherUserCheckinStats` 一次聚合，再遍历 seed 评估，避免每种条件单独扫表。  
4. **已解锁如何幂等？** `unlockIfMissing` 查重后再 insert，重复打卡不会 duplicate key。

### 实现原理

`evaluateBadges(userId)`：stats → 对每个 badge 算 `computeProgress` → 达标则写 `user_badges` 并返回新解锁列表。图鉴 API 合并定义 + 用户解锁态 + 进度条数据。

### 最终方案

- 11 种徽章 seed（城市、累计打卡、多城、照片、单路线等）。  
- API：`GET /badges`（图鉴）、`GET /badges/mine`（已解锁）。  
- 移动端「我的」入口进 badges 页，支持分类/解锁状态筛选。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **stats 聚合复用** | `checkin-stats.service` 供 B4/B5/B6 共用，一次查询多模块使用。 |
| 2 | **进度可展示** | 未解锁也返回 current/target，图鉴可显示「3/10」式进度。 |
| 3 | **seed 驱动扩展** | 新增徽章主要改 seed + 枚举，不必改评估主流程。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **条件类型扩展** | `BadgeConditionType` 枚举 + switch；新类型需同时改 compute 与 seed。 |
| 2 | **城市徽章与 cityCode** | 依赖 B1 正确写入 city_code；否则 CITY_CHECKIN 类永不触发。 |
| 3 | **评估性能** | 用户打卡量大时 stats 多表聚合；MVP 可接受，后续可 Redis 缓存 stats 快照。 |

---

## B5 成就体系完善

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | B5 |
| **关联代码** | 迁移 `0012_achievement_definitions.sql`；`data/achievement-seeds.ts`、`services/achievement.service.ts`；`routes/achievements.ts`；`pages/achievements/index.vue` |

### 背景与目标

MVP 仅 3 条硬编码成就。B5 引入 `achievement_definitions` 配置表、8 种条件类型，与徽章并行但侧重「里程碑」叙事与图鉴 completion 率。

### 思考过程

1. **旧 `achievements` 表怎么办？** 保留用户解锁记录表；定义迁到 `achievement_definitions`，`achievement_type` 与 `achievement_code` 对齐 seed。  
2. **比徽章多哪些条件？** 增加 `CONSECUTIVE_DAYS`（连续打卡天）、`DAILY_CHECKINS`（单日多次），stats 服务里算 streak 与 max daily。  
3. **兼容旧 API？** `GET /achievements` 仍返回已解锁列表；新增 `/catalog`、`/mine` 支撑图鉴页。  
4. **解锁描述从哪来？** 展示以 definition 为准，unlock 行只存 code 与时间，避免文案双份不一致。

### 实现原理

与 B4 对称：`gatherUserCheckinStats` → `computeAchievementProgress` → `unlockIfMissing` → 写 `achievements` 表。catalog 接口 left join 用户解锁态，输出 `unlocked` + `progress`。

### 最终方案

- 8 条 achievement seed，分类 explore/social/streak 等。  
- 打卡响应与徽章一并返回 `newAchievements`。  
- 移动端 achievements 页：总进度、筛选、进度条。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **定义与解锁分离** | 改文案/阈值只动 definitions + seed，不动用户历史。 |
| 2 | **连续天算法** | 对 DATE 集合排序算最长 streak，支持「坚持打卡」类成就。 |
| 3 | **与徽章互补** | 同一次 stats 评估双轨输出，用户一次打卡可能同时解锁徽章+成就。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **时区与 DATE()** | MySQL `DATE(checked_at)` 依赖 DB 时区；与 Docker MySQL `+08:00` 配置一致。 |
| 2 | **迁移 0012 与旧数据** | 旧解锁行保留；新 definition 通过 code 关联，需 seed 与旧 type 字符串一致。 |
| 3 | **catalog 数据量** | 成就数少可全量；上百条时需分页（当前未做，文档标注 MVP 范围）。 |

---

## B6 排行榜

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | B6 |
| **关联代码** | `services/leaderboard.service.ts`、`routes/leaderboard.ts`；`pages/leaderboard/index.vue`；`@douxing/shared` LeaderboardPeriod/Metric |

### 背景与目标

设计文档 §4.2 社交激励中的排行榜。提供周榜/月榜，按打卡次数或积分排序，并展示当前用户名次。

### 思考过程

1. **实时榜还是周期榜？** 首版做自然周/自然月聚合，SQL `GROUP BY user_id`，不做 Redis ZSET，与当前仅 MySQL 的基础设施一致。  
2. **排序指标？** `checkins` 次数与 `points` 求和二选一，接口参数 `metric`；同分再比打卡数。  
3. **是否只统计已通过打卡？** 是，过滤 `CheckInStatus.APPROVED` 与活跃用户，避免测试数据污染。  
4. **当前用户不在 Top N？** 全量聚合后在内存算 rank，返回 `myRank` / `myValue`；未上榜则 rank null、value 0。

### 实现原理

`resolvePeriodRange` 算本周一 0 点或本月 1 号；SQL aggregate → 排序 → slice limit → 补用户 nickname/avatar。周期与 metric 枚举放 shared，移动端 chip 切换触发重新请求。

### 最终方案

- API：`GET /leaderboard?period=week|month&metric=checkins|points&limit=`  
- 移动端独立排行榜页（profile 入口）。  
- 默认 limit 20，上限 shared 常量控制。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **周期边界清晰** | 周一为周起点，与中文产品习惯一致。 |
| 2 | **我的排名始终可见** | 不在 Top N 也返回 myRank，减少挫败感。 |
| 3 | **双指标一套聚合** | 同一次 SQL 结果按 metric 选排序字段，避免 duplicate 查询。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **全量聚合再 slice** | 用户量大时需 SQL LIMIT 或物化视图；MVP 用户少可接受。 |
| 2 | **服务器时区** | period 起止用 JS Date 构造，需与 MySQL 存储时区对齐。 |
| 3 | **积分与次数口径** | 积分来自 B1 计分规则变更会影响历史榜；文档说明「规则变更不追溯重算」。 |

---

## C1 多轮对话规划

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | C1 |
| **关联代码** | 迁移 `0013_plan_sessions.sql`；`schema/plan-sessions.ts`、`services/plan-session.service.ts`、`routes/plan-sessions.ts`；`llm-client.service.ts`（history）；`pages/plan/plan.vue`（对话 UI） |

### 背景与目标

设计文档 §3.3 规划多 Agent 协作；MVP 阶段先落地**可追问、可改方案**的多轮会话，不引入 LangChain 与独立 Python 服务。

### 思考过程

1. **扩展现有 `/generate` 还是新会话 API？** 单轮接口保留兼容；新增 `plan-sessions` 专门承载会话与消息，避免每次 generate 都猜是否在续聊。  
2. **history 怎么传给 LLM？** 用户消息原样保留；助手侧存 `route_snapshot` JSON，续聊时序列化为 assistant 内容，并在 system prompt 增加「结合历史输出完整 JSON」提示。  
3. **追问是新建路线还是改草稿？** 同一会话绑定一条 `route_id`，追问走 `regenerateRouteFromPrompt` + history，避免列表里一次对话产生多条草稿。  
4. **规划页形态？** 由单次 textarea 改为聊天气泡；首条创建会话，后续 `POST .../messages`；保留模型选择与 `requestAiPlan` 遮罩。

### 实现原理

`createPlanSession`：generate → 写 `travel_routes` → 插入 session + 首对 user/assistant 消息（assistant 带 snapshot）。`appendPlanSessionMessage`：加载历史 → `buildHistoryForLlm` → regenerate 同一路线 → 追加消息。LLM 侧 `chatCompletionWithProvider` 在 system 后插入 history，再发当前 user 内容。

### 最终方案

- 表：`plan_sessions`、`plan_session_messages`（`route_snapshot` JSON）。  
- API：`POST/GET /api/routes/plan-sessions`、`POST .../:id/messages`。  
- 移动端规划 Tab 对话式 UI；`POST /routes/generate` 仍可用于单轮。  
- 迁移文件：`0013_plan_sessions.sql`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **会话与路线一对一** | 追问更新同一条草稿，列表不膨胀，和「改 prompt 覆盖」心智一致。 |
| 2 | **snapshot 可续聊** | 助手消息存完整路线 JSON，LLM 能在上下文里做「第三天改西湖」类修改。 |
| 3 | **单轮/多轮并存** | 老接口与模板降级逻辑不变，逐步迁移到规划页对话流。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **迁移记录与表不一致** | `__drizzle_migrations` 有 tag 但 SQL 未执行会导致「表不存在」；删除错误 hash 后重跑 `db:migrate`，并强调 generate≠migrate。 |
| 2 | **history token 膨胀** | 每轮 assistant 带整段 JSON，轮次多时会超长；C1 先可用，后续 C2/C4 可摘要或只传 diff 字段。 |
| 3 | **模板降级与多轮** | 无 LLM 时把历史 user 句拼接进 prompt 做模板匹配，保证离线演示仍可追问（精度有限）。 |

---

## C1+ 路线解锁支付开关

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | 阶段 C 体验增强 / §5.4 前置 |
| **关联代码** | `config/route-unlock.ts`；`route.service.ts`、`route-info.util.ts`、`order.service.ts`；`routes/orders.ts`（payment-config）；`.env.example` |

### 背景与目标

开发演示希望「生成即可看完整行程」；商业化阶段再通过环境变量打开解锁付费，且须服务端强制生效，防止仅隐藏前端按钮。

### 思考过程

1. **开发期要不要每次模拟支付？** 会打断「生成 → 看行程」演示链路；产品希望默认**直接可见**完整 `route_detail`。  
2. **开关放哪？** 环境变量 `ROUTE_UNLOCK_PAYMENT_REQUIRED`，默认 `false`；上线商业化再设 `true`，无需改代码。  
3. **只改前端行不行？** 不行；须服务端 `toRouteInfo` 对创建者强制 `isUnlocked`，并拒绝创建解锁订单，防绕过。

### 实现原理

`isRouteUnlockPaymentRequired()` 为 false 时：新建路线 `isUnlocked: true`；详情接口对 owner 覆盖 unlocked；`createRouteUnlockOrder` 直接返回「无需支付」。`GET /orders/payment-config` 下发 `routeUnlockPaymentRequired` 供移动端隐藏解锁卡片。

### 最终方案

- `.env`：`ROUTE_UNLOCK_PAYMENT_REQUIRED=false`（默认）。  
- `true` 时恢复原有解锁卡片 + 订单 + 模拟/微信支付流程。  
- 服务启动日志打印 `[route-unlock]` 当前模式。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **一套代码两种商业模式** | 演示/内测免支付，正式环境一行配置打开变现。 |
| 2 | **服务端权威** | 前端隐藏按钮不够，详情 API 与订单创建双侧校验。 |
| 3 | **与 payment-config 统一** | 移动端一次拉配置即可同步解锁 UI 与支付能力。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **库内旧数据 isUnlocked=false** | 靠 `toRouteInfo` 运行时覆盖，无需批量迁移历史路线。 |
| 2 | **重新生成后解锁态** | 免支付模式下 `buildRouteDetailFromDraft` 直接 `isUnlocked: true`；付费模式下仍重置为 false。 |
| 3 | **与 E2 真支付衔接** | 开关仅控制「要不要解锁」；`PAYMENT_MODE` 仍管 mock/wechat，两维配置正交。 |

---

## C2 意图结构化解析

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | C2 |
| **关联代码** | `services/travel-intent.service.ts`；`route-generator.service.ts`、`llm-client.service.ts`；`plan-session.service.ts`；迁移 `0014_plan_session_intent.sql`；`pages/plan/plan.vue`（约束条） |

### 思考过程

1. **要不要上独立 NLP 模型？** 设计文档提 BERT/BiLSTM，但当前阶段用规则 + 关键词即可覆盖 MVP 验收（城市/天数/预算/主题），且 LLM/模板降级路径都能复用。
2. **意图存哪？** 会话级 `plan_sessions.intent_snapshot` 持久化；生成链路通过 `GenerateRouteInput.intent` 传递，避免每轮从 history 重算不一致。
3. **只解析不约束不够** | 验收要求是「生成结果符合约束」：LLM prompt 注入约束块 + `enforceRouteConstraints` 对天数/城市/预算/标签做后处理校正。
4. **追问怎么合并？** 首条走全量 `parseTravelIntent`；后续识别「改成 5 天」「预算降到 3000」等修改句，在已有 intent 上增量合并。

### 实现原理

用户消息 → `buildIntentFromHistory` 抽取/合并 → 写入 session → `generateRoute` 带 intent 调 LLM 或模板 → `formatIntentConstraintsForLlm` 写入 user 内容 → 返回 JSON → `enforceRouteConstraints` 校正 days/city/budget/tags → 助手回复附带「已理解需求：…」摘要。

### 最终方案

- 新增 `TravelIntentSnapshot` 类型与 `travel-intent.service.ts`（解析、合并、LLM 约束文案、后处理）。
- `plan_sessions.intent_snapshot` JSON 字段；API 响应与详情带 `intentSnapshot`。
- 规划页展示「已理解需求」约束条；助手消息含解析摘要。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **双路径一致** | LLM 与模板降级共用同一套 intent 解析与 enforce，离线演示也遵守天数/城市。 |
| 2 | **多轮增量合并** | 支持「改成 5 天」「换成上海」类修改句，不必每轮重复完整需求。 |
| 3 | **约束可视化** | 移动端约束条 + 助手摘要，用户可确认系统理解是否正确。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **中文天数表达多样** | 同时支持「3天」「三日游」「周末=2天」及阿拉伯数字修改句。 |
| 2 | **LLM 仍可能越界** | prompt 强约束 + 生成后截断/补全天数、校正 matchedCity 与 budgetRange。 |
| 3 | **追问与首条解析歧义** | 用修改关键词触发 patch 模式，否则按新句全量解析并覆盖同字段。 |

---

## C3 RAG 景点检索

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | C3 |
| **关联代码** | `services/attraction-rag.service.ts`；`route-generator.service.ts`、`llm-client.service.ts`；`plan.vue`（库内景点计数） |

### 思考过程

1. **要不要上向量库？** C3 验收是「景点来自内容库」；当前 `attractions` 仅数十条，MySQL 按 city + 标签/关键词打分足够，后续数据量大再换 Milvus。
2. **RAG 放哪一环？** 检索 → 注入 LLM prompt → 生成后 `applyRagToRouteDraft` 对齐 name/坐标/票价/`attractionId`；模板降级可走 `buildRouteFromRagCatalog` 纯库组装。
3. **LLM 仍乱编怎么办？** system 规则 + 候选 JSON 白名单 + 后处理模糊匹配；`syncAttractionsFromRouteDetail` 仍作兜底。

### 实现原理

`retrieveAttractionsForPlanning` 按 `cityCode` 拉库 → 主题/关键词打分排序 → `formatRagContextForLlm` 写入 user 消息 → LLM/模板生成 → `applyRagToRouteDraft` 绑定库内 POI → 写入 `routeDetail.ragMatchedCount`。

### 最终方案

- 新增 `attraction-rag.service.ts`（检索、格式化、绑定、模板 RAG 组装）。
- `GenerateRouteInput.ragCandidates` 贯穿 LLM 与模板路径。
- 规划页约束条展示「库内景点 N 处」；助手回复同步提示。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **轻量可演进** | 规则打分 + MySQL，无额外基础设施；接口可平滑换向量检索。 |
| 2 | **双路径覆盖** | LLM 注入候选；离线模板可直接从库组装整日行程。 |
| 3 | **可观测** | `ragCandidateCount` / `ragMatchedCount` 写入 routeDetail，便于验收与调试。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **库小城市少** | 仅杭/沪/京/成等有种子；无 city 时跳过 RAG，行为与 C2 一致。 |
| 2 | **LLM 不完全遵命** | 后处理按 name/alias 模糊对齐，尽量补上 `attractionId` 与坐标。 |
| 3 | **餐食节点干扰** | `applyRag` 仅处理 attraction/restaurant/hotel，meal/transport 保持原样。 |

---

## C4 多方案生成

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | C4 |
| **关联代码** | 迁移 `0015_plan_session_candidates.sql`；`config/plan-route-variants.ts`；`plan-session.service.ts`；`POST .../select-candidate`；`pages/plan/plan.vue` 候选卡片 |

### 思考过程

1. **一次 LLM 返回数组还是多次调用？** 多次调用复用现有 `createRouteFromPrompt`，每套带 `variantHint`（经典/文化/休闲等），与 C3 RAG 轮换起点组合，差异更稳定。
2. **未选中的草稿怎么办？** 保留为用户草稿路线，会话 `route_id` 指向当前选中；切换只改关联，不删其他路线。
3. **追问还要多方案吗？** 否；`appendPlanSessionMessage` 只改当前选中路线并 `clearSessionCandidates`，避免组合爆炸。

### 实现原理

首条 `createPlanSession` → 按意图生成 3 个 variant → 各调一次 `createRouteFromPrompt` → 写入 `plan_session_candidates` → 默认选中方案 A → 移动端横向卡片 → `select-candidate` 更新 `is_selected` 与 `session.route_id`。

### 最终方案

- 表 `plan_session_candidates`（sessionId、routeId、label、variantKey、isSelected）。
- API：`POST /plan-sessions/:id/select-candidate`。
- 类型 `PlanRouteCandidate`；规划页候选卡片 UI。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **主题自适应变体** | 亲子/美食/浪漫等意图会替换 B/C 方案风格提示。 |
| 2 | **与 C3 协同** | `ragVariantIndex` 轮换 RAG 候选，模板降级也能产出差异方案。 |
| 3 | **追问单线收敛** | 选定后多轮只维护一条 active 路线，逻辑清晰。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **生成耗时 ×3** | Loading 文案改为「生成多套方案」；变体串行生成，失败则跳过并保证至少 1 套。 |
| 2 | **草稿列表膨胀** | 3 条均入库为草稿；后续可加「仅保留选中」清理策略。 |
| 3 | **LLM 方案趋同** | variantHint + RAG 轮换双保险；仍趋同时可加强 prompt 或提高 temperature（未改）。 |

---

## C5 Python AI 微服务

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | C5 |
| **关联代码** | `packages/ai-service/`（FastAPI + LangChain）；`server/config/ai-service.ts`；`server/services/ai-service-client.service.ts`；`route-generator.service.ts` 三级降级 |

### 思考过程

1. **是否必须独立 Python 服务？** 设计文档 §1.4 规划 Node + Python 双栈；当前 Node 已能直连 DeepSeek/LM Studio。C5 定位为「可选增强」：对齐架构、便于后续 Agent/RAG 扩展，默认关闭不影响现有部署。
2. **Python 与 Node 谁负责 RAG/意图？** Node 仍做 MySQL 检索与意图解析（C2/C3 已稳定）；Python 只接收结构化 payload 调 LangChain，避免双份 DB 依赖。
3. **失败如何降级？** 严格三级：`AI_SERVICE` → Node `chatCompletionForRoute` → 模板/RAG 组装；任一级失败只 warn 不中断用户流程。

### 实现原理

Node `generateRoute` 在 `AI_SERVICE_ENABLED=true` 时 POST `packages/ai-service` 的 `/v1/route/generate`；Python 用 LangChain `ChatOpenAI`（OpenAI 兼容）按与 Node 相同的 system prompt 与约束块生成 JSON；Node 用现有 `llmRouteSchema` 二次校验后走 RAG 对齐与意图校正。

### 最终方案

- 新包 `packages/ai-service`：`GET /health`、`GET /v1/status`、`POST /v1/route/generate`。
- 环境变量：`AI_SERVICE_ENABLED`、`AI_SERVICE_URL`（默认 `http://127.0.0.1:8100`）。
- 启动：`pnpm dev:ai-service`；与 Node 共用根目录 `.env` 中的 DeepSeek/LM Studio 配置。
- `llmProvider=ai-service` 标记来源；`/llm-status` 增加 `aiService` 探测字段。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **可选开关** | 默认 `AI_SERVICE_ENABLED` 未开启，零侵入现有 MVP 部署。 |
| 2 | **Prompt 对齐** | Python 与 Node 共用同一 JSON 结构与约束文案，输出可互换校验。 |
| 3 | **三级降级链** | Python 宕机 / 超时 / 502 自动回退 Node LLM，再回退模板。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **双栈 env 同步** | Python 读取 monorepo 根 `.env`，与 Node 共用 `DEEPSEEK_*` / `LLM_*`，避免两套密钥。 |
| 2 | **LangChain JSON 模式** | DeepSeek 用 `response_format=json_object`，失败时去掉该参数重试（与 Node 一致）。 |
| 3 | **类型双端校验** | Python Pydantic + Node Zod 各验一遍，防止微服务返回脏数据入库。 |

---

## A3++ 草稿编辑入口分离

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-22 |
| **路线图** | A3 体验优化 |
| **关联代码** | `pages/routes/detail.vue`（`showDraftEdit`）；`pages/routes/list.vue`（`edit=1`  query） |

### 背景与目标

A3 已在详情页提供草稿名称/简介编辑。C1 生成流落地后，用户首屏更需要看行程而非编辑表单；需把「改标题简介」收敛到「我的路线 → 草稿」 intentional 路径。

### 思考过程

1. **生成后详情页信息过载** 用户刚生成完更关心行程节点，「编辑草稿」名称/简介块抢注意力。  
2. **编辑能力要不要删？** 不删；改名称/简介仍是有效需求，应放在「我的路线 → 草稿」 intentional 入口。  
3. **如何区分场景？** 列表打开草稿时带 `?edit=1`；规划页/订单等跳转不带参，详情页 `allowDraftEdit` 为 false。

### 实现原理

`onLoad` 解析 query `edit=1` 写入 `allowDraftEdit`；模板 `v-if="showDraftEdit"` 三条件与运算。列表 `goDetail` 对 mine  scope 下草稿项拼接 query 参数。

### 最终方案

- 详情页：`showDraftEdit = isDraft && isOwner && allowDraftEdit`。  
- `我的`  scope 下点击**草稿**路线：`/pages/routes/detail?id=&edit=1`。  
- 规划页「查看路线详情」、重新生成后刷新等路径不变，无编辑卡片。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **零后端改动** | 纯前端路由参数区分场景，风险小。 |
| 2 | **生成流更干净** | 首屏聚焦行程与发布/追问，符合 C1 对话规划主路径。 |
| 3 | **草稿管理仍完整** | 从列表进入仍可改标题简介，与 A3 验收点不冲突。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **「全部」里点草稿** | 只要 `item.status === DRAFT` 且 scope 为 mine 即带 `edit=1`，不限于「草稿」筛选 Tab。 |
| 2 | **深链与分享** | 分享链接无 `edit=1` 时只读详情，避免他人看到编辑表单（仍受 isOwner 约束）。 |

---

## G8 生产部署与 API 公有化

| 项 | 说明 |
|----|------|
| **完成日** | 2026-05-23（2026-05-24 补充 MySQL/PM2 踩坑） |
| **路线图** | G8 |
| **关联代码** | `scripts/deploy-server.mjs`、`scripts/fix-mysql-password.mjs`、`scripts/setup-debian12.sh`、`scripts/install-nginx-auto.sh`、`scripts/install-nginx-certbot.sh`、`scripts/install-nginx-ssl.sh`；`deploy/docker-compose.prod.yml`、`deploy/ecosystem.config.cjs`、`deploy/nginx/*.template`；`packages/server/src/config/env.ts`、`packages/server/src/routes/health.ts`；`docs/deploy-production.md`、`docs/启动与部署流程.md`、`docs/env-environments.md`；`packages/server/src/utils/public-asset-url.util.ts` |

### 背景与目标

微信小程序上线要求 API **HTTPS + 合法域名**。本地 `127.0.0.1:3000` 无法真机 uploadFile；需将 Node API 部署到腾讯云轻量服务器（`api.yxtao.site`），并沉淀**可重复的一键发版流程**，避免每次部署手工配 Nginx/Certbot。

### 思考过程

1. **系统选 Debian 还是 OpenCloudOS？** 轻量机常见 OpenCloudOS 的 Nginx 目录、dnf、SELinux 与文档差异大；重装 **Debian 12** 后 `apt` + `sites-available` 与社区教程一致，踩坑更少。  
2. **进程怎么守护？** 开发用 `pnpm dev:server`，生产用 **PM2** + `deploy/ecosystem.config.cjs`；MySQL/Redis 用 **Docker** 且端口只绑 `127.0.0.1`，对外仅 Nginx 80/443。  
3. **2G 内存能否跑 `tsc`？** 生产 `pnpm build:server` 在轻量机上常卡在 `@douxing/server` 的 `tsc`；改为 **tsx 直跑源码**（仍 build `@douxing/shared`），PM2 通过 `SERVER_RUNTIME=tsx` 或内存 ≤4G 自动启用。  
4. **Nginx 与 deploy 要不要分两步？** 用户每次 deploy 后再跑 certbot 体验差；从 `.env` 的 `API_PUBLIC_BASE_URL` 解析域名，`deploy:server` 末尾调用 `install-nginx-auto.sh`：**HTTPS 已通则跳过**，已有 Certbot 配置则不覆盖。  
5. **静态资源 URL 怎么给小程序？** DB 存相对路径 `/uploads/...`，接口返回时按 `API_PUBLIC_BASE_URL` 动态拼 HTTPS，避免历史 localhost 写入库。  
6. **三套环境怎么区分？** 根目录 `.env.development` / `.env.staging` / `.env.production` 管前端打包；服务器 `.env` 管后端；文档 [env-environments.md](./env-environments.md) + [启动与部署流程.md](./启动与部署流程.md) 统一说明。  
7. **改 `.env` 数据库密码后 API 仍 Access denied？** 分两类：**MySQL 数据卷**里用户密码仍是旧值（`MYSQL_*` 环境变量只在容器**首次初始化**时生效）；**PM2 进程**可能缓存旧 `DATABASE_URL`，且 `dotenv` 默认不覆盖已有环境变量——部署 shell 里 `wait-only` 能连上，PM2 内查库仍失败。  
8. **`/api/health` 能通是否代表数据库正常？** 否。health 不查库，仅返回静态 JSON；后台任务（如 `[order-timeout]`）或登录接口才会暴露真实连接问题。  
9. **改密后如何可靠发版？** 提供 `pnpm fix:mysql-password`（宿主机 `DATABASE_URL` 验证 + 容器内 `ALTER USER`）；PM2 改为 **delete + start**（不用 reload）；`env.ts` 设 `dotenv override: true`；`ecosystem.config.cjs` 启动时注入完整 `.env`。

### 实现原理

`pnpm deploy:server`：校验生产 `.env` → Docker compose 起 MySQL+Redis → `pnpm install` →（低内存则 shared build + tsx）→ 宿主机 `wait-only` 验连 → `db:migrate` → **PM2 delete + start**（注入 `.env`）→ 健康检查 → **Nginx 自动配置**。Nginx 脚本检测 `curl https://域名/api/health`，失败则写反代配置；Let's Encrypt 证书存在则装 SSL 模板，否则 HTTP + 可选 `NGINX_CERTBOT_EMAIL` 自动 certbot。

**数据库密码与连接链路**：

```text
.env（MYSQL_PASSWORD + DATABASE_URL 三处须一致）
    ↓
PM2 进程环境变量（reload 可能残留旧 DATABASE_URL）
    ↓
dotenv 加载（override: true 后以 .env 为准）
    ↓
Node API → 127.0.0.1:3307 → Docker MySQL（MySQL 侧见来源 IP 172.18.0.1）
```

改 `.env` 只影响**新启动**的 Docker 卷初始化；已有 `mysql_data` 卷须在容器内 `ALTER USER`，或 `pnpm fix:mysql-password --recreate` 清卷重建。

### 最终方案

- **一键部署**：`pnpm deploy:server --skip-docker --use-tsx`（2G 机推荐）。  
- **Nginx 合并进 deploy**：`.env` 设 `NGINX_AUTO_SETUP=true`、`API_PUBLIC_BASE_URL`；跳过：`--skip-nginx`。  
- **HTTPS**：`install-nginx-certbot.sh` 或 deploy 自动 certbot；已有证书用 `install-nginx-ssl.sh`。  
- **发版**：后端 `git pull` + deploy；小程序本地 `pnpm build:mp-weixin` 上传微信；服务器重启**不必**重配 Nginx（PM2/Docker/Nginx 开机自启）。  
- **运维文档**：`deploy-production.md`（Debian 12）、`启动与部署流程.md`（含发版 checklist）。  
- **改数据库密码**：`.env` 中 `MYSQL_ROOT_PASSWORD`、`MYSQL_PASSWORD`、`DATABASE_URL` 三处一致 → `pnpm fix:mysql-password` → `pnpm exec pm2 delete douxing-api` → `pnpm deploy:server --skip-docker --skip-build`；可接受丢库时用 `pnpm fix:mysql-password --recreate`。  
- **发版后验证**：除 `curl .../api/health` 外，应用登录等查库接口或查看 `pm2 logs` 无 `Access denied`。

### 亮点

| # | 亮点 | 说明 |
|---|------|------|
| 1 | **deploy + Nginx 一步到位** | `API_PUBLIC_BASE_URL` 驱动域名；HTTPS 已通则自动跳过，避免重复配置。 |
| 2 | **tsx 低内存模式** | 内存 ≤4G 自动启用，跳过 server `tsc`，2G 轻量机可稳定发版。 |
| 3 | **文档化发版流程** | 开发/测试/生产启动命令、发版类型决策、重启 vs 发版区分，新人可按文档操作。 |
| 4 | **MySQL 改密工具链** | `fix:mysql-password` 以宿主机 `DATABASE_URL` 验连；`ecosystem` + `dotenv override` 保证 PM2 与 `.env` 一致。 |

### 重难点

| # | 重难点 | 思考与解决 |
|---|--------|------------|
| 1 | **Nginx 首次 SSL 与 certbot 死锁** | 模板不能预写 `listen 443 ssl` 且无证书；首次仅 HTTP:80，certbot 成功后再追加 HTTPS。Debian 12 用 `listen 443 ssl http2`，勿用 `http2 on;`。 |
| 2 | **根域证书不能给 api 子域用** | `yxtao.site` 证书 SAN 不含 `api.yxtao.site`；须单独申请子域证书或 `*.yxtao.site` 通配符，或 Certbot 签 `api.yxtao.site`。 |
| 3 | **`@douxing/shared` 未 build 导致 PM2 报错** | `package.json` exports 指向 `dist/`；`deploy:server` 必须先 build shared；tsx 模式同样依赖 shared dist。 |
| 4 | **MySQL 改 `.env` 不生效** | `MYSQL_*` 仅容器**首次**建卷时写入；已有 `mysql_data` 须 `ALTER USER` 或 `fix:mysql-password --recreate`。`DATABASE_URL` 密码须与 `MYSQL_PASSWORD` 一致。 |
| 5 | **PM2 reload 残留旧 `DATABASE_URL`** | `dotenv` 默认不覆盖进程已有变量；deploy shell 的 `wait-only` 通过但 PM2 内 `[order-timeout]` 仍 Access denied。**解决**：`pm2 delete` + 重新 start；`env.ts` 加 `override: true`；`ecosystem.config.cjs` 注入 `.env`。 |
| 6 | **health 正常但业务接口失败** | `/api/health` 不查库，不能作为数据库验收依据；应测登录等写库接口或看 `pm2 logs`。 |
| 7 | **小程序真机仍连 127.0.0.1** | 须打生产包 `pnpm build:mp-weixin`（读 `.env.production`），并在微信后台配置 uploadFile/request 合法域名；与 MySQL Access denied **无关**。 |

---

## 附录：撰写模板（新任务复制使用）

```markdown
## {任务名称}

| 项 | 说明 |
|----|------|
| **完成日** | YYYY-MM-DD |
| **路线图** | {步骤编号} |
| **关联代码** | {路径} |

### 思考过程
（为何这样做、曾考虑又否定的做法，2～4 条即可）

### 实现原理
（关键链路 / 数据流，一句话到一小段）

### 最终方案
（落地结论，不展开代码细节）

### 亮点（2～3 个）
| # | 亮点 | 说明 |

### 重难点（2～3 个）
| # | 重难点 | 思考与解决 |
```

---

## 维护说明

1. 完成可验收任务后，按模板追加一章，**每章 2～3 亮点 + 2～3 重难点**，不写流水账。  
2. [ROADMAP.md](./ROADMAP.md) §2.4 只保留索引表，指向本章对应锚点。  
3. 若思考过程涉及未采纳方案，简要写明「为何不选」，便于后人避免重复踩坑。

---

*文档版本 1.10 · 最后更新：2026-05-24*
