# 下一步工作

> **定位**：只回答「接下来先做什么、做到什么算完成」。  
> 完整能力清单与历史进度见 [ROADMAP.md](./ROADMAP.md)；合规/资质阻塞见 [后期待办.md](./后期待办.md)；已交付方案细节见 [开发记录-重难点与亮点.md](./开发记录-重难点与亮点.md)。

**最后更新：** 2026-06-01（v0.8.7 · D5 补全 + 手帐全量行程）

---

## 当前状态（一句话）

路线规划、Enricher、手帐分享（H2-a + D5-a + **D5 补全** + 内容/地图扫尾）、景点封面 P0/P1 已可用；**宣传 Sprint 主体结束**，进入「H2-a+ 自定义 + 基础设施补全 + 可选模型/行中能力」阶段。

---

## 建议执行顺序

按 **一条主链 + 两条可并行支线** 推进；人力紧时只做主链前两项。

### 主链（产品 · 分享与内容）

| 序 | 工作包 | 做什么 | 完成标准 |
|----|--------|--------|----------|
| 1 | **H2-a+** 手帐轻量自定义 | 在现有双模板上增加：自定义副标题、2～4 套主题色、每日展示 POI 条数（2/3/4）、是否显示时间；可选角标贴纸 PNG | 路线详情可改选项并重新出图；zh-CN/en-US 不破版；**不做**拖拽画布（归 H2-c） |
| 2 | **A2+++ Phase 2** 封面存储与运维 | 封面迁 **OSS/COS** + CDN 公网 URL；管理端「重新拉取高德封面」；Wikimedia 兜底与孤儿文件清理脚本 | 多机/生产环境封面 URL 稳定；本地 `uploads` 不再为唯一依赖 |
| 3 | **H2-b** 打卡/成就炫耀海报 | 复用 `packages/mobile/src/poster/`：打卡地图缩略 + 徽章/成就模板；成就页、打卡地图页入口 | 用户可生成第二张「炫耀图」并保存/分享 |
| ~~4~~ | ~~**D5 补全**~~ | ~~海报二维码小程序码~~ | **已完成（v0.8.7）** — 见 [开发记录 § D5 补全](./开发记录-重难点与亮点.md#d5-补全--海报二维码与行程全量展示) |

### 支线 A（AI · 可与人并行）

| 序 | 工作包 | 做什么 | 完成标准 |
|----|--------|--------|----------|
| A1 | **I2** PAI LoRA 微调 | 上传 I1 产出的 train/val 至 OSS，按 [阿里云专属模型](./阿里云-兜行专属模型训练与部署.md) 跑通训练 | 微调任务成功，模型 artifact 可注册 |
| A2 | **I3** 百炼接入 | `llmProvider` 增加专属模型；失败降级 DeepSeek；小规模 A/B | 生产可切换；JSON 合法率不低于基线 |

### 支线 B（行程质量 · 可与人并行）

| 序 | 工作包 | 做什么 | 完成标准 |
|----|--------|--------|----------|
| B1 | **H9+ Playbook** | 扩充玩法 seed + Web 管理端 CRUD（见 ROADMAP § H9+） | 运营可维护 playbook，Enricher 推荐理由可配置 |
| B2 | **H9-3 扫尾**（若未全验收） | 班次 Catalog、开放时长、`warnings` 闭馆冲突在详情/流程图可见 | 跨城段有 `scheduleNo` 或明确模板/demo 标记 |

### 行中能力（主链 1～2 完成后再开）

| 工作包 | 做什么 | 完成标准 |
|--------|--------|----------|
| **H8** 位置实时重规划 | GPS + 剩余 POI，一键刷新当日安排 | 行中可触发重排且结果可展示 |
| **H7** 错过景点补救 | 计划 POI vs 打卡/GPS，遗漏列表 + 替补推荐 | 进行中路线可见「应到未到」 |
| **A3-3+** 足迹合并动画 | 打卡地图 `mergeRoutePaths` 多路线播放 | 足迹页可播合并路径 |

---

## 暂不做（避免范围膨胀）

| 项 | 原因 |
|----|------|
| **H2-c** 拖拽画布 / 我的模板 | 单独立项，成本高 |
| **`path-scrapbook` / 第四套海报** | 排版算法成本高，H2-a+ 后再评估 |
| **H5 社交 IM、D2/D3 搭子** | 依赖关系链与产品定稿 |
| **E 商业闭环（盲盒/分佣等）** | 依赖 E2 真支付与主体资质 |
| **H10 UGC 短视频** | 已录入 ROADMAP，待分享与内容稳定后启动 |

---

## 上线与合规（非功能开发）

代码就绪 ≠ 可正式商用。主体与资质按 [后期待办.md](./后期待办.md) **建议顺序**推进：注册公司 → 对公户 → 腾讯云企业认证/短信 → 小程序备案 → 微信支付联调。

与开发可并行准备材料，**不占用**上述主链排期 unless 团队专职运维/商务。

---

## 近期验收自检（发版前）

- [ ] 手帐：`diary-page` / `timeline-columns` **全量节点、全量天数**；长描述、多图、标签同行无裁切；底栏 **二维码/小程序码** 可扫  
- [ ] 地图：打卡点常驻 `序号. 名称` 气泡  
- [ ] 封面：无图 POI 仍可通过 enrich/高德兜底；有图在三端（详情/海报/分享）一致  
- [ ] en-US 主流程 + `Accept-Language` API 抽检  
- [ ] 微信小程序：海报保存相册、分享卡片、地图全屏  
- [ ] 海报 QR：生产 API 已配 `MP_WEIXIN_APPID` + `WECHAT_MINI_APP_SECRET`；或打包 env 已配 `VITE_H5_BASE_URL`

---

## 文档索引

| 文档 | 何时查阅 |
|------|----------|
| [ROADMAP.md](./ROADMAP.md) | 全量步骤状态、版本时间轴、H/I/D/E 阶段表 |
| [后期待办.md](./后期待办.md) | 备案、支付、短信、公司主体 |
| [开发记录-重难点与亮点.md](./开发记录-重难点与亮点.md) | 某功能「为什么这样实现」 |
| [代码解析与审核.md](./代码解析与审核.md) | 新模块 Runbook + Review 清单 |
| [阿里云-兜行专属模型训练与部署.md](./阿里云-兜行专属模型训练与部署.md) | I2/I3 操作步骤 |

---

## 变更记录

| 日期 | 说明 |
|------|------|
| 2026-06-01 | 初版：v0.8.6 后主链 H2-a+ → A2+++ OSS → H2-b；并行 I2/I3、H9+ |
| 2026-06-01 | v0.8.7：D5 补全（海报三层 QR）+ 手帐取消「还有 N 项/天」截断；主链移除 D5 补全 |
