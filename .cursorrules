# 项目基础规范
- 语言要求：所有回复、代码注释、提交信息必须使用中文。
- 函数注释：新增或修改函数时必须同步编写完整中文 JSDoc（`@param`、`@returns`、适用时 `@throws`），详见 `.cursor/rules/code-comments-required.mdc`。
- 代码风格：变量和函数命名使用驼峰式 (camelCase)，常量使用全大写加下划线 (UPPER_SNAKE_CASE)。
- 修改代码前，必须先阅读并理解当前的代码上下文，不要盲目重构。

# 技术栈约束
- 前端组件统一使用函数式组件 + Hooks。
- 样式统一使用 Tailwind CSS。
- 禁止在代码中硬编码敏感信息（如 API Key、密码），必须使用环境变量。
- 新页面须符合三端品牌风格与布局，预留主题与国际化，优先复用/封装通用组件；详见 `.cursor/rules/page-ui-standards.mdc`。
- 用户可见时间展示统一为 `yyyy-mm-dd HH:mm:ss`；详见 `.cursor/rules/display-datetime-format.mdc`。
- **PC 用户端仅服务 C 端**；**全部 B 端操作放 Web**（含 `/partner` 商户工作台）；详见 `.cursor/rules/pc-c-web-b-split.mdc` 与 `docs/PC双平台分工.md`。

# Git 提交规范
- 提交信息必须遵循 Conventional Commits 格式，例如：`feat(auth): 添加用户登录接口` 或 `fix(ui): 修复按钮点击无效的问题`。
- 常用 type 包括：feat, fix, docs, style, refactor, test, chore。
- 第一行概述，第二行空行，第三行详细描述。

# 安全与性能
- 所有用户输入在用于数据库查询前必须进行验证或参数化，防止 SQL 注入。
- 异步函数必须包含 try...catch 进行错误处理。

# 发单接单 M 线文档
- 模块 B（marketplace / M0～M7）阶段或 Step 状态变更时，须同步更新 `docs/发单接单路线图.md` 等关联文档；细则见 `.cursor/rules/marketplace-roadmap-sync.mdc` 与路线图 §10。
