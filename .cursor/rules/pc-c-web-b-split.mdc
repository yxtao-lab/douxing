---
description: PC 用户端仅服务 C 端；全部 B 端操作放在 Web 管理端/商户工作台
alwaysApply: true
---

# PC（C 端）与 Web（B 端）分工

完整说明见 [PC双平台分工.md](../docs/PC双平台分工.md)。

## 强制

| 端 | 包 | 面向角色 | 允许能力 |
|----|-----|----------|----------|
| **PC 用户端** | `packages/pc` | **仅 C 端用户** | 旅行自助（规划/路线/打卡/相册等）+ **发单方**（定制服务大厅、发单、我的需求、团体） |
| **Web** | `packages/web` | **全部 B 端** + 平台运营 | 管理后台；**商户/服务者工作台** `/partner`（入驻、接单、报价、履约） |
| **移动端** | `packages/mobile` | C 端为主 | 与 PC 对齐的 C 端能力；可有轻量引导，**完整 B 端闭环仍以 Web `/partner` 为准** |

## 禁止

- 在 **`packages/pc`** 新增或迁移任何 **B 端**页面/入口：商户入驻、服务者认证工作台、接单大厅、我的报价、卖方订单履约、商户财务/排期等
- 用 PC 用户端「冒充」或替代 Web **商户工作台**（`/partner`）
- 在 Web 商户工作台放置 **纯 C 端发单方**专属能力（如「我的团体」）——团体发单属 C 端发单方，落 **mobile / PC**

## 新功能落点判断

```text
谁在用？
  ├─ 普通用户 / 发单方（个人或团体发需求） → mobile + PC（C 端）
  └─ 商户 / 个人服务者 / 平台运营          → Web（B 端 / 管理端）
```

| 场景 | 落点 |
|------|------|
| 浏览/发布需求、我的团体、选定报价（买方） | mobile · **PC** |
| 商户入驻、接单大厅、报价、卖方履约 | **Web** `/partner` |
| 入驻审核、需求监管、全站配置 | **Web** 管理菜单 |

## 验收自检

- [ ] 未在 `packages/pc` 增加 B 端路由或菜单
- [ ] B 端操作可在 Web（`/partner` 或管理菜单）完成
- [ ] C 端发单方能力未错误塞进商户工作台
