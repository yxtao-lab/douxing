---
description: 发单接单 M 线代码或阶段状态变更时，必须同步更新路线图与关联文档
globs: "**/marketplace/**,docs/发单接单*.md,docs/下一步工作.md,packages/server/src/scripts/m*-marketplace*.ts"
alwaysApply: false
---

# 发单接单路线图文档同步（M 线）

完整规范见 [发单接单路线图.md §10](../docs/发单接单路线图.md#10-维护约定)。

## 强制

M 线（模块 B marketplace）发生以下任一情况时，**须在同一次交付中**更新文档，**禁止只改代码不改正文**：

- 启动 / 完成某 Phase（M0～M7）或 Step（M1-1 等）
- 验收脚本（`mN:marketplace-*-cases`）首次全绿或指针切换
- 新增 / 变更 `/api/marketplace/*` 接口
- 明确范围外缺口（如「仅 API、无 C 端页」）

## 必改文件（按顺序）

1. **`docs/发单接单路线图.md`**（主文档）
   - §3 对应 Step：`[ ]` → `[x]`，勾选验收标准
   - §2 里程碑（阶段验收时 `[x]` + 日期）
   - §0.1 当前步 / 上一阶段 / 已知缺口
   - §0.2 进度条 · §4 依赖图（阶段完成时）
   - 文首版本号 + **最后更新**（含迁移号、验收命令、指针 → 下一阶段）
   - **变更记录** 追加一行
2. **`docs/下一步工作.md`** — M 线表格与文首指针
3. **`docs/发单接单平台.md`** — 文首「实施状态」（若 M 阶段状态变）
4. **API 变更时**：`docs/API接口文档.md` §25 · `docs/openapi.yaml`（对应路线图 M1-9 类 Step）

## 验收门禁

- 文档标 `[x]` / 里程碑 ✅ **之前**，须先跑通 §6 验收命令（如 `m1:marketplace-onboard-cases`）。
- §0 指针与 §3 Step 表**必须一致**（禁止只改其一）。

## 版本号

- 单 Step 扫尾：补丁 +0.0.1
- 整阶段验收 / MB 达成：次版本 +0.1.0

## 自检

- [ ] §0 当前步 = 第一个未验收 Phase 的下一步？
- [ ] 变更记录已写清「完成了什么 · 指针指向哪里」？
- [ ] C 端 / OpenAPI 等已知缺口是否在 §0.1 或 Phase「不做」中说明？
